# DECISIONS

스펙에 없거나 해석이 갈리는 부분을 어떻게 정했는지 적는다.

## 범위·환경

- **개인용 v1.** LeeoKit(피드백·사용 통계)과 수익화는 넣지 않았다. 출시할 때 붙인다.
- **UI는 한국어.** 문자열은 SwiftUI 리터럴 그대로 두고 `Localizable.xcstrings`(원본 언어 ko)를 추가했다. 영어가 필요해지면 카탈로그에 번역만 더하면 된다.
- **XcodeGen.** `project.yml`에서 `xcodegen generate`로 프로젝트를 만든다. 번들 ID는 `com.leeo.QARotation`, App Group은 `group.com.leeo.QARotation`이다. `DEVELOPMENT_TEAM`은 비워 두었다. 기기에 설치하려면 팀을 지정하고 App Group을 등록해야 한다.
- **Swift 6 + `SWIFT_STRICT_CONCURRENCY=complete`.** 경고 0으로 빌드된다.

## App Store 가져오기

- **artistId 기본값은 `1502508537`.** 클립키보드(1543660502)를 lookup해서 확인했다. 설정에서 바꿀 수 있다.
- **스토어는 `kr,us` 두 곳을 합친다.** 스펙의 URL에는 country가 없어서 미국 스토어만 조회된다. 실제로 조회해 보니 한국 45개, 미국 46개였고, 합치면 46개다. 같은 앱이면 먼저 적은 스토어(kr)의 이름을 쓴다.
- **`entity=software` 한 번이면 Mac 앱도 온다.** `kind: "mac-software"`로 5개가 온다. `macSoftware`를 따로 부를 필요가 없었다.
- **짝짓기: App Store ID가 먼저, 없으면 번들 ID로.** 번들 ID로 짝짓는 것은 직접 추가한 앱이 나중에 스토어 앱과 합쳐지게 하기 위해서다.
- **덮어쓰는 값과 건드리지 않는 값을 나눴다.** 스토어가 주인인 값(이름·번들 ID·아이콘 URL·버전·플랫폼)만 덮어쓴다. 사용자가 정한 값(등급·URL 스킴·메모·보관·전용 체크 항목·기록)은 절대 건드리지 않는다. 지우는 동작은 코드에 아예 없다.
- **아이콘은 180px PNG로 줄여 SwiftData에 저장한다.** 위젯은 네트워크 이미지를 못 띄우고, 큰 이미지는 메모리 한도에 걸린다. 아이콘 URL이 바뀌면 다시 받는다.

## 로테이션

- **경과 일수는 달력 날짜로 센다.** 어제 밤 11시에 했으면 오늘은 1일이다. 따라서 오늘 QA한 앱은 점수가 0이다.
- **한 번도 안 한 앱끼리는 최근에 추가된 것이 먼저다.** 한 번에 가져온 앱들은 같은 시각을 받으므로 그 안에서는 등급 → 이름 순이다. 그래서 "새로 가져온 앱이 맨 앞"이 성립한다.
- **동점일 때는 마지막 QA가 더 오래된 쪽 → 이름 순이다.**
- **건너뛰기는 오늘 하루만 기억한다.** App Group UserDefaults에 두어서 앱과 위젯이 같은 추천을 본다. 전부 건너뛰면 방금 건너뛴 것만 남기고 한 바퀴 다시 돈다. QA 기록은 남지 않는다.
- **사이클의 뜻.** "지금 로테이션에 있는 모든 앱을 한 번씩 QA하는 한 바퀴"로 정했다. 기록을 시간순으로 훑다가 모든 앱이 채워지면 다음 사이클로 넘어간다. 보관한 앱의 기록은 세지 않는다. 앱을 새로 가져오면 분모가 바로 늘어난다.
- **"밀린 앱"은 마지막 QA 후 21일을 넘긴 앱만 센다.** 한 번도 안 한 앱은 "아직 QA 안 한 앱 N개"로 따로 보여 준다. 첫 가져오기 직후 46개가 전부 밀린 것으로 보이지 않게 하려는 것이다.
- **`lastQADate`는 `TrackedApp`에 따로 저장한다.** 세션에서 파생되는 값이지만 목록 정렬과 위젯 조회를 가볍게 하려는 것이다. 세션을 지우면 다시 계산한다.

## QA 세션

- **완료를 누르기 전에는 아무것도 저장하지 않는다.** 닫으면 기록이 남지 않는다(진행한 게 있으면 확인을 받는다).
- **같은 결과를 다시 누르면 선택이 풀린다.** 잘못 누른 것을 되돌리는 용도다.
- **"나머지 N개 통과" 버튼을 넣었다.** 10개 모두 통과일 때 필요한 탭 수는 다음과 같다.
  - 항목별로 누르면: 시작 1 + 항목 10 + 완료 1 = 12
  - 이 버튼을 쓰면: 시작 1 + 나머지 통과 1 + 완료 1 = 3
- **실패를 누르면 메모 칸과 스크린샷 버튼이 그 자리에서 펼쳐진다.** 시트를 따로 띄우지 않아 탭이 늘지 않는다.
- **답하지 않은 항목은 기록하지 않는다.** 일부만 답하고 완료를 누르면 두 가지 중에서 고른다.
  - 이대로 완료
  - 나머지를 모두 통과로 하고 완료
- **같은 제목의 이슈가 이미 열려 있으면 새로 만들지 않는다.** 이번 결과를 그 이슈에 잇고 메모는 덧붙인다. 매번 QA할 때마다 같은 버그가 쌓이지 않게 하려는 것이다.
- **다시 확인할 이슈**
  - 열린 이슈는 세션 맨 위에 "고쳐졌어요 / 아직 그래요"로 보여 준다. 고르지 않으면 열린 채로 둔다.
  - "고쳐졌어요"를 골랐어도 이번 세션에서 같은 항목이 실패했으면 열어 둔다.
- **자동 기록 값**
  - 기기: `UIDevice.model` + 모델 식별자(예: `iPhone17,1`)
  - OS: `iOS 26.0`
  - 앱 버전: 스토어의 현재 버전
  - 세 값 모두 "기록 정보"에서 고칠 수 있다.
- **세션 날짜는 시작 시각이다.** 걸린 시간은 완료 시각에서 시작 시각을 뺀 값이다.
- **"앱 열기"**
  - URL 스킴이 있으면 그 스킴으로 연다. `myapp`과 `myapp://` 모두 받는다.
  - 열리지 않으면 App Store 페이지로 보낸다.
  - `LSApplicationQueriesSchemes`는 쓰지 않았다(열어 보고 실패하면 대체 경로로 간다).
- **타이머는 막지 않는다.** 1분 남으면 주황, 넘기면 빨간 `+초과 시간`으로 바뀐다.

## 이슈·기록

- **상태를 고침·안 고침으로 바꾸면 `resolvedAt`을 기록한다.** 다시 열림으로 바꾸면 지운다.
- **체크리스트 항목 제목은 결과와 이슈에 복사해 둔다.** 체크리스트를 고쳐도 과거 기록은 그대로다(테스트로 확인).
- **앱 삭제는 앱 목록의 스와이프 → 확인으로만 한다.** 상세 화면에서 지우면 지워진 모델을 보여 주다가 앱이 죽을 수 있다. 로테이션에서만 빼려면 보관을 쓰면 된다.
- **기본 체크리스트는 처음 한 번만 넣는다.** 사용자가 다 지워도 다시 채우지 않는다. 필요하면 "빠진 기본 항목 다시 넣기"를 쓴다.
- **항목마다 "어떻게 확인하는지"를 한 줄로 붙였다.**
  - `ChecklistItem.steps`에 `여는 곳 → 하는 일 → 기대 결과` 꼴로 적는다. 세션에서는 제목 바로 아래 작은 글씨로 늘 보인다. 접었다 펴지 않으므로 탭 수는 그대로다.
  - 기본 11개와 앱 전용 2,462개 전부에 있다. 앱 전용 단계는 각 앱 소스에서 실제 화면·버튼 이름을 확인해 적었다.
  - 예전에 받은 기기를 위해 실행할 때 빈 단계를 채운다(기본 항목은 `ChecklistSeeder.fillMissingSteps`, 앱 전용은 `AppChecklistSeeder.seedNewItems`가 함께 한다).
  - 결과·이슈에는 제목만 복사한다. 단계는 확인하는 방법이지 기록할 내용이 아니다.
- **앱 전용 항목은 기능 전체를 도는 리그레션 목록이다.**
  - `AppChecklistCatalog`에 번들 ID(대소문자 무시)별로 적어 두었다. 앱 47개에 2,462개이고, 한 앱에 41~86개다. 각 앱 저장소의 README·docs·QA 문서·릴리즈 노트·소스를 읽고 뽑았다.
  - 기능 영역별로 `// 섹션` 주석을 달아 앱을 쓰는 순서대로(첫 실행 → 주요 화면 → 부가 기능 → 위젯·확장 → 설정 → 데이터) 늘어놓았다.
  - 이 맥에 소스가 없는 네 앱(LeeoCon·TetraTint·여울·질문노트)만 스토어 설명에서 뽑은 5개짜리다. 소스를 찾으면 같은 방식으로 늘리면 된다.
  - **넣은 제목을 기억한다.** App Group UserDefaults(`seededChecklistTitles`)에 앱별로 넣어 본 제목을 적어 둔다. 그래서 사용자가 지운 항목은 돌아오지 않고, 카탈로그에 새로 적은 항목만 다음 실행에 들어간다. v1.0에서 쓰던 "이 앱은 넣었음" 목록(`seededAppChecklists`)은 그때 넣은 제목을 알 수 없으므로, 그 앱에 지금 있는 항목으로 갈음한다(시뮬레이터에서 321개 → 2,462개로 늘어나는 것을 확인했다).
  - 실행할 때와 가져오기 뒤에 넣는다. 가져오기(`AppImporter`)는 여전히 전용 항목을 건드리지 않는다. 넣는 일은 `AppChecklistSeeder`가 따로 한다.
  - 앱 상세의 "빠진 추천 항목 N개 다시 넣기"로 지운 것만 되살린다.
  - **앱이 바뀌어 문구를 고치면 `renamedTitles`에, 기능이 사라져 항목을 빼면 `removedTitles`에 적는다.** 실행할 때 이미 받은 기기의 항목도 따라 바뀌고 지워진다. 지운 제목은 "넣어 본 제목" 기록에서도 빼서 되살아나지 않는다(시뮬레이터에서 2,462개 → 2,443개로 줄고 옛 제목이 남지 않는 것을 확인했다).

## 위젯·알림

- **위젯 크기**
  - 홈 화면: small, medium
  - 잠금 화면: rectangular, circular, inline
  - 누르면 `qarotation://session/<id>`로 그 앱의 세션이 바로 열린다.
- **위젯 갱신 시점.** 자정에 다시 계산한다(일수가 바뀌므로). 앱에서 다음 일을 하면 즉시 `reloadAllTimelines` 한다.
  - QA 완료, 건너뛰기, 가져오기
  - 등급·보관·가중치 변경
  - 앱이 백그라운드로 갈 때
- **쉬는 시간 알림**
  - 매일 반복 알림이라 문구가 고정된다. 그래서 추천이 바뀔 때마다(위와 같은 시점) 다시 건다.
  - 알림을 누르면 딥링크로 세션이 열린다.
  - 권한이 거부되면 토글을 끄고 이유를 보여 준다.

## 접근성

- **큰 글자(AX1 이상)에서 바뀌는 것**
  - 결과 버튼과 행 레이아웃이 세로로 바뀐다.
  - 오늘 탭의 "QA 시작"이 앱 이름 바로 아래로 올라온다(스크롤 없이 누르도록).
  - 아이콘 크기에 상한을 둔다.
- **세션 하단 고정 막대만 글자 크기를 AX1까지로 제한했다.** AX5에서는 막대가 화면의 1/3을 덮었다. 목록 본문은 제한하지 않는다.
- **VoiceOver**
  - 결과 버튼은 "항목명, 통과"처럼 읽고, 선택되면 `isSelected` 특성이 붙는다.
  - 앱 행은 이름·마지막 QA·이슈 수를 한 문장으로 읽는다.
- **AX5 + 다크 모드에서 오늘 탭과 세션 화면을 시뮬레이터로 확인했다.** VoiceOver는 기기에서 직접 확인해 보지 않았다.

## CloudKit 대비

- 모든 속성에 기본값을 두었다. 관계는 전부 옵셔널이고 unique 제약은 없다.
- 열거형은 원시 문자열로 저장한다.
- 컨테이너는 `cloudKitDatabase: .none`으로 명시했다. 나중에 `.automatic`으로 바꾸고 iCloud 권한을 추가하면 된다.

## 개발용

- DEBUG 빌드에서만 쓰는 실행 인자가 두 개 있다. 릴리스 빌드에는 들어가지 않는다.
  - `-importOnLaunch`: 가져오기를 바로 돌린다.
  - `-deepLink <url>`: 위젯·알림과 같은 경로로 화면을 연다.
- 둘 다 시뮬레이터 확인과 스크린샷용이다.
