import Foundation

/// 스토어에 올린 앱마다 QA할 때 볼 전용 항목. 번들 ID로 찾는다(대소문자 무시).
/// 각 앱의 README·문서·소스를 읽고 기능 전체를 도는 리그레션 목록으로 적었다.
/// 이 맥에 소스가 없는 네 앱(LeeoCon·TetraTint·여울·질문노트)만 스토어 설명에서 뽑은 5개짜리다.
/// 기본 체크리스트와 겹치는 것은 뺐다.
enum AppChecklistCatalog {
    static func key(_ bundleID: String) -> String {
        bundleID.trimmingCharacters(in: .whitespaces).lowercased()
    }

    static func items(for bundleID: String) -> [(ChecklistCategory, String)] {
        byKey[key(bundleID)] ?? []
    }

    private static let byKey: [String: [(ChecklistCategory, String)]] = Dictionary(
        uniqueKeysWithValues: itemsByBundleID.map { (key($0.key), $0.value) }
    )

    static let itemsByBundleID: [String: [(ChecklistCategory, String)]] = [
        // StickyPresenter
        "com.leeo.StickyPresenter": [
            // 첫 실행과 메뉴 막대
            (.stability, "첫 실행에 사용법 스티키 노트가 하나 뜬다"),
            (.appearance, "Dock에 아이콘 없이 메뉴 막대에만 산다"),
            (.platform, "⌘⌃N을 누르면 빈 노트가 새로 뜬다"),
            (.platform, "⌘⌃S·⌘⌃H로 모든 노트가 보였다 숨는다"),
            (.custom, "기본 색 메뉴에서 고른 색으로 새 노트가 난다"),
            (.stability, "모든 창을 닫아도 앱이 메뉴 막대에 남는다"),
            (.platform, "Finder에서 다시 열면 타이머 목록이 뜬다"),

            // 스티키 노트
            (.platform, "전체 화면 Keynote 위에 스티키 노트가 보인다"),
            (.custom, "⌘V로 클립보드 내용이 새 노트가 된다"),
            (.appearance, "마우스를 올려야 노트 조절 단추가 나타난다"),
            (.custom, "색 동그라미를 눌러 여섯 색으로 바꾼다"),
            (.custom, "투명도 슬라이더로 노트가 옅어진다"),
            (.custom, "글자 크기 ±로 10pt에서 32pt까지 간다"),
            (.stability, "자물쇠를 채우면 글이 안 고쳐지고 선택만 된다"),
            (.appearance, "아래 줄에 글자 수가 세어져 보인다"),
            (.stability, "모두 지우기는 한 번 묻고 나서 지운다"),

            // 텔레프롬프터
            (.custom, "텔레프롬프터가 자동 스크롤되고 좌우 반전된다"),
            (.custom, "클립보드 글이 대본으로 들어와 있다"),
            (.stability, "클립보드가 비면 안내 문구가 대신 들어온다"),
            (.custom, "속도 슬라이더로 스크롤이 빨라지고 느려진다"),
            (.custom, "되감기를 누르면 대본이 맨 위로 돌아간다"),
            (.custom, "연필을 눌러 대본을 그 자리에서 고친다"),
            (.appearance, "위아래 그라데이션이 글을 덮어 읽기 쉽다"),

            // 타이머 목록과 입력
            (.custom, "5:30·1h 20m·45s·10이 모두 시간으로 읽힌다"),
            (.appearance, "입력 아래에 해석한 시간이 미리 보인다"),
            (.stability, "못 읽는 입력에는 보내기 단추가 안 생긴다"),
            (.custom, "25/5를 넣으면 뽀모도로 타이머가 선다"),
            (.platform, "⌘⌃B로 25분·5분 뽀모도로가 바로 돈다"),
            (.custom, "타이머가 없을 때만 Quick Start 줄이 보인다"),
            (.custom, "프리셋 편집에서 값을 1~6개로 바꿔 둔다"),
            (.stability, "고친 프리셋이 원문 그대로 다시 열린다"),

            // 타이머 위젯 창
            (.stability, "타이머 창이 첫 클릭부터 끌려 옮겨진다"),
            (.stability, "왼쪽 위 닫기 버튼이 위젯을 감추고 타이머는 계속 흐른다"),
            (.custom, "S·M·L을 눌러 위젯 크기가 세 단으로 바뀐다"),
            (.custom, "오른쪽 아래 그립으로 정사각 비율이 지켜진다"),
            (.appearance, "끝이 가까우면 링과 숫자가 붉게 변한다"),
            (.appearance, "다 끝나면 붉은 테두리가 다섯 번 깜빡인다"),
            (.custom, "테마 단추가 시스템·밝게·어둡게·카멜레온을 돈다"),
            (.stability, "화면 기록을 막으면 카멜레온이 원래 테마로 돌아간다"),
            (.custom, "Window로 열면 신호등 없는 창으로 뜬다"),
            (.custom, "Align을 누르면 위젯이 패널 옆에 붙는다"),

            // 배경음악
            (.custom, "Focus·Calm·Jazz·Upbeat 네 분위기를 고른다"),
            (.stability, "곡이 없는 분위기는 비었다고 알려 준다"),
            (.custom, "폴더 열기로 음원 넣을 자리가 Finder에 뜬다"),
            (.custom, "Play With Timer면 타이머와 함께 음악이 선다"),
            (.stability, "볼륨과 분위기가 다시 켜도 그대로다"),
            (.appearance, "시작·정지가 0.8초에 걸쳐 부드럽게 넘어간다"),

            // 리모컨 연결 메뉴
            (.platform, "Remote 메뉴에 네 자리 코드가 띄어 적힌다"),
            (.platform, "붙어 있는 리모컨 수가 메뉴에 세어져 있다"),
            (.custom, "New Pairing Code를 누르면 코드가 바뀐다"),
            (.stability, "기억한 리모컨이 없으면 잊기 항목이 흐려진다"),
            (.stability, "리모컨 모두 잊기는 한 번 묻고 연결을 끊는다"),
            (.platform, "로컬 네트워크를 막으면 리모컨이 Mac을 못 찾는다"),

            // 알림 센터 위젯
            (.platform, "알림 센터 위젯에서 타이머가 줄어든다"),
            (.platform, "타이머가 없으면 위젯이 빈 안내로 바뀐다"),
            (.platform, "멈춘 타이머는 위젯에 일시정지로 표시된다"),
            (.platform, "끝난 타이머는 위젯에서 붉은 종으로 바뀐다"),
            (.stability, "앱을 끄면 위젯 카운트다운이 멎는다"),

            // 설정과 데이터
            (.stability, "노트 색·투명도·잠금이 재실행 후 남는다"),
            (.custom, "문의 메뉴에서 메일과 인스타그램이 열린다"),
            (.appearance, "시스템 언어가 한국어면 메뉴가 한국어로 나온다"),
            (.stability, "타이머를 지워도 남은 타이머가 계속 돈다"),
            (.stability, "화면을 하나 뽑아도 위젯 창이 남은 화면에 있다"),
        ],
        // StickyPresenterRemote
        "com.leeo.StickyPresenter.Remote": [
            // 연결 전 화면
            (.platform, "Mac 목록이 뜨고 네 자리 코드로 짝이 맺어진다"),
            (.stability, "Mac을 못 찾으면 다섯 줄 점검표가 나온다"),
            (.platform, "iPhone 설정 열기가 로컬 네트워크로 데려간다"),
            (.appearance, "찾는 동안 위쪽 기호가 천천히 뛴다"),
            (.stability, "옛 Mac 앱은 업데이트가 필요하다며 안 눌린다"),
            (.appearance, "지난번에 연결한 Mac이라고 줄 밑에 적힌다"),
            (.accessibility, "글자를 키워도 점검표 설명이 안 잘린다"),

            // 페어링
            (.stability, "코드 칸이 열리자마자 숫자 판이 올라온다"),
            (.stability, "네 자리를 다 채워야 연결 단추가 켜진다"),
            (.stability, "코드를 틀리면 그 자리에 안내가 뜬다"),
            (.custom, "숫자가 아닌 붙여넣기는 걸러져 들어온다"),
            (.stability, "붙는 순간 코드 화면이 저절로 닫힌다"),
            (.appearance, "코드 숫자가 넓게 벌어져 세기 좋다"),
            (.custom, "취소를 누르면 Mac 목록으로 되돌아간다"),

            // 타이머 목록
            (.stability, "남은 시간·진행률이 1초마다 갱신된다"),
            (.custom, "3/5/10/15분 프리셋으로 새 타이머가 시작된다"),
            (.stability, "타이머가 없으면 프리셋만 있는 빈 화면이 된다"),
            (.appearance, "끝난 타이머 줄에 빨간 완료 딱지가 붙는다"),
            (.appearance, "감춘 타이머에는 눈 가림 표시가 붙는다"),
            (.appearance, "뽀모도로는 집중·휴식과 회차가 함께 보인다"),
            (.accessibility, "글자를 키우면 프리셋이 두 줄로 접힌다"),

            // 타이머 조작
            (.custom, "시작·일시정지·±30초가 Mac 타이머에 반영된다"),
            (.custom, "S·M·L을 고르면 Mac 위젯 크기가 바뀐다"),
            (.custom, "초기화를 누르면 시간이 처음으로 돌아간다"),
            (.custom, "표시·감추기로 Mac 위젯이 사라졌다 나온다"),
            (.custom, "정렬을 누르면 위젯이 패널 옆으로 간다"),
            (.stability, "삭제하면 Mac 목록에서도 그 타이머가 빠진다"),
            (.appearance, "한 시간이 넘어도 숫자가 한 줄에 담긴다"),

            // 위치 판
            (.custom, "미니 화면에서 끌면 Mac 위젯이 옮겨진다"),
            (.custom, "판을 톡 찍기만 해도 위젯이 그 자리로 간다"),
            (.platform, "화면이 두 대면 판에도 두 대가 그려진다"),
            (.appearance, "메뉴 막대가 있는 화면에 얇은 띠가 있다"),
            (.appearance, "감춘 위젯은 점선 사각형으로 옅게 보인다"),
            (.stability, "판 구석까지 끌면 위젯이 화면 안에 멈춘다"),
            (.platform, "손을 뗄 때 짧은 진동이 한 번 온다"),
            (.stability, "위젯 창이 없으면 판이 흐려져 안 눌린다"),

            // 연결 유지와 복귀
            (.stability, "재실행하면 코드 없이 짝지은 Mac에 붙는다"),
            (.stability, "연결 표시를 눌러 짝을 풀고 다른 Mac을 고를 수 있다"),
            (.stability, "홈으로 나갔다 돌아오면 다시 붙는다"),
            (.appearance, "오른쪽 위 점이 초록과 주황으로 갈린다"),
            (.stability, "Mac 앱을 끄면 곧 연결 끊긴 화면이 된다"),
            (.stability, "Wi-Fi를 끄면 점검표 화면으로 되돌아간다"),
            (.appearance, "Mac 이름이 길어도 제목을 밀지 않는다"),
        ],
        // 골드위크
        "com.Ysoup.LeaveWise": [
            // 첫 실행·온보딩
            (.custom, "첫 실행에 국가를 고르면 언어가 따라 바뀐다"),
            (.custom, "온보딩에서 총 연차를 넣으면 홈에 반영된다"),
            (.stability, "온보딩을 건너뛰어도 홈이 기본값으로 열린다"),
            (.custom, "직장인·여가 유형에 따라 화면 문구가 달라진다"),
            (.custom, "설정에서 온보딩·튜토리얼을 다시 볼 수 있다"),
            (.appearance, "팁 풍선은 한 번 닫으면 다시 뜨지 않는다"),

            // 홈 화면
            (.stability, "휴가 등록 후 재실행해도 남은 연차가 같다"),
            (.stability, "홈과 사용 내역의 사용 완료 일수가 같다"),
            (.custom, "다가오는 휴가 카드에 공휴일이 함께 묶여 보인다"),
            (.custom, "보너스 합산을 끄면 남은 연차 숫자가 줄어든다"),
            (.appearance, "기록이 하나도 없으면 안내 배너가 뜬다"),
            (.custom, "상황별 추천 배너를 닫으면 한동안 안 뜬다"),
            (.custom, "지난 휴가 빠른 입력으로 사용 일수만 넣는다"),

            // 휴가 등록
            (.custom, "연차·반차·반반차가 0.25·0.5·1로 차감된다"),
            (.stability, "종료일을 시작일보다 앞에 두면 경고가 뜬다"),
            (.stability, "남은 연차보다 많이 넣으면 저장이 막힌다"),
            (.custom, "보너스 연차를 골라 차감 대상으로 쓴다"),
            (.custom, "출장·병가는 연차가 깎이지 않는다고 표시된다"),
            (.custom, "메모를 적으면 내역 목록에 그대로 보인다"),
            (.platform, "사진 가져오기로 휴가 표에서 기록을 뽑는다"),

            // 캘린더
            (.custom, "캘린더 날짜를 눌러 그 날 휴가를 수정·삭제한다"),
            (.appearance, "공휴일·주말·내 휴가가 색으로 구분된다"),
            (.custom, "월 이동 화살표로 앞뒤 달을 넘긴다"),
            (.custom, "숨긴 공휴일은 캘린더에서 사라진다"),
            (.appearance, "공휴일 데이터가 끝나는 해엔 안내 띠가 뜬다"),
            (.stability, "가로로 돌려도 캘린더가 한 화면에 들어간다"),

            // 추천·플래너
            (.monetization, "무료는 추천 일부와 보너스 연차가 잠겨 있다"),
            (.custom, "연차 없이 쉬는 날 구간이 따로 모여 보인다"),
            (.monetization, "무료는 내년 추천으로 넘기면 자물쇠가 뜬다"),
            (.custom, "추천 카드를 누르면 날짜 미리보기가 펼쳐진다"),
            (.custom, "선호 기간·계절·활동을 바꾸면 추천이 달라진다"),
            (.monetization, "연간 플래너는 Pro에서만 계산된다"),
            (.stability, "남은 연차가 0이면 추천이 빈 상태로 안내된다"),

            // 사용 내역·통계
            (.custom, "유형·상태 필터 칩으로 목록이 걸러진다"),
            (.custom, "기록 행을 눌러 날짜·유형·상태를 고친다"),
            (.appearance, "기록이 없으면 빈 상태 화면이 보인다"),
            (.custom, "연간 통계 카드가 사용률을 퍼센트로 보여 준다"),
            (.custom, "소진 속도 그래프가 진행률과 나란히 비교된다"),
            (.custom, "번아웃 타임라인에 오늘 위치가 표시된다"),

            // 공유·가족
            (.custom, "공유 전에 연차 현황 이미지 미리보기가 뜬다"),
            (.platform, "일정 공유를 시작하면 초대 시트가 열린다"),
            (.custom, "공유받은 일정이 있으면 가족 탭이 생긴다"),
            (.custom, "공유 중단을 누르면 참여자 목록이 비워진다"),
            (.stability, "네트워크 없이 공유를 열면 실패 안내가 뜬다"),
            (.custom, "공유받은 사람 칸을 펼치면 다가오는 휴가가 보인다"),

            // 위젯·알림
            (.platform, "연차 현황·D-Day 위젯이 남은 연차를 보여 준다"),
            (.platform, "잠금화면 위젯에 D-day가 작게 나온다"),
            (.platform, "소진 속도 위젯이 갱신일까지 속도를 보여 준다"),
            (.platform, "휴가를 지우면 위젯 숫자가 따라 줄어든다"),
            (.platform, "휴식 레이더 알림에 미루기 버튼이 달려 있다"),
            (.platform, "알림 권한을 거부해도 앱이 정상 동작한다"),

            // 설정
            (.custom, "이름을 고치면 프로필 표시가 바로 바뀐다"),
            (.custom, "연차 기준월을 바꾸면 회계연도 구간이 옮겨진다"),
            (.custom, "공휴일 관리에서 내 공휴일을 추가·삭제한다"),
            (.custom, "언어를 바꾸면 화면 글이 즉시 번역된다"),
            (.custom, "버전 행을 일곱 번 누르면 개발자 화면이 열린다"),
            (.custom, "전체 초기화 후 온보딩부터 다시 시작한다"),

            // 데이터·백업
            (.stability, "타임머신 스냅샷에서 데이터가 복원된다"),
            (.custom, "지금 저장으로 스냅샷이 목록에 추가된다"),
            (.platform, "iCloud 백업 후 복원하면 기록이 되살아난다"),
            (.stability, "iCloud 로그인 없이 백업하면 안내가 뜬다"),
            (.stability, "휴가를 지운 뒤 재실행해도 다시 나타나지 않는다"),
        ],
        // 낫마템포
        "com.leeo.HandsFreeMetronome": [
            // 첫 실행·권한
            (.platform, "첫 실행에 마이크·음성 인식 권한을 묻는다"),
            (.stability, "권한을 허용한 직후에도 앱이 살아 있다"),
            (.custom, "권한을 거부하면 음성 상태 띠가 사용 불가로 뜬다"),
            (.custom, "첫 실행 팁이 마이크·음성 제어 환경에 맞게 뜬다"),
            (.custom, "팁 투어가 악센트·튜너를 차례로 소개한다"),
            (.stability, "마지막 템포·박자·분할이 재실행 후 남는다"),

            // 메트로놈 기본
            (.stability, "시작 버튼을 누르면 클릭이 바로 울린다"),
            (.custom, "± 버튼과 슬라이더로 BPM이 1씩 움직인다"),
            (.custom, "박자 점을 탭하면 2/4~7/4로 바뀐다"),
            (.custom, "빠르기말을 탭하면 BPM 숫자로 뒤집힌다"),
            (.custom, "분할 카드에서 1/8·셋잇단·1/16을 고른다"),
            (.appearance, "재생을 시작하면 설정 카드가 사라진다"),
            (.custom, "재생 중에는 화면이 꺼지지 않는다"),

            // 비트 시각화
            (.appearance, "다운비트엔 빨강, 나머지 박엔 금색으로 화면이 번쩍인다"),
            (.appearance, "진자 공이 포물선을 그리며 타깃에서 터진다"),
            (.appearance, "링 스윕이 12시에서 차오르고 터진다"),
            (.custom, "뷰 버튼으로 진자·궤도·링이 차례로 바뀐다"),
            (.custom, "궤도 화면에서 클릭에 맞춰 탭하면 판정이 뜬다"),
            (.appearance, "동작 줄이기를 켜면 화면 번쩍임이 없어진다"),

            // 음성 제어
            (.custom, "\"start\"/\"stop\" 음성으로 재생·정지된다"),
            (.custom, "\"faster\"·\"tempo 120\" 음성에 BPM이 바뀐다"),
            (.custom, "\"sixteenth\"·\"triplet\"으로 분할이 바뀐다"),
            (.custom, "\"three beats\"라고 하면 박자가 3/4이 된다"),
            (.custom, "\"louder\"·\"mute\"로 클릭 소리 크기가 바뀐다"),
            (.custom, "\"help\"를 말하면 모든 조작에 명령이 붙는다"),
            (.custom, "인식된 명령이 버튼에 잠깐 표시된다"),

            // 악센트·프리셋
            (.monetization, "분할 카드를 더블탭하면 Pro 안내가 뜬다"),
            (.custom, "그리드 칸을 누를 때마다 고음·저음·무음이 돈다"),
            (.custom, "프리셋을 불러오면 박자와 분할도 같이 돌아온다"),
            (.stability, "박자·분할 조합별 패턴이 재실행 후 남는다"),
            (.custom, "Reset을 누르면 기본 악센트로 돌아온다"),
            (.stability, "재생 중 악센트를 고쳐도 소리가 끊기지 않는다"),

            // 연습 모드·튜너
            (.monetization, "무료는 연습 모드·악센트 에디터·튜너가 잠겨 있다"),
            (.custom, "연습 모드가 몇 마디마다 템포를 올려 준다"),
            (.custom, "목표 BPM에 닿으면 템포 상승이 멈춘다"),
            (.custom, "\"tune\"으로 튜너가 옆면처럼 굴러 들어온다"),
            (.custom, "튜너가 음 이름과 센트 눈금을 보여 준다"),
            (.custom, "오른쪽 스와이프나 \"close\"로 튜너가 닫힌다"),

            // 결제
            (.monetization, "잠긴 기능을 누르면 그 기능이 제목으로 뜬다"),
            (.monetization, "가격을 못 불러오면 다시 시도 버튼이 뜬다"),
            (.monetization, "연간 상품에 자동 갱신 고지가 함께 보인다"),
            (.monetization, "\"close\" 음성으로 잠금 안내 화면도 닫힌다"),
            (.monetization, "도움말 시트에 Pro 잠금 상태가 표시된다"),

            // 안정성·접근성
            (.stability, "느린 셋잇단 재생 중 음성 정지에도 크래시 없다"),
            (.stability, "전화·Siri가 끼어들면 메트로놈이 깨끗이 멈춘다"),
            (.stability, "시작·정지를 빠르게 반복해도 소리가 안 깨진다"),
            (.accessibility, "VoiceOver가 템포를 \"분당 120박\"으로 읽는다"),
            (.accessibility, "색 구분 없이를 켜면 튜너가 기호로 알려 준다"),
            (.platform, "보조접근에서 큰 버튼만 남은 화면이 나온다"),
            (.custom, "가로로 돌리면 두 칸으로 나뉘어 다 보인다"),
        ],
        // 달빛
        "com.leeo.LullabyRecipe": [
            // 첫 실행
            (.custom, "첫 실행 안내 네 장을 넘기면 홈이 열린다"),
            (.custom, "건너뛰기를 눌러도 안내가 다시 안 뜬다"),
            (.custom, "홈에서 제스처 코치마크가 조작법을 알려 준다"),
            (.custom, "설정에서 첫 실행 안내를 다시 볼 수 있다"),
            (.appearance, "앱 전체가 항상 어두운 밤 테마로 보인다"),

            // 홈(달)
            (.stability, "화면을 잠가도 소리가 계속 재생된다"),
            (.custom, "달을 길게 누르면 즐겨찾기에 담긴다"),
            (.custom, "좌우로 밀면 다음·이전 소리로 넘어간다"),
            (.custom, "위아래로 밀면 타이머·보관함으로 이동한다"),
            (.appearance, "재생하면 달에 그림자가 서서히 차오른다"),
            (.appearance, "가끔 혜성·행성·우주선이 밤하늘을 지나간다"),
            (.stability, "저장한 소리가 없으면 만들기 안내가 뜬다"),

            // 재생·잠금화면
            (.platform, "잠금 화면 재생 컨트롤로 재생·일시정지가 된다"),
            (.platform, "제어센터에 현재 소리 제목이 보인다"),
            (.stability, "백그라운드에서 돌아와도 재생이 이어진다"),
            (.custom, "미니 플레이어에 남은 타이머 시간이 뜬다"),
            (.custom, "잠시 안 만지면 화면이 서서히 어두워진다"),

            // 보관함
            (.stability, "만든 사운드가 재실행 후 보관함에 남는다"),
            (.custom, "검색창에 이름을 넣으면 결과 수가 표시된다"),
            (.custom, "왼쪽 스와이프로 즐겨찾기에 담고 뗀다"),
            (.custom, "오른쪽 스와이프로 이름 수정·삭제를 한다"),
            (.stability, "기본 제공 소리는 삭제가 막혀 있다"),
            (.custom, "검색 결과가 없으면 안내 문구가 보인다"),

            // 사운드 만들기
            (.custom, "배경음을 고르고 볼륨을 따로 맞춘다"),
            (.custom, "효과음을 여러 개 겹쳐 한 소리로 만든다"),
            (.custom, "레이어마다 볼륨·피치·간격을 조절한다"),
            (.custom, "리듬 두드리기로 재생 간격을 정한다"),
            (.custom, "만드는 중에 소리가 바로 미리 들린다"),
            (.stability, "이름 없이 저장하면 입력을 다시 요구한다"),
            (.custom, "저장하면 보관함에 새 소리가 생긴다"),

            // 타이머·알람
            (.custom, "타이머가 끝나면 소리가 서서히 줄며 멈춘다"),
            (.custom, "타이머 진행 중 남은 시간이 원형으로 돈다"),
            (.custom, "타이머를 일시정지했다가 이어서 돌린다"),
            (.platform, "설정한 알람이 알람음과 함께 울린다"),
            (.custom, "알람에 요일 반복과 이름을 붙인다"),
            (.custom, "싱잉볼·새소리 등 알람음을 골라 쓴다"),
            (.platform, "알람 권한을 거부하면 안내가 나온다"),

            // 결제
            (.monetization, "무료는 내 사운드 3개 뒤로 저장이 막힌다"),
            (.monetization, "잠긴 카테고리를 고르면 구독 화면이 열린다"),
            (.monetization, "무료·구독 기능 비교표가 보인다"),
            (.monetization, "상품을 못 불러오면 다시 시도가 뜬다"),
            (.monetization, "프로모션 코드를 넣으면 만료일이 표시된다"),

            // 설정·기록
            (.custom, "효과음 끄기를 켜면 탭 소리가 안 난다"),
            (.custom, "즐겨찾기만 재생을 켜면 담은 소리만 돈다"),
            (.custom, "화면 어두워짐을 설정에서 끌 수 있다"),
            (.custom, "나의 기록에 누적 청취·연속일이 보인다"),
            (.stability, "기록이 없으면 빈 상태 안내가 뜬다"),
            (.custom, "버전 행을 여러 번 누르면 개발자 화면이 열린다"),

            // 데이터·예외
            (.stability, "재생 중 다른 앱 소리가 끼어들어도 복귀한다"),
            (.stability, "즐겨찾기 표시가 재실행 후에도 남는다"),
            (.custom, "사운드 이름을 바꾸면 보관함에 바로 반영된다"),
            (.accessibility, "VoiceOver로 달에서 다음 소리·즐겨찾기를 누른다"),
            (.custom, "달빛 이야기 화면이 설정에서 열린다"),
        ],
        // 달빛 오락실
        "com.example.MoonlitOrder": [
            // 홈·닉네임
            (.stability, "닉네임이 재실행 후 그대로 남는다"),
            (.custom, "게임 4종 카드가 각각 게임 메뉴를 연다"),
            (.stability, "닉네임을 비우면 방 만들기가 막힌다"),
            (.custom, "작은 화면에서도 홈이 스크롤로 다 보인다"),
            (.stability, "게임 중 앱을 다시 열면 이어서 하기 카드가 보인다"),
            (.custom, "오른쪽 위 톱니로 지원 화면이 열린다"),

            // 연결·대기실
            (.platform, "방 만들기 때 로컬 네트워크 권한 요청이 뜬다"),
            (.custom, "참가 화면에 주변 방 목록이 나타난다"),
            (.custom, "방이 하나도 없으면 찾는 중 안내가 뜬다"),
            (.custom, "대기실에 참가자 수와 필요한 인원이 보인다"),
            (.stability, "최소 인원이 안 차면 시작 버튼이 막힌다"),
            (.custom, "참가자가 들어오면 대기실 목록이 바로 늘어난다"),

            // 달빛 결사
            (.custom, "역할 카드는 버튼을 눌러야 공개된다"),
            (.custom, "리더가 정해진 인원만큼만 원정대를 고른다"),
            (.custom, "찬반 투표 결과가 전원에게 공개된다"),
            (.custom, "연속 부결 횟수가 화면에 표시된다"),
            (.custom, "결사단원은 실패 카드를 낼 수 없다"),
            (.custom, "원정 3회 성공 뒤 암살 단계가 열린다"),
            (.custom, "종료 화면에서 전원 역할이 공개된다"),

            // 라이어 게임
            (.custom, "라이어에게만 제시어가 감춰져 보인다"),
            (.custom, "발언 순서가 차례로 넘어간다"),
            (.custom, "표가 갈리면 라이어가 이긴다"),
            (.custom, "잡힌 라이어에게 마지막 추측 기회가 온다"),
            (.custom, "카테고리가 화면 위에 표시된다"),

            // 달 없는 밤
            (.custom, "밤에 무당·밤손님·장난꾼이 각자 행동한다"),
            (.custom, "도깨비가 혼자면 중앙 카드를 볼 수 있다"),
            (.custom, "낮 토론 뒤 방장이 투표를 시작한다"),
            (.custom, "동률이면 지목된 사람이 모두 추방된다"),
            (.custom, "결과에서 중앙 카드까지 공개된다"),

            // 달빛 화실
            (.custom, "화가가 제시어 셋 중 하나를 고른다"),
            (.custom, "그린 선이 다른 기기에 곧바로 나타난다"),
            (.custom, "되돌리기와 전체 지우기가 동작한다"),
            (.custom, "정답을 맞히면 점수가 올라간다"),
            (.custom, "그림을 탭하면 키보드가 내려간다"),
            (.custom, "라운드 수와 최종 순위가 표시된다"),

            // 게임방법(봇)
            (.custom, "게임방법에서 봇과 한 판을 끝까지 할 수 있다"),
            (.custom, "단계마다 안내 문구가 먼저 뜬다"),
            (.custom, "연습 도중 홈으로 빠져나올 수 있다"),
            (.custom, "봇이 알아서 투표와 카드를 제출한다"),
            (.custom, "게임방법은 인터넷 없이 혼자서도 된다"),

            // 방장·GM
            (.custom, "방장 왕관 버튼으로 GM 제어판이 열린다"),
            (.custom, "멈춘 단계를 강제로 넘길 수 있다"),
            (.custom, "응답 없는 참가자를 내보낼 수 있다"),
            (.custom, "역할·정답 열람 전에 경고가 먼저 뜬다"),
            (.custom, "게임 중단하고 대기실로 돌아갈 수 있다"),

            // 끊김·복귀
            (.custom, "게임 중 화면이 자동으로 잠기지 않는다"),
            (.stability, "연결이 끊기면 재접속 대기 배너가 뜬다"),
            (.stability, "앱을 다시 열면 같은 역할로 돌아간다"),
            (.stability, "참가자가 빠져도 남은 인원으로 진행된다"),
            (.stability, "Wi-Fi를 끄면 방 목록이 비워진다"),
            (.stability, "백그라운드에 다녀와도 단계가 유지된다"),
        ],
        // 돈꼬마트
        "com.leeo.DontGoMart": [
            // 첫 실행·매장 선택
            (.stability, "매장을 하나도 안 고르면 안내가 뜬다"),
            (.stability, "여러 마트 선택이 재실행 후 남는다"),
            (.custom, "지역별 대형마트 휴무 요일을 골라 켠다"),
            (.custom, "코스트코 지점을 따로 선택할 수 있다"),
            (.custom, "설정을 닫으면 홈 숫자가 바로 갱신된다"),
            (.custom, "매장을 모두 끄면 다음 휴무일이 비어 보인다"),

            // 홈 화면
            (.custom, "오늘 갈 수 있는지 한 줄로 크게 답한다"),
            (.appearance, "전부 휴무면 🚫, 일부면 ⚠️로 바뀐다"),
            (.custom, "다음 휴무일 카드에 D-Day가 표시된다"),
            (.custom, "2주 넘게 남은 휴무일은 몇 주 뒤인지 보인다"),
            (.stability, "자정을 넘겨 돌아오면 오늘 상태가 갱신된다"),
            (.custom, "여러 마트를 고르면 휴무 매장이 나열된다"),

            // 캘린더
            (.stability, "선택한 마트의 휴무일이 캘린더에 표시된다"),
            (.custom, "화살표로 지난 달·다음 달을 넘긴다"),
            (.custom, "날짜를 누르면 그 날 휴무 매장이 나온다"),
            (.custom, "휴무가 없는 날은 없다고 알려 준다"),
            (.appearance, "마트마다 다른 색으로 구분되어 보인다"),
            (.accessibility, "VoiceOver가 날짜와 휴무 여부를 함께 읽는다"),

            // 나만의 마트
            (.custom, "커스텀 매장 휴무 패턴이 저장·표시된다"),
            (.custom, "매주·격주·매월 주차 세 방식을 고른다"),
            (.custom, "격주는 첫 휴무일을 달력에서 고른다"),
            (.custom, "패턴을 고르면 미리보기 문구가 나온다"),
            (.stability, "이름이나 패턴이 비면 저장이 막힌다"),
            (.custom, "커스텀 매장을 지우면 캘린더에서도 빠진다"),

            // 공유
            (.custom, "휴무 카드 이미지를 만들어 공유한다"),
            (.custom, "인스타 스토리 공유 버튼이 따로 있다"),
            (.stability, "인스타가 없으면 설치 안내가 뜬다"),
            (.custom, "스크롤하면 공유 버튼이 동그랗게 접힌다"),
            (.custom, "공유 카드에 휴무 매장 이름이 들어간다"),

            // 알림
            (.platform, "알림이 설정한 시각에 정확히 온다"),
            (.platform, "전날 장보기 좋은 날 알림이 온다"),
            (.custom, "며칠 전에 알릴지 고를 수 있다"),
            (.platform, "알림 권한을 거부하면 토글이 꺼진 채 안내된다"),
            (.stability, "알림을 꺼도 예약이 남지 않는다"),
            (.platform, "알림을 탭하면 앱이 열린다"),

            // 위젯
            (.platform, "D-Day·일요일 영업 위젯이 맞게 보인다"),
            (.platform, "휴무 임박 위젯이 일주일 안에 경고한다"),
            (.platform, "다음 휴무 두 개를 보여 주는 위젯이 있다"),
            (.platform, "잠금화면 위젯에 일요일 영업 여부가 뜬다"),
            (.platform, "매장을 바꾸면 위젯 내용도 따라 바뀐다"),
            (.platform, "위젯 갤러리에 이름과 설명이 한국어로 나온다"),

            // 후원
            (.monetization, "후원 완료 시 콘페티와 ☕️ 배지가 뜬다"),
            (.monetization, "커피·케이크·식사 세 가지를 고를 수 있다"),
            (.monetization, "상품을 못 불러오면 다시 시도가 뜬다"),
            (.monetization, "후원을 취소하면 결과 알림이 뜬다"),
            (.monetization, "후원자에게 가끔 감사 인사가 다시 뜬다"),
            (.custom, "후원 카드를 다음에로 닫으면 한동안 안 뜬다"),

            // 설정·데이터
            (.custom, "설정에서 문의를 보내고 리뷰를 남긴다"),
            (.custom, "개인정보 처리방침과 지원 페이지가 앱에서 열린다"),
            (.custom, "버전 행을 일곱 번 누르면 개발자 화면이 열린다"),
            (.stability, "네트워크 없이도 휴무일 계산이 된다"),
            (.stability, "기기를 돌려도 캘린더 칸이 깨지지 않는다"),
            (.custom, "영어 기기에서 화면 글이 모두 영어로 나온다"),
        ],
        // 두번알림
        "com.xa.toki": [
            // 첫 실행과 안내
            (.stability, "첫 실행에서 안내가 순서대로 넘어간다"),
            (.stability, "안내 속 데모 타이머가 실제로 돈다"),
            (.stability, "알림 권한을 거부하면 안내 문구가 뜬다"),
            (.custom, "권한 안내에서 설정 앱으로 바로 간다"),
            (.custom, "안내를 건너뛰어도 타이머 화면이 열린다"),
            (.custom, "설정의 도움말에서 안내를 다시 본다"),

            // 타이머 다이얼과 시작
            (.stability, "다이얼을 돌리면 분·초가 같이 바뀐다"),
            (.accessibility, "다이얼을 돌릴 때 진동이 함께 온다"),
            (.custom, "시간 칸을 눌러 숫자로 직접 입력한다"),
            (.stability, "최소 길이보다 짧게는 잡히지 않는다"),
            (.stability, "일시정지 후 재개하면 남은 시간이 이어진다"),
            (.stability, "백그라운드에 뒀다 와도 남은 시간이 맞다"),
            (.stability, "앱을 껐다 켜도 돌던 타이머가 복원된다"),
            (.appearance, "가로로 돌려도 다이얼이 잘리지 않는다"),

            // 사전 알림
            (.platform, "종료 전 사전 알림이 설정 시점에 온다"),
            (.custom, "프리셋을 눌러 사전 알림을 붙였다 뗀다"),
            (.custom, "마커를 끌어 사전 알림 시점을 미세 조정한다"),
            (.stability, "본 시간보다 긴 사전 알림은 잡히지 않는다"),
            (.monetization, "무료는 사전 알림 두 개째에 안내가 뜬다"),
            (.custom, "사전 알림 목록이 남은 순서대로 선다"),

            // 발표 모드와 오버타임
            (.custom, "모드 고르개에서 발표 모드로 넘어간다"),
            (.appearance, "발표 모드는 글자가 크고 화면이 단순해진다"),
            (.custom, "구간을 나누면 구간별 남은 시간이 선다"),
            (.custom, "구간마다 대본을 적어 두고 펼쳐 본다"),
            (.stability, "0을 지나면 초과 시간이 양수로 쌓인다"),
            (.monetization, "무료는 체험을 다 쓰면 0에서 멈춘다"),

            // 템플릿과 기록
            (.stability, "Pro에서 저장한 템플릿이 재실행 후 불러와진다"),
            (.custom, "템플릿을 눌러 시간·알림이 한 번에 들어간다"),
            (.custom, "템플릿을 밀어 이름 수정·삭제가 된다"),
            (.custom, "템플릿에 라벨 색을 골라 구분한다"),
            (.stability, "템플릿이 없으면 빈 안내가 대신 보인다"),
            (.custom, "사용 기록에 완료율과 총 시간이 나온다"),
            (.custom, "지난번에 쓴 시간과 알림을 다시 알려 준다"),

            // 알림과 소리
            (.custom, "소리·진동·무음 중에서 울림을 고른다"),
            (.stability, "무음 스위치에서도 알람 방식은 크게 울린다"),
            (.custom, "종료 메시지를 직접 적어 두면 그대로 뜬다"),
            (.stability, "메시지를 비우면 시간 기반 문구가 대신 뜬다"),
            (.custom, "테스트 모드 배속으로 알림 시점이 당겨진다"),
            (.custom, "토스트 표시를 끄면 종료 안내가 안 뜬다"),

            // 위젯·워치·단축어
            (.platform, "잠금 화면·다이내믹 아일랜드에 타이머가 보인다"),
            (.platform, "라이브 액티비티 버튼으로 앱 없이 멈춘다"),
            (.platform, "Apple Watch와 타이머가 동기화된다"),
            (.platform, "워치만 켜 두고도 타이머가 끝까지 간다"),
            (.platform, "제어 센터 컨트롤로 타이머를 켜고 끈다"),
            (.platform, "홈 위젯이 남은 시간을 따라 줄어든다"),
            (.platform, "타이머 시작 단축어가 음성으로 실행된다"),
            (.platform, "앱 클립 링크로 설치 없이 타이머를 쓴다"),

            // 구매와 무료 한도
            (.monetization, "복원·코드 교환 직후 재실행 없이 잠금이 풀린다"),
            (.monetization, "무료는 템플릿 저장이 잠기고 시드는 남는다"),
            (.monetization, "잠긴 기능에 남은 체험 횟수가 표시된다"),
            (.monetization, "여섯 번째에 다섯 번 더 쓰기를 고를 수 있다"),
            (.monetization, "열한 번째부터는 구매만 남는다"),
            (.stability, "앱을 지웠다 깔아도 Pro가 유지된다"),
            (.monetization, "앱으로 돌아올 때마다 구매 상태를 다시 본다"),

            // 설정과 데이터
            (.appearance, "강조색을 바꾸면 모든 화면에 바로 퍼진다"),
            (.appearance, "테마를 시스템 대신 직접 고를 수 있다"),
            (.custom, "언어를 바꾸면 워치와 위젯 문구도 함께 바뀐다"),
            (.custom, "내 기기 목록에 연결된 기기가 뜬다"),
            (.stability, "네트워크가 없어도 타이머와 알림은 그대로 된다"),
            (.custom, "피드백 보내기로 글이 접수된다"),
            (.stability, "기록을 지우면 통계가 0으로 돌아간다"),
        ],
        // 마이파이
        "com.leeo.wifisnap": [
            // 첫 화면과 내 와이파이
            (.custom, "연결된 와이파이가 큰 QR 카드로 보인다"),
            (.stability, "연결된 와이파이가 없으면 카메라가 먼저 열린다"),
            (.custom, "비밀번호를 넣기 전에는 QR 버튼이 잠겨 있다"),
            (.custom, "카드를 뒤집으면 손님용 안내판 면이 나온다"),
            (.stability, "설정에서 와이파이를 바꾸고 오면 이름이 갱신된다"),
            (.custom, "첫 실행에 스캔 팁이 한 번 떴다 닫힌다"),
            (.stability, "위치 권한을 거부해도 QR 만들기는 된다"),

            // 찍어서 연결
            (.custom, "안내판을 찍으면 ID/PW가 인식돼 연결된다"),
            (.custom, "앨범 사진에서도 아이디·비밀번호가 읽힌다"),
            (.stability, "카메라가 없는 기기에선 촬영 버튼이 꺼진다"),
            (.custom, "인식 중에는 진행 표시가 돌아간다"),
            (.stability, "글자를 못 읽어도 직접 입력 칸이 열린다"),
            (.custom, "아이디 후보 칩을 눌러 이름을 바꾼다"),
            (.stability, "연결에 실패하면 설정을 되돌리고 알려 준다"),

            // 퍼즐로 고치기
            (.custom, "인식이 틀리면 퍼즐 조각을 끌어 고칠 수 있다"),
            (.custom, "두 칸끼리 끌면 아이디와 비밀번호가 맞바뀐다"),
            (.custom, "조각이 여러 줄로 다 보이게 펼쳐진다"),
            (.custom, "칸에 직접 타이핑해서 고칠 수도 있다"),
            (.custom, "이 폰이 아는 이름도 조각으로 함께 뜬다"),
            (.stability, "규격을 넘는 긴 이름은 받아들이지 않는다"),

            // 저장 목록과 상세
            (.stability, "저장된 네트워크가 재실행 후 남는다"),
            (.custom, "목록 행을 밀어 연결·QR 공유가 된다"),
            (.custom, "행을 눌러 큰 QR과 비밀번호 상세가 열린다"),
            (.custom, "상세에서 비밀번호를 눌러 복사한다"),
            (.custom, "길게 눌러 삭제·안내판 메뉴가 나온다"),
            (.appearance, "긴 이름도 잘리지 않고 줄바꿈된다"),
            (.custom, "근처 네트워크가 뱃지를 달고 위에 선다"),
            (.stability, "저장된 게 없으면 목록 카드가 접혀 있다"),

            // 손님용 안내판
            (.stability, "안내판 문구·테마가 재실행 후 남는다"),
            (.custom, "테마 일곱 가지를 골라 색이 바로 바뀐다"),
            (.custom, "카드형과 A4 세로 중에서 고를 수 있다"),
            (.custom, "매장 이름·로고를 얹어 안내판에 보인다"),
            (.custom, "직접 쓰는 줄을 더하고 지울 수 있다"),
            (.custom, "되돌리기로 기본 문구와 디자인이 돌아온다"),
            (.custom, "안내판을 여러 장 만들어 목록에 쌓인다"),
            (.platform, "인쇄 버튼으로 AirPrint 미리보기가 뜬다"),

            // 지도와 목록 가져오기
            (.custom, "지도에 위치가 남은 네트워크가 핀으로 선다"),
            (.custom, "핀을 누르면 아래 카드에서 QR을 공유한다"),
            (.custom, "설정 화면 캡처에서 와이파이 이름을 뽑아 온다"),
            (.custom, "뽑은 이름을 체크하고 고쳐서 추가한다"),
            (.custom, "가져온 이름은 밀어서 지울 수 있다"),
            (.stability, "캡처에서 건질 이름이 없으면 그렇게 알려 준다"),

            // 위젯
            (.platform, "와이파이 QR 위젯이 고른 네트워크 QR을 보인다"),
            (.platform, "위젯을 눌러 그 네트워크 QR 상세가 열린다"),
            (.platform, "잠금 화면 위젯에 이름과 QR이 함께 선다"),
            (.platform, "원형 위젯은 QR 대신 지름길로 보인다"),
            (.platform, "저장된 게 없으면 위젯에 안내 문구가 뜬다"),
            (.platform, "지금 연결된 것에 초록 표시가 붙는다"),
            (.platform, "목록을 고치면 위젯이 따라 갱신된다"),

            // 데이터와 경계
            (.stability, "비행기 모드에서도 QR 만들기가 된다"),
            (.stability, "한글 이름도 QR로 만들어져 인식된다"),
            (.stability, "백그라운드에서 돌아오면 연결 상태를 다시 본다"),
            (.appearance, "비밀번호가 점으로 가려져 보인다"),
        ],
        // 무지개 공방
        "com.devkoan.ScheduleDensityApp": [
            // 첫 실행과 안내
            (.stability, "처음 열면 한 주 짜는 순서를 안내한다"),
            (.custom, "기본 루틴과 샘플을 넣을지 먼저 묻는다"),
            (.custom, "안내를 설정에서 처음부터 다시 볼 수 있다"),
            (.custom, "닫았던 조언 팁을 한 번에 되살린다"),
            (.stability, "아무것도 없을 때 빈 안내가 그 자리에 선다"),
            (.platform, "⌘1·⌘2로 일간과 주간을 오간다"),

            // 주간 그리드
            (.stability, "할 일을 요일에 끌어 놓으면 계획 블록이 된다"),
            (.custom, "블록을 다른 요일로 끌어 옮길 수 있다"),
            (.custom, "블록을 목록으로 끌어 내리면 할 일로 돌아간다"),
            (.custom, "요일 줄의 점이 그날 계획한 수를 보인다"),
            (.custom, "다른 주로 넘겨도 오늘 자리가 표시된다"),
            (.platform, "⌘Z로 옮기기·지우기·체크가 되돌려진다"),
            (.appearance, "구체성을 못 채운 블록은 옅은 색으로 선다"),

            // 일간 타임라인
            (.stability, "빈 시간을 쓸어 내리면 그 범위에 일정이 잡힌다"),
            (.custom, "양 끝을 끌어 시작과 끝 시각을 다듬는다"),
            (.custom, "가운데를 끌면 시간대가 통째로 옮겨진다"),
            (.custom, "30분 넘게 비면 빈 시간 길이가 적힌다"),
            (.custom, "끌고 오면 놓일 시각이 손 아래에 따라온다"),
            (.appearance, "지나간 빈 시간은 흐리게 보인다"),
            (.custom, "빈 시간을 눌러 들어갈 할 일을 고른다"),

            // 백로그와 쪼개기
            (.custom, "할 일을 바로·펼치고·몰입해서로 나눠 적는다"),
            (.custom, "착수 조건을 고르면 기본 시간이 따라 들어간다"),
            (.custom, "단계 시간을 더하면 상위 할 일 시간이 바뀐다"),
            (.custom, "카테고리로 걸러 목록이 줄어든다"),
            (.custom, "앞 동그라미를 누르면 진행이 한 칸 찬다"),
            (.stability, "모든 단계를 끝낸 할 일은 목록에서 빠진다"),
            (.custom, "프로젝트로 묶어 그 안에 할 일을 적는다"),

            // 고정 루틴
            (.custom, "수면·식사 루틴을 먼저 깔아 둔다"),
            (.stability, "루틴은 달성 체크 줄에 나오지 않는다"),
            (.custom, "수면 시간을 타임라인에서 숨길 수 있다"),
            (.stability, "루틴은 달성률 분모에서 빠진다"),
            (.custom, "루틴 상세에서 요일과 시간을 고친다"),

            // 타이머와 돌아보기
            (.custom, "타이머 멈출 때 적은 다음 첫 동작이 다시 보인다"),
            (.custom, "블록마다 달성·부분·건너뜀을 찍는다"),
            (.custom, "안 돌아본 날에 요일 줄 표시가 선다"),
            (.custom, "주간 회고에 안 찍은 것이 맨 위에 모인다"),
            (.custom, "저녁이면 내일이 비었는지 알려 준다"),
            (.custom, "아침엔 오늘의 첫 걸음 하나가 크게 선다"),
            (.stability, "하루에 한 번 닫으면 그날은 다시 안 뜬다"),

            // 캘린더와 다른 기기
            (.platform, "공유 메뉴로 보낸 글이 백로그에 들어온다"),
            (.platform, "아이폰에서 적은 할 일이 백로그에 보인다"),
            (.custom, "고른 캘린더의 일정만 계획 블록으로 들어온다"),
            (.stability, "캘린더 권한을 거부하면 시스템 설정으로 안내한다"),
            (.stability, "다시 가져와도 손댄 블록은 지워지지 않는다"),
            (.custom, "종일 일정은 하루를 통째로 먹지 않는다"),
            (.stability, "아무 캘린더도 안 고르면 아무것도 안 들어온다"),

            // 설정과 데이터
            (.stability, "재실행 후에도 루틴·계획·백로그가 남는다"),
            (.custom, "설정에 지금 할 일과 분류 개수가 나온다"),
            (.stability, "iCloud가 안 붙으면 설정에 그렇게 적힌다"),
            (.custom, "익명 사용 통계를 설정에서 끌 수 있다"),
            (.stability, "네트워크가 없어도 계획을 적고 옮길 수 있다"),
            (.custom, "전체 삭제는 되돌릴 수 없다고 먼저 묻는다"),
            (.stability, "다시 받아오기 전에 할 일을 파일로 떠 둔다"),

            // 유료화
            (.monetization, "출시 빌드에서는 Pro 잠금 표시가 안 보인다"),
            (.monetization, "설정에 무료·프로 등급이 언제나 적힌다"),
            (.monetization, "회고 추세는 지난 2주까지 무료로 보인다"),
        ],
        // 밑줄
        "com.leeo.bookmarkshot": [
            // 서재와 책 등록
            (.stability, "표지를 찍으면 제목·저자가 채워진다"),
            (.custom, "제목 인식 신뢰도가 퍼센트로 나온다"),
            (.stability, "뒤표지 바코드를 찍으면 ISBN이 들어간다"),
            (.stability, "인식이 틀려도 저장 전에 고칠 수 있다"),
            (.stability, "책이 없으면 첫 책을 담으라는 안내가 뜬다"),
            (.custom, "책 표지가 격자로 늘어선다"),
            (.stability, "카메라 권한을 거부하면 앨범으로 넘어간다"),
            (.custom, "책을 지울 때 한 번 더 묻는다"),

            // 페이지 촬영과 펴기
            (.stability, "네 모서리를 맞추면 페이지가 반듯이 펴진다"),
            (.custom, "모서리 초기 위치가 자동으로 잡혀 있다"),
            (.stability, "편 뒤에는 글자가 더 크고 또렷해진다"),
            (.stability, "페이지 번호가 자동으로 잡히고 고칠 수 있다"),
            (.stability, "기울게 찍어도 반듯하게 펴진다"),
            (.stability, "네트워크가 없어도 글자 인식이 된다"),

            // 밑줄 긋기
            (.stability, "손가락으로 그은 문장만 추출된다"),
            (.custom, "지우개 모드로 잘못 그은 단어를 뺀다"),
            (.custom, "전체 지우기로 그은 것이 한 번에 사라진다"),
            (.custom, "페이지 전체 버튼으로 모든 단어가 잡힌다"),
            (.custom, "두 손가락으로 확대·이동해 정밀하게 긋는다"),
            (.custom, "이동 모드에서는 그어지지 않고 화면만 움직인다"),
            (.custom, "안내 문구가 처음 세 번만 보인다"),

            // 문장 보관함
            (.custom, "전체 문장 검색으로 스크랩이 찾아진다"),
            (.custom, "메모와 책 제목으로도 검색이 된다"),
            (.custom, "즐겨찾기한 문장만 따로 모아 본다"),
            (.stability, "검색 결과가 없으면 빈 화면이 안내된다"),
            (.custom, "책 상세에 그 책의 스크랩 수가 나온다"),
            (.stability, "책을 지우면 그 책 문장도 함께 정리된다"),

            // 스크랩 상세와 공유
            (.custom, "문장을 카드 이미지로 공유할 수 있다"),
            (.custom, "추출된 문장을 직접 고쳐 저장한다"),
            (.custom, "페이지 번호와 메모를 나중에 적어 넣는다"),
            (.custom, "하트를 눌러 즐겨찾기로 올린다"),
            (.custom, "원본 페이지 사진을 다시 열어 본다"),
            (.custom, "텍스트로 공유하면 문장만 복사된다"),
            (.stability, "스크랩 삭제는 되돌릴 수 없게 묻는다"),

            // 오늘의 문장
            (.custom, "매일 문장 하나가 골라져 뜬다"),
            (.stability, "자정이 지나면 다른 문장으로 바뀐다"),
            (.custom, "즐겨찾기가 있으면 그중에서 골라진다"),
            (.stability, "스크랩이 없으면 안내 문구가 대신 선다"),
            (.custom, "오늘의 문장에서 바로 공유 카드를 만든다"),

            // 위젯
            (.platform, "오늘의 문장 위젯이 스크랩을 보여 준다"),
            (.platform, "작은 위젯은 문장이 다섯 줄까지 보인다"),
            (.platform, "큰 위젯에 책 제목과 페이지가 함께 선다"),
            (.platform, "잠금 화면 사각 위젯에 문장이 들어간다"),
            (.platform, "원형 위젯에 스크랩 개수가 나온다"),
            (.platform, "문장을 담으면 위젯이 따라 갱신된다"),
            (.platform, "스크랩이 없는 위젯은 담으라고 알려 준다"),

            // 설정과 데이터
            (.stability, "저장한 문장이 재실행 후에도 서재에 남는다"),
            (.stability, "iCloud가 안 되면 기기에만 저장된다"),
            (.platform, "기기를 바꿔도 같은 계정이면 서재가 따라온다"),
            (.custom, "설정 톱니에서 지원 화면이 열린다"),
            (.appearance, "아이콘이 다크·틴티드에서도 어울린다"),
            (.stability, "사진이 밖으로 나가지 않고 기기에서 처리된다"),
        ],
        // 벅뚜벅뚜
        "com.leeo.FootprintDiary": [
            // 첫 실행과 권한
            (.stability, "안내가 세 걸음으로 넘어가고 끝난다"),
            (.custom, "안내에서 첫 도장을 그 자리에 찍는다"),
            (.stability, "위치를 잡는 동안 찾는 중이라고 적힌다"),
            (.stability, "위치 권한을 거부해도 지도 화면은 열린다"),
            (.custom, "나중에 항상 허용으로 올릴지 묻는다"),
            (.stability, "안내를 마치면 다시 뜨지 않는다"),

            // 내 지도
            (.stability, "걸은 자리가 백지도에 점으로 찍힌다"),
            (.appearance, "오늘 걸음은 붉게, 지난 걸음은 푸르게 보인다"),
            (.custom, "지도 아이콘을 누르는 동안만 배경 지도가 비친다"),
            (.appearance, "석 달 안 밟은 자리는 잿빛으로 물러난다"),
            (.appearance, "자주 지난 칸일수록 점이 짙어진다"),
            (.stability, "차로 지난 구간은 그려지지 않는다"),
            (.stability, "기록이 끊긴 사이는 직선으로 잇지 않는다"),
            (.stability, "정확도가 나쁜 점은 저장돼도 그려지지 않는다"),

            // 도장과 스탬프
            (.stability, "지도를 길게 누르면 도장이 찍힌다"),
            (.custom, "길게 누른 자리가 가까운 내 길에 붙는다"),
            (.custom, "찍은 도장을 끌어 자리를 고칠 수 있다"),
            (.custom, "왼쪽 아래 도장으로 지금 자리에 찍는다"),
            (.appearance, "가볼 곳은 점선 윤곽에 속이 비어 보인다"),
            (.custom, "가볼 곳 가까이 닿으면 굳힐지 묻는다"),
            (.custom, "스탬프가 갈래별로 묶여 한 번에 골라진다"),
            (.stability, "다시 다녀오기는 하루에 한 번만 센다"),

            // 심볼 스티커
            (.custom, "간판을 찍으면 배경이 지워지고 오려진다"),
            (.appearance, "오려 낸 심볼에 흰 테두리가 둘러진다"),
            (.custom, "만든 심볼이 지도 위 도장 자리에 선다"),
            (.stability, "배경이 복잡해도 오려 내기가 끝난다"),
            (.stability, "카메라 권한을 거부하면 안내가 뜬다"),

            // 오늘의 발자취와 목록
            (.custom, "요약 판을 누르면 들른 곳이 시간순으로 선다"),
            (.custom, "자리 사이에 얼마나 걸어 왔는지 적힌다"),
            (.custom, "이미 도장이 있는 자리는 다녀왔는지 묻는다"),
            (.stability, "아니라고 한 자리는 그날 다시 묻지 않는다"),
            (.custom, "목록을 갈래별과 오늘로 나눠 본다"),
            (.custom, "아직 안 가본 곳이 맨 위 한 묶음으로 선다"),
            (.custom, "오래 머문 자리를 하루 두 곳까지 짚어 준다"),

            // 개척 나침반
            (.custom, "여덟 방위의 채움과 뻗음이 도형으로 그려진다"),
            (.custom, "바늘이 가장 얕게 팬 쪽을 가리킨다"),
            (.custom, "판이 기기가 보는 쪽을 위로 두고 돈다"),
            (.custom, "방위마다 채움 퍼센트가 숫자로 나온다"),
            (.stability, "걸은 기록이 없으면 도형이 비어 보인다"),

            // 필름
            (.custom, "기간을 골라 지도가 그려지는 과정을 본다"),
            (.custom, "날짜를 골라 그날 하루만 돌려 본다"),
            (.custom, "영상으로 내보내는 동안 진행률이 오른다"),
            (.custom, "새로 닿은 자리가 지나는 길에 찍힌다"),
            (.stability, "기록이 없는 기간은 만들 수 없다고 알린다"),

            // 일지와 달력
            (.custom, "달력 칸에 그날 걸은 모양이 보인다"),
            (.appearance, "걷지 않은 날은 빈칸으로 남는다"),
            (.custom, "날짜별로 글과 사진을 남길 수 있다"),
            (.custom, "달력 칸을 눌러 그날 일지로 넘어간다"),
            (.stability, "사진 권한을 거부해도 글은 적힌다"),

            // 위젯
            (.platform, "위젯이 오늘 그은 길 모양과 길이를 보여 준다"),
            (.platform, "잠금 화면 위젯은 숫자 한 줄만 보인다"),
            (.platform, "걷고 돌아오면 위젯 모양이 갱신된다"),
            (.platform, "오늘 안 걸으면 위젯이 빈 종이로 선다"),

            // 설정과 데이터
            (.stability, "재실행 후에도 걸음·도장이 그대로 남는다"),
            (.custom, "기록 방식을 정확·배터리 중에서 고른다"),
            (.stability, "iCloud가 안 붙으면 설정에 그렇게 적힌다"),
            (.custom, "걷기 기록 진단에서 안 그려진 까닭을 본다"),
            (.custom, "익명 통계를 설정에서 끌 수 있다"),
            (.stability, "비행기 모드에서도 걸음이 계속 쌓인다"),
            (.platform, "아이패드에서 같은 지도가 보인다"),
        ],
        // 불타는내인생
        "com.burningparchment.app": [
            // 첫 실행 · 시간 설정
            (.stability, "기상·취침 시간을 저장하면 바로 반영된다"),
            (.custom, "추천 취침시간 프리셋을 누르면 값이 채워진다"),
            (.stability, "자정을 넘긴 취침시간도 정상으로 계산된다"),
            (.platform, "첫 실행에서 알림 권한을 한 번 물어본다"),
            (.custom, "기상 전 시간대에는 기상까지 남은 시간이 뜬다"),
            (.stability, "시 피커와 분 피커가 기기 시간 표기를 따른다"),
            (.custom, "취소를 누르면 고친 시간이 저장되지 않는다"),

            // 양피지 카운트다운
            (.stability, "설정한 취침시간까지 카운트다운이 흐른다"),
            (.appearance, "시간이 갈수록 양피지가 위에서 타들어간다"),
            (.appearance, "불씨와 재 입자가 끊기지 않고 떠다닌다"),
            (.stability, "취침시간이 지나면 다 탄 양피지로 바뀐다"),
            (.custom, "취침 90분 전에 회고 넛지 띠가 떴다 사라진다"),
            (.custom, "하루를 못 마치면 이유를 묻는 시트가 뜬다"),
            (.stability, "백그라운드에 뒀다 돌아오면 남은 시간이 맞춰진다"),
            (.accessibility, "손동작 줄이기를 켜면 재 날림이 생략된다"),

            // 기간 페이지 · 데드라인
            (.custom, "좌우로 쓸면 오늘·주·달·해 페이지가 바뀐다"),
            (.stability, "이번 주 페이지가 주 마감까지 남은 몫을 보인다"),
            (.custom, "데드라인을 만들면 목표 페이지가 생긴다"),
            (.monetization, "무료에선 데드라인이 1개까지만 만들어진다"),
            (.custom, "데드라인 카드를 꾹 누르면 삭제가 뜬다"),
            (.stability, "마감이 지난 데드라인은 완료로 표시된다"),
            (.custom, "데드라인이 하나도 없으면 빈 안내가 보인다"),
            (.stability, "이모지와 제목을 고치면 목록에 바로 반영된다"),

            // 재 항아리
            (.stability, "담은 재가 이번 주 항아리에 남아 있다"),
            (.custom, "항아리에 의미를 새기면 재에 색이 돈다"),
            (.custom, "회고를 쓰면 그 날짜의 주 항아리로 들어간다"),
            (.custom, "사분면 그래프를 찍으면 분류가 정해진다"),
            (.stability, "한 톨도 없는 항아리는 바닥이 비어 보인다"),
            (.custom, "이름 없는 지난 항아리를 맨 위에서 물어본다"),
            (.stability, "입력 중에 닫아도 자동 저장 표시가 남는다"),
            (.custom, "내일 할 일을 적으면 다음 날 띠로 올라온다"),

            // 재의 흐름 · 공백 회수
            (.custom, "3일 이상 비면 모아둔 재 카드가 뜬다"),
            (.custom, "날려버리기를 고르면 재가 흩어져 사라진다"),
            (.stability, "그냥 닫으면 공백을 다음에 다시 물어본다"),
            (.appearance, "재의 분포 그래프에 무게중심이 찍힌다"),
            (.custom, "잔불 달력의 날짜를 누르면 그날 회고가 나온다"),
            (.stability, "재가 없을 땐 흐름 화면이 빈 안내로 보인다"),

            // 위젯 · 라이브 액티비티
            (.platform, "다이나믹 아일랜드에 남은 시간이 보인다"),
            (.platform, "잠금화면 위젯이 취침까지 남은 시간을 보여 준다"),
            (.platform, "홈 위젯을 주·달·해용으로 각각 고를 수 있다"),
            (.platform, "데드라인 위젯이 다음 마감을 보여 준다"),
            (.platform, "데드라인이 없으면 위젯에 없음이라 뜬다"),
            (.platform, "앱에서 시간을 고치면 위젯 값도 따라 바뀐다"),
            (.platform, "취침 1시간 전 알림이 한 번 울린다"),

            // 설정 · 꾸미기
            (.appearance, "라이트 테마에서도 불꽃이 보인다"),
            (.custom, "화면 테마를 시스템·라이트·다크로 고른다"),
            (.custom, "탭 표시를 끄면 그 기간 페이지가 사라진다"),
            (.appearance, "페이지 인디케이터 모양을 점·막대로 바꾼다"),
            (.custom, "인디케이터 표시를 끄면 아래 점이 없어진다"),
            (.custom, "프로 구매 뒤에는 이용 중 문구가 보인다"),
            (.custom, "문의 메일 주소가 설정에서 열린다"),

            // 데이터 · 경계 상황
            (.stability, "재실행해도 회고와 데드라인이 그대로 있다"),
            (.stability, "알림 권한을 거부해도 카운트다운은 돈다"),
            (.stability, "비행기 모드에서도 화면이 정상으로 뜬다"),
            (.stability, "제목 없이 저장하려 하면 추가가 막힌다"),
            (.stability, "회고를 지우면 항아리 개수도 함께 준다"),
            (.accessibility, "페이지 제목에서 위아래 쓸기로 이동된다"),
            (.stability, "기기 시간을 바꿔도 남은 시간이 다시 계산된다"),
        ],
        // 세끼
        "com.ysoup.RoutineCamera": [
            // 첫 실행 · 챙길 식사
            (.custom, "첫 실행에서 챙길 식사를 고르는 화면이 뜬다"),
            (.custom, "고르지 않은 식사는 알림이 오지 않는다"),
            (.stability, "시작일 이전 날짜는 거른 끼니로 세지 않는다"),
            (.custom, "처음 3일은 미기록도 빨갛게 보이지 않는다"),
            (.platform, "알림 권한을 거부해도 기록은 그대로 된다"),
            (.custom, "기록할 시간 배너가 홈 위에 한 줄로 뜬다"),

            // 기록하기
            (.stability, "사진을 찍으면 시각에 맞는 끼니로 기록된다"),
            (.custom, "식전·식후를 골라 두 장까지 남길 수 있다"),
            (.custom, "사진 없이도 다 먹음만으로 기록이 된다"),
            (.stability, "카메라 권한이 없으면 설정 열기 안내가 뜬다"),
            (.custom, "앨범에서 고른 사진도 같은 끼니로 저장된다"),
            (.appearance, "찍은 사진에 날짜와 시각이 함께 찍힌다"),
            (.custom, "식후 사진 끄기를 켜면 식전만 한 장 남는다"),
            (.stability, "지난 날짜의 빈 칸을 눌러 소급 기록이 된다"),

            // 피드 · 상세
            (.stability, "기록이 재실행 후에도 피드에 남는다"),
            (.custom, "순간 피드가 오늘·어제로 묶여 시간순으로 선다"),
            (.custom, "줄을 밀면 그 기록이 삭제된다"),
            (.custom, "상세에서 메모를 적으면 사진 아래 남는다"),
            (.custom, "더 보기로 사진앱 저장·교체·삭제가 된다"),
            (.stability, "기록이 하나도 없으면 빈 안내가 보인다"),
            (.custom, "간식 보이기를 끄면 간식 칸이 숨는다"),

            // 통계 · 업적 · 운동
            (.custom, "한 끼만 기록해도 연속 일수가 이어진다"),
            (.custom, "기록한 날 수는 하루 걸러도 줄지 않는다"),
            (.custom, "이번 주·이번 달 기록 일수가 카드로 보인다"),
            (.custom, "업적 목록에서 달성과 잠김이 구분된다"),
            (.custom, "비교 화면에서 두 날 사진을 나란히 본다"),
            (.appearance, "운동 앨범이 날짜 붙은 3열 격자로 보인다"),
            (.custom, "목표 일수를 켜면 진행 바가 헤더에 뜬다"),

            // 친구 · 그룹
            (.custom, "내 친구 코드가 화면에 보이고 복사된다"),
            (.custom, "친구 요청을 보내고 상대가 수락하면 이어진다"),
            (.stability, "없는 코드를 넣으면 이유가 적힌 안내가 뜬다"),
            (.platform, "iCloud 미로그인이면 로그인 안내가 뜬다"),
            (.custom, "6자리 코드로 그룹을 만들고 참여한다"),
            (.custom, "그룹 공개 범위를 기록만·전체로 바꾼다"),
            (.custom, "친구 기록에 콕 찌르기와 댓글이 남는다"),
            (.stability, "네트워크가 끊기면 목록에 다시 시도가 뜬다"),

            // AI 분석
            (.monetization, "AI 분석 시 코인이 1개 차감된다"),
            (.monetization, "무료 분석 잔여 횟수가 설정에 보인다"),
            (.custom, "분석 결과를 메모에 그대로 넣을 수 있다"),
            (.stability, "식후 사진은 분석할 수 없다고 알려 준다"),
            (.monetization, "월 구독을 사면 코인이 매달 채워진다"),
            (.stability, "API 키를 지우면 고급 인식이 꺼진다"),

            // 위젯 · 알림 · 단축어
            (.platform, "위젯의 '다 먹음'이 앱 기록에 반영된다"),
            (.platform, "알림의 '다 먹음'으로 바로 기록된다"),
            (.platform, "단축어 '다 먹음'으로 기록이 남는다"),
            (.platform, "위젯에 오늘 기록 개수가 숫자로 보인다"),
            (.platform, "잠금화면 위젯도 같은 값을 보여 준다"),
            (.platform, "식사 두 시간 뒤 다 드셨어요 알림이 온다"),
            (.platform, "앱을 열면 위젯으로 남긴 기록이 합쳐진다"),

            // 설정 · 데이터
            (.custom, "순간 피드와 격자 화면을 토글로 바꾼다"),
            (.custom, "운동 모드로 바꾸면 식단과 따로 쌓인다"),
            (.custom, "사진앱 자동 저장이 전용 앨범으로 들어간다"),
            (.custom, "예전 기록 공유가 범위와 예상 용량을 보인다"),
            (.stability, "백필을 중단해도 다음에 이어서 올라간다"),
            (.custom, "친구에게 보이는 이름을 고치면 바로 반영된다"),
            (.accessibility, "로터로 오늘과 거른 날로 바로 건너뛴다"),
        ],
        // 쑥쑥용돈
        "com.family.FamilyBank": [
            // 첫 설정
            (.custom, "화폐 이름을 정해야 다음 단계로 넘어간다"),
            (.custom, "PIN 두 번이 다르면 다음으로 못 넘어간다"),
            (.custom, "부모와 자녀 역할을 골라 구성원을 등록한다"),
            (.stability, "설정을 마치면 온보딩이 다시 뜨지 않는다"),
            (.custom, "이모지를 고르면 구성원 카드에 그대로 뜬다"),
            (.stability, "구성원 없이 시작하면 빈 안내가 보인다"),

            // 대시보드 · 구성원
            (.custom, "가족별 총자산이 카드로 한눈에 보인다"),
            (.custom, "물가 카드가 지수와 오른 비율을 보여 준다"),
            (.custom, "구성원을 누르면 지갑·저축·투자가 나뉘어 뜬다"),
            (.custom, "자녀 초대 QR 코드가 보이고 공유된다"),
            (.stability, "자녀가 없으면 투자 화면이 등록 안내를 띄운다"),
            (.custom, "화폐 이름을 바꾸면 모든 화면 단위가 바뀐다"),

            // 용돈 · 결제
            (.stability, "용돈을 지급하면 잔액과 거래 기록에 남는다"),
            (.custom, "차감하면 잔액이 줄고 회수로 기록된다"),
            (.stability, "잔액보다 큰 금액은 결제가 실패한다"),
            (.custom, "결제 이유를 적으면 기록에 함께 남는다"),
            (.stability, "0이나 빈 금액으로는 보내기가 막힌다"),
            (.custom, "결제가 끝나면 완료 알림이 한 번 뜬다"),
            (.custom, "받을 사람이 없으면 안내 문구가 보인다"),

            // 저축과 이자
            (.stability, "저축·투자 잔액이 재실행 후에도 남는다"),
            (.custom, "저축 입금을 하면 지갑에서 그만큼 빠진다"),
            (.stability, "한 주가 지나면 밀린 이자가 한 번에 붙는다"),
            (.custom, "12주 복리 예상 금액이 미리 보기로 나온다"),
            (.stability, "저축보다 큰 금액은 출금이 막힌다"),
            (.custom, "이자가 붙으면 거래 기록에 이자로 남는다"),

            // 투자
            (.custom, "모의 펀드 매수·매도가 잔액에 반영된다"),
            (.custom, "위험도별로 안정·보통·위험 상품이 나뉜다"),
            (.stability, "하루가 지나면 시세가 한 번 바뀐다"),
            (.custom, "보유 수량과 평균 단가, 수익률이 보인다"),
            (.stability, "없는 수량을 팔려 하면 매도가 막힌다"),
            (.custom, "가격 기록이 적으면 차트 대신 안내가 뜬다"),
            (.custom, "상품을 끄면 목록에서 판매가 중지된다"),

            // 가게 · 바코드
            (.custom, "바코드를 찍으면 가게 물품이 결제된다"),
            (.custom, "원 가격을 넣으면 화폐 가격이 자동 계산된다"),
            (.custom, "물품 바코드가 가격표처럼 크게 보인다"),
            (.stability, "카메라 권한을 거부하면 스캔이 막힌다"),
            (.stability, "잔액이 모자라면 결제 실패가 뜬다"),
            (.custom, "물품이 없으면 먼저 등록하라고 안내한다"),

            // 시장 · 기록 · 배우기
            (.custom, "물가 추이 그래프가 주 단위로 이어진다"),
            (.custom, "우리집 통화량이 원 환산으로 보인다"),
            (.custom, "거래 기록이 날짜별로 묶여 보인다"),
            (.stability, "거래가 없으면 빈 기록 안내가 보인다"),
            (.custom, "복리 계산기의 값을 바꾸면 결과가 바뀐다"),
            (.custom, "인플레이션 체험이 천 원의 힘 변화를 보인다"),

            // 부모 모드 · 데이터
            (.custom, "부모 모드는 PIN을 넣어야 열린다"),
            (.custom, "자녀 폰 모드는 PIN 없이 못 벗어난다"),
            (.custom, "자녀 폰 모드에서는 부모 탭이 보이지 않는다"),
            (.custom, "이자율과 물가 상승률을 슬라이더로 바꾼다"),
            (.custom, "PIN 변경 뒤에는 새 PIN으로만 열린다"),
            (.stability, "전체 초기화를 하면 첫 설정으로 돌아간다"),
            (.platform, "같은 iCloud 기기에서 같은 자료가 보인다"),
            (.stability, "인터넷이 없어도 지급과 결제가 동작한다"),
        ],
        // 아이일정
        "com.devkoan.CalendarSnap": [
            // 첫 실행 · 스캔
            (.stability, "달력 사진에서 한 달 일정이 추출된다"),
            (.stability, "눕혀 찍은 달력 사진도 일정이 읽힌다"),
            (.stability, "카메라 권한을 거부하면 앨범으로 넘어간다"),
            (.stability, "글씨가 없는 사진이면 못 찾았다고 알려 준다"),
            (.custom, "처음 열면 스캔 탭에 안내 화면이 보인다"),
            (.stability, "처리 중에는 진행 표시가 화면에 뜬다"),
            (.custom, "사진을 여러 장 찍으면 일정이 쌓여 더해진다"),

            // 추출 결과 편집
            (.custom, "추출된 일정 개수가 목록 위에 적힌다"),
            (.custom, "제목과 시각을 그 자리에서 고칠 수 있다"),
            (.custom, "종일 스위치를 켜면 시간이 사라진다"),
            (.custom, "준비물 메모를 일정마다 적을 수 있다"),
            (.custom, "필요 없는 일정은 밀어서 지운다"),
            (.custom, "일정 직접 추가로 한 건을 손으로 넣는다"),
            (.stability, "같은 일정이 두 번 들어오면 하나만 남는다"),

            // 저장 · 알림
            (.stability, "추가한 일정이 재실행 후에도 남는다"),
            (.platform, "준비물 메모가 알림 본문에 보인다"),
            (.custom, "하단 저장 바가 개수와 함께 늘 보인다"),
            (.platform, "전날 저녁·당일 아침 알림 시점을 고른다"),
            (.platform, "종일 일정에는 한 시간 전 알림이 안 걸린다"),
            (.stability, "알림 권한이 없으면 허용 안내가 뜬다"),
            (.stability, "저장하면 스캔 목록이 비워지고 달력으로 간다"),

            // 문자로 일정 추가
            (.custom, "붙여넣은 문자에서 날짜와 준비물이 읽힌다"),
            (.custom, "내일·이번 주 금요일 같은 말도 날짜가 된다"),
            (.custom, "클립보드 버튼 한 번으로 내용이 채워진다"),
            (.stability, "아무 날짜도 없으면 일정이 만들어지지 않는다"),
            (.custom, "지우기를 누르면 입력칸이 비워진다"),
            (.custom, "추출한 결과를 저장 전에 고칠 수 있다"),

            // 달력 탭
            (.custom, "달력에서 일정이 있는 날에 점이 찍힌다"),
            (.custom, "월간과 주간 보기를 접었다 펼 수 있다"),
            (.custom, "오늘 버튼을 누르면 이번 달로 돌아온다"),
            (.custom, "날짜를 고르면 그 날 일정이 아래에 뜬다"),
            (.stability, "일정이 없는 날은 없다고 표시된다"),
            (.custom, "목록에서 밀면 그 일정이 삭제된다"),
            (.custom, "그 날 일정 추가가 날짜를 미리 채운다"),

            // 아이 프로필 · 반
            (.custom, "아이를 등록하면 일정이 아이별로 나뉜다"),
            (.custom, "동그라미를 눌러 프로필 사진을 넣는다"),
            (.custom, "반을 적으면 다른 반 일정이 걸러진다"),
            (.custom, "아이 미등록이면 저장 전에 이름을 묻는다"),
            (.custom, "아바타로 아이별 일정만 골라 본다"),
            (.stability, "아이를 지우면 확인을 한 번 더 묻는다"),

            // 캘린더 · 가족 공유
            (.platform, "애플 캘린더에 중복 없이 일정이 들어간다"),
            (.platform, "가족 초대 링크로 일정이 공유된다"),
            (.platform, "캘린더 권한이 없으면 권한 안내가 뜬다"),
            (.platform, "미러링을 끄면 애플 캘린더에 안 올라간다"),
            (.custom, "참여자 목록에 함께 보는 사람이 나온다"),
            (.custom, "지금 동기화를 누르면 양쪽이 맞춰진다"),
            (.custom, "공유를 중단해도 받은 일정은 남아 있다"),

            // 위젯 · 공유 · 설정
            (.platform, "위젯이 다가오는 일정을 보여 준다"),
            (.platform, "일정이 없으면 위젯이 찍으라고 안내한다"),
            (.platform, "일정 파일을 보내면 상대 앱에 채워진다"),
            (.platform, "ics 파일과 텍스트로도 공유된다"),
            (.stability, "인터넷이 없어도 스캔과 저장은 된다"),
            (.custom, "설정에서 알림 시점을 바꾸면 다시 예약된다"),
        ],
        // 욕망의 무지개
        "com.example.ScheduleDensityApp": [
            // 첫 실행 · 무지개
            (.stability, "일정이 겹칠수록 무지개 칸이 진해진다"),
            (.custom, "첫 실행에 무지개 사용법 안내가 뜬다"),
            (.custom, "칸을 꾹 누르면 그 날 일정이 만들어진다"),
            (.custom, "오늘 칸에 오늘 표시가 따로 붙는다"),
            (.stability, "일정이 없으면 비었다는 안내가 보인다"),
            (.custom, "종료일까지 이어 표시를 켜면 옅게 깔린다"),
            (.custom, "인사이트 카드를 접었다 펼 수 있다"),

            // 일정 만들기 · 관리
            (.custom, "새 일정에 제목·기간·하루 시간을 넣는다"),
            (.custom, "칸을 꾹 누르면 수정·삭제 메뉴가 열린다"),
            (.stability, "일정을 지울 때 한 번 확인을 묻는다"),
            (.stability, "iCloud에 못 닿으면 삭제가 막힌다"),
            (.custom, "반복 일정에서 이 날짜만 뺄 수 있다"),
            (.custom, "일정 관리 목록에서 전부 한 번에 지운다"),
            (.custom, "지나간 일정 보기를 끄면 옛 줄이 숨는다"),

            // 하루 화면
            (.custom, "하루가 색 알약이 이어진 한 줄로 보인다"),
            (.custom, "30분 넘게 비면 빈 시간 길이가 적힌다"),
            (.custom, "빈 시간에 집을 조각 개수가 함께 뜬다"),
            (.custom, "매여 있는 일이 시계 위에 따로 선다"),
            (.custom, "알약 색이 무지개 줄 색과 같다"),
            (.stability, "하루에 다 안 들어가면 경고 줄이 뜬다"),
            (.custom, "알약을 누르면 그 일로 타이머가 시작된다"),

            // 할 일 목록
            (.stability, "단계를 다 끝낸 할 일이 목록에서 내려간다"),
            (.custom, "바로 하면 되는 일이 목록 맨 위에 모인다"),
            (.custom, "줄을 오른쪽으로 밀면 번개 표시가 붙는다"),
            (.custom, "왼쪽 원이 단계 수만큼 잘린 도넛으로 찬다"),
            (.custom, "꾹 누르면 데드라인·분류·삭제 메뉴가 열린다"),
            (.stability, "지울 때 딸린 단계 개수를 세어 알려 준다"),
            (.custom, "할 일이 없으면 적어 보라는 안내가 보인다"),
            (.custom, "완료한 것 목록에서 되돌리기가 된다"),

            // 단계 쪼개기 · 두 질문
            (.custom, "빈 줄에 적으면 단계가 순서대로 쌓인다"),
            (.custom, "앱이 조각인지 덩어리인지 먼저 답해 둔다"),
            (.custom, "단계를 밀면 두 질문의 답을 뒤집는다"),
            (.custom, "순서대로와 아무거나를 묶음마다 고른다"),
            (.custom, "길게 눌러 끌면 단계 차례가 바뀐다"),
            (.custom, "소요시간을 넣으면 위로 합산돼 보인다"),
            (.custom, "쪼개기 도우미가 네 마디 설명을 보여 준다"),

            // 타이머
            (.platform, "앱을 닫아도 타이머 종료 알림이 온다"),
            (.custom, "탭 막대 위에 지금 세는 줄이 뜬다"),
            (.custom, "계획을 넘기면 초과분이 빨갛게 센다"),
            (.custom, "5분 더하기와 처음부터로 시간을 다시 잡는다"),
            (.platform, "잠금화면과 다이나믹 아일랜드에 남은 시간이 뜬다"),
            (.custom, "줄을 꾹 누르면 끝내거나 숨길 수 있다"),

            // 결산 · 통계
            (.custom, "이번 주 결산이 남은 몫과 회수를 나눠 센다"),
            (.custom, "회수한 시간을 분 단위 버튼으로 적는다"),
            (.custom, "숨 돌린 것은 진행률에 섞이지 않는다"),
            (.custom, "화살표로 지난 주와 다음 주를 오간다"),
            (.custom, "사용 통계가 꾸준함과 완주율을 보여 준다"),
            (.stability, "적어 둔 것이 없으면 빈 안내가 보인다"),

            // 위젯 · 확장
            (.platform, "번개 위젯이 지금 집을 단계를 보여 준다"),
            (.platform, "제어센터 버튼이 새 할 일 줄을 연다"),
            (.platform, "공유 시트로 보낸 글이 할 일에 추가된다"),
            (.platform, "할 일 위젯이 남은 개수와 제목을 보인다"),
            (.platform, "무지개 위젯이 오늘 부하를 퍼센트로 보인다"),
            (.platform, "집을 조각이 없으면 위젯이 없다고 말한다"),
            (.platform, "잠금화면 원형 위젯에도 같은 값이 뜬다"),

            // 결제
            (.monetization, "구매 전에는 위젯에 잠금 안내가 보인다"),
            (.monetization, "무지개와 할 일 쪼개기는 잠기지 않는다"),
            (.monetization, "캘린더 가져오기는 구매해야 열린다"),
            (.monetization, "잠긴 위젯을 누르면 안내 화면이 열린다"),
            (.monetization, "최근 2주 결산은 구매 없이 열린다"),

            // 설정 · 동기화 · 경계
            (.platform, "캘린더 권한을 거부하면 권한 안내가 뜬다"),
            (.custom, "가져올 캘린더와 일정을 골라 담는다"),
            (.custom, "분류를 만들어 이름·색·기호를 정한다"),
            (.custom, "공유 탭 표시를 켜면 탭이 하나 늘어난다"),
            (.stability, "iCloud 미로그인이면 공유가 막히고 안내가 뜬다"),
            (.custom, "동기화 진단에서 올라간 개수를 세어 본다"),
            (.stability, "앱을 껐다 켜도 타이머가 이어서 센다"),
        ],
        // 우리집 가정법원
        "com.family.familycourt": [
            // 첫 실행과 가족 등록
            (.stability, "처음 열면 가족 목록이 비어 보인다"),
            (.stability, "이름을 비우면 등록 버튼이 안 눌린다"),
            (.custom, "고른 이모지에 파란 테두리가 생긴다"),
            (.stability, "등록한 가족이 재실행 후에도 남는다"),
            (.custom, "가족을 왼쪽으로 밀어 지울 수 있다"),
            (.stability, "가족이 셋보다 적으면 접수 화면에 경고가 뜬다"),
            (.appearance, "홈 타일 여섯 개가 한 화면에 들어온다"),

            // 법전
            (.stability, "기본 법 다섯 조가 법전에 들어 있다"),
            (.custom, "새 법을 올리면 마지막 조로 붙는다"),
            (.stability, "법 이름이 비면 저장 버튼이 꺼진다"),
            (.custom, "법을 밀어서 고치기·없애기가 된다"),
            (.stability, "법을 지우면 조 번호가 다시 매겨진다"),
            (.stability, "법전을 다 비우면 빈 안내가 보인다"),

            // 소송 접수
            (.stability, "민사 소송 접수 후 사건 화면이 열린다"),
            (.stability, "판사는 원고·피고 외 사람만 고를 수 있다"),
            (.stability, "제목 없이 접수하면 안내창이 뜬다"),
            (.stability, "원고와 피고를 같은 사람으로 못 고른다"),
            (.stability, "형사는 어긴 법을 골라야 접수된다"),
            (.custom, "고른 법 옆에 체크 표시가 붙는다"),
            (.custom, "원고를 바꾸면 판사 선택이 풀린다"),
            (.stability, "사건번호가 연도와 순번으로 붙는다"),

            // 사건 화면
            (.appearance, "접수·재판·판결·약속 네 단계가 보인다"),
            (.custom, "다음 할 일 문구가 상태에 맞게 바뀐다"),
            (.stability, "사건 내용이 비면 적은 내용이 없다고 뜬다"),
            (.custom, "상태 띠 색이 대기·진행·완료로 달라진다"),
            (.custom, "역할 줄에 변호인·서기가 정한 만큼만 뜬다"),
            (.stability, "사건을 지우면 목록에서 사라진다"),
            (.platform, "아이패드 가로 화면에서 사건 화면이 안 깨진다"),

            // 재판 진행
            (.custom, "변호인과 서기는 없음으로 둘 수 있다"),
            (.stability, "재판 시작을 누르면 의사봉 소리가 난다"),
            (.custom, "재판 타이머가 끝나면 알림음이 울린다"),
            (.custom, "+30초를 누르면 남은 시간이 늘어난다"),
            (.custom, "대본에 고른 가족 이름이 들어가 있다"),
            (.stability, "첫 단계에서는 이전 버튼이 안 보인다"),
            (.custom, "진행 점이 지나온 단계만큼 초록으로 찬다"),
            (.stability, "재판을 멈추면 사건이 대기로 돌아간다"),
            (.accessibility, "무음 모드에서도 의사봉 진동이 온다"),

            // 판결과 약속
            (.custom, "판결 선고 때 의사봉 소리가 난다"),
            (.stability, "판결문이 비면 써 달라는 안내가 뜬다"),
            (.custom, "추천 벌칙을 고르면 칸에 채워진다"),
            (.custom, "형사·민사에 따라 판결 선택지가 다르다"),
            (.stability, "약속 체크가 재실행 후에도 남는다"),
            (.appearance, "다 지킨 약속에 취소선이 그어진다"),
            (.custom, "판결이 끝난 사건은 판례에 쌓인다"),

            // 소환·판례·법원 공부
            (.stability, "소환 타일 배지에 대기 사건 수가 뜬다"),
            (.stability, "기다리는 사건이 없으면 평화 문구가 보인다"),
            (.stability, "끝난 사건이 없으면 판례가 비어 보인다"),
            (.custom, "목록에서 사건을 누르면 그 사건이 열린다"),
            (.custom, "공부 화면에 민사·형사 순서가 나뉜다"),
            (.custom, "재판 약속 다섯 가지가 번호로 보인다"),

            // 설정과 데이터
            (.appearance, "말투 단계를 바꾸면 홈 문구가 바뀐다"),
            (.custom, "말투 미리보기 줄이 바로 바뀐다"),
            (.custom, "법원 이름을 바꾸면 대본에 반영된다"),
            (.stability, "기록 저장으로 파일이 공유 시트에 뜬다"),
            (.stability, "저장한 기록 파일을 불러오면 복원된다"),
            (.stability, "엉뚱한 파일을 고르면 못 읽는다고 뜬다"),
            (.stability, "글자를 쓰다 빈 곳을 누르면 키보드가 내려간다"),
        ],
        // 이어생각
        "com.leeo.thinkflow": [
            // 첫 실행과 빈 상태
            (.stability, "처음 열면 첫 생각 꺼내기 버튼만 보인다"),
            (.stability, "샘플 생각이 미리 들어 있지 않다"),
            (.custom, "빈 상태에서 내보내기 메뉴가 꺼져 있다"),
            (.stability, "스레드를 다 지우면 빈 화면으로 돌아간다"),
            (.appearance, "오른쪽 아래 주황 더하기 버튼이 떠 있다"),
            (.accessibility, "더하기 버튼을 생각 꺼내기로 읽어 준다"),

            // 생각 꺼내기
            (.stability, "시트를 열자마자 입력칸에 커서가 있다"),
            (.custom, "줄바꿈 수만큼 생각 개수가 세어진다"),
            (.custom, "한 줄만 쓰면 바로 스레드가 만들어진다"),
            (.custom, "여러 줄이면 하나만 고르는 화면이 뜬다"),
            (.stability, "빈 칸이면 다 꺼냈어요 버튼이 꺼진다"),
            (.stability, "브레인덤프 항목이 스레드로 승격된다"),
            (.stability, "새 생각 스레드가 재실행 후에도 남는다"),

            // 홈 화면 목록
            (.custom, "고르지 않은 생각이 대기 목록에 남는다"),
            (.custom, "대기 항목을 꾹 누르면 삭제가 뜬다"),
            (.appearance, "가장 최근 스레드가 큰 카드로 뜬다"),
            (.custom, "사흘 넘긴 스레드가 되살릴 생각으로 묶인다"),
            (.custom, "카드에 마지막 방문이 상대 시간으로 뜬다"),
            (.stability, "카드를 누르면 스레드 상세가 열린다"),
            (.accessibility, "섹션 제목을 머리글로 읽어 준다"),

            // 이어쓰기
            (.stability, "이어쓰기를 열면 끊어둔 문장이 맨 위에 있다"),
            (.stability, "입력칸에 바로 커서가 잡힌다"),
            (.custom, "생각 추가를 누르면 번호가 붙어 쌓인다"),
            (.custom, "쌓은 생각을 다시 빼낼 수 있다"),
            (.custom, "글자 수가 입력하는 대로 세어진다"),
            (.custom, "조사 결과·자료를 출처와 함께 붙인다"),
            (.stability, "입력이 있으면 닫기 버튼이 사라진다"),
            (.stability, "끊어둘 문장 없이는 이어쓰기가 안 닫힌다"),
            (.stability, "쓰던 중 앱을 내렸다 켜도 입력이 남는다"),

            // 스레드 상세
            (.appearance, "끊어둔 문장이 주황 줄과 함께 보인다"),
            (.custom, "끊어둔 문장이 없으면 안내 문구가 뜬다"),
            (.custom, "메모·사고시간·깊이·시작일이 한 줄에 뜬다"),
            (.custom, "브릿지 수정으로 끊어둔 문장을 고친다"),
            (.custom, "전체 복사를 누르면 복사됨으로 바뀐다"),
            (.custom, "최신순과 오래된순 정렬이 서로 바뀐다"),
            (.stability, "기록이 없으면 첫 기록 안내가 보인다"),
            (.stability, "스레드를 지우면 홈에서 사라진다"),

            // 엔트리 정리
            (.stability, "엔트리 레이어 승급이 저장된다"),
            (.appearance, "레이어마다 배지 색이 다르게 보인다"),
            (.custom, "엔트리를 고치면 내용이 바로 바뀐다"),
            (.custom, "엔트리 하나만 지워도 나머지는 남는다"),
            (.custom, "사고 시간이 머문 만큼 늘어난다"),
            (.accessibility, "레이어를 바꾸면 음성 안내가 들린다"),

            // 위젯과 라이브 액티비티
            (.platform, "위젯이 끊어둔 문장을 보여 준다"),
            (.platform, "위젯을 탭하면 이어쓰기 화면이 열린다"),
            (.platform, "이어쓰기 중 Live Activity가 뜬다"),
            (.platform, "기록이 없으면 위젯이 빈 상태로 보인다"),
            (.platform, "다이내믹 아일랜드에 사고 타이머가 흐른다"),
            (.platform, "스레드를 지우면 위젯 내용이 바뀐다"),
            (.platform, "위젯 중간 크기에도 글이 잘리지 않는다"),

            // 내보내기와 데이터
            (.custom, "마크다운 내보내기로 공유 시트가 열린다"),
            (.custom, "JSON 파일 이름에 날짜가 붙는다"),
            (.stability, "내보낸 파일에 대기 중인 생각도 들어 있다"),
            (.stability, "기기를 회전해도 카드 배치가 안 깨진다"),
            (.stability, "비행기 모드에서도 모든 화면이 열린다"),
            (.custom, "문의 메뉴가 메일과 DM으로 이어진다"),
        ],
        // 인생맛집
        "com.Ysoup.restaurantmap": [
            // 첫 실행과 권한
            (.stability, "위치 권한을 거절해도 지도가 열린다"),
            (.stability, "권한 허용 후 현재 위치 점이 보인다"),
            (.stability, "처음 목록 탭에 빈 안내가 보인다"),
            (.stability, "첫 실행에 네 개 탭이 모두 눌린다"),
            (.stability, "사진 권한을 거절하면 카메라만 쓸 수 있다"),
            (.platform, "알림 권한을 거절해도 앱이 잘 돈다"),

            // 지도
            (.stability, "현재 위치로 추가한 식당 핀이 지도에 보인다"),
            (.custom, "저장된 식당만 보기로 주변 장소가 감춰진다"),
            (.custom, "지도를 옮기면 주변 장소가 새로 불린다"),
            (.custom, "핀을 누르면 간단 정보가 먼저 뜬다"),
            (.custom, "플로팅 버튼으로 위치를 직접 골라 추가한다"),
            (.stability, "위치 고르기 중 임시 핀이 지도에 남는다"),
            (.stability, "시트를 닫으면 임시 핀이 사라진다"),
            (.custom, "저장한 식당 핀과 검색 핀 색이 다르다"),
            (.stability, "비행기 모드에서 지도 타일이 없어도 안 멈춘다"),

            // 장소 검색
            (.custom, "지도에서 카카오 장소 검색 결과가 뜬다"),
            (.custom, "Apple 지도 검색으로 바꿔서도 찾아진다"),
            (.stability, "없는 이름을 넣으면 결과 없음이 보인다"),
            (.custom, "검색 결과에 현재 위치에서의 거리가 뜬다"),
            (.custom, "장소 정보에서 전화와 웹사이트가 열린다"),
            (.custom, "목록에 추가하면 정보가 미리 채워진다"),
            (.stability, "네트워크가 없으면 검색 실패 안내가 뜬다"),

            // 식당 추가
            (.stability, "이름 없이 저장하면 저장이 막힌다"),
            (.custom, "카테고리 아이콘을 골라 바꿀 수 있다"),
            (.custom, "아직 방문 안 한 식당으로 저장된다"),
            (.custom, "저장한 위치의 위도·경도가 보인다"),
            (.stability, "저장 직후 목록과 지도에 함께 뜬다"),
            (.custom, "메모를 적어 두면 상세에서 다시 보인다"),
            (.stability, "취소하면 아무것도 저장되지 않는다"),

            // 목록과 탑6
            (.custom, "가본 곳과 가볼 곳이 따로 묶여 보인다"),
            (.custom, "나의 미슐랭에 탑6 식당이 순위로 뜬다"),
            (.stability, "탑6는 여섯 개까지만 지정된다"),
            (.stability, "가볼 곳은 탑6로 지정할 수 없다"),
            (.custom, "목록 행에 방문 횟수가 표시된다"),
            (.custom, "스와이프로 식당을 지울 수 있다"),
            (.stability, "탑6가 없으면 안내 문구가 보인다"),
            (.stability, "지운 식당은 지도 핀에서도 빠진다"),

            // 상세와 메뉴
            (.stability, "상세 화면에서 Apple 지도로 이동한다"),
            (.custom, "카카오맵과 네이버맵으로도 열린다"),
            (.custom, "전화번호를 누르면 전화가 걸린다"),
            (.custom, "총평에 전체 방문 평균이 뜬다"),
            (.custom, "메뉴를 등록하면 개수가 상세에 뜬다"),
            (.stability, "메뉴가 없으면 첫 메뉴 안내가 보인다"),
            (.custom, "메뉴별 주문 횟수가 쌓인다"),
            (.stability, "식당을 지우면 방문 기록도 함께 지워진다"),

            // 방문 기록과 사진
            (.stability, "방문 기록과 사진이 재실행 후에도 남는다"),
            (.custom, "방문 기록에 메뉴를 고르거나 직접 적는다"),
            (.custom, "사진 여러 장을 한 번에 담을 수 있다"),
            (.custom, "카메라로 찍은 사진이 바로 붙는다"),
            (.custom, "사진을 누르면 크게 볼 수 있다"),
            (.custom, "별점을 매기면 총평에 반영된다"),
            (.custom, "맛 강도와 만족도를 항목마다 고른다"),
            (.stability, "맛 평가를 건너뛰어도 저장된다"),
            (.stability, "방문 기록을 지우면 사진도 같이 지워진다"),

            // 추천과 취향
            (.custom, "맛 평가 5개를 채우면 추천이 열린다"),
            (.appearance, "남은 평가 수가 원형 진행도로 보인다"),
            (.stability, "저장 식당이 없으면 지도로 보내는 버튼이 뜬다"),
            (.custom, "새로고침을 누르면 추천이 다시 뽑힌다"),
            (.custom, "추천 카드에 어울리는 이유가 적혀 있다"),
            (.custom, "추천을 내 목록에 바로 추가할 수 있다"),
            (.custom, "취향 탭에 여섯 속성 그래프가 그려진다"),

            // 설정과 백업
            (.custom, "설정에 저장 가게 수와 방문 수가 뜬다"),
            (.custom, "추가한 음식 종류가 선택지에 보인다"),
            (.stability, "iCloud 백업이 오류 없이 끝난다"),
            (.stability, "iCloud 미로그인이면 백업 안내가 뜬다"),
            (.stability, "복구하면 백업 시점 데이터로 바뀐다"),
            (.custom, "마지막 백업 시각이 설정에 남는다"),
            (.stability, "모든 데이터 삭제 전에 한 번 더 묻는다"),
            (.platform, "식당 근처를 다녀오면 기록 알림이 온다"),
        ],
        // 장표스냅
        "com.leeo.slidesnap": [
            // 첫 실행과 카메라 권한
            (.stability, "카메라 권한을 거절하면 설정 열기가 뜬다"),
            (.stability, "처음 열면 발표가 없다는 안내가 보인다"),
            (.stability, "권한 허용 후 프리뷰가 바로 뜬다"),
            (.stability, "+ 버튼으로 빈 발표를 만든다"),
            (.custom, "새 발표 기본 제목에 날짜가 들어간다"),
            (.stability, "촬영 없이 닫으면 빈 발표가 지워진다"),

            // 촬영
            (.stability, "찍은 장표가 반듯하게 펴져 저장된다"),
            (.appearance, "프리뷰에 장표 모서리가 실시간으로 잡힌다"),
            (.custom, "잘 잡히면 띠가 초록으로 바뀌고 진동이 온다"),
            (.custom, "셔터 테두리가 잡힐 때 초록으로 변한다"),
            (.custom, "두 손가락으로 벌리면 배율 숫자가 뜬다"),
            (.custom, "한 손가락 세로 스와이프로도 줌이 된다"),
            (.custom, "왼쪽 아래 썸네일에 촬영 장수가 뜬다"),
            (.stability, "썸네일을 누르면 그리드로 넘어간다"),
            (.stability, "5분 안에 다시 찍으면 같은 발표에 붙는다"),

            // 자동 촬영
            (.custom, "자동 촬영이 같은 장표를 다시 안 찍는다"),
            (.custom, "자동 버튼을 끄면 번개 표시가 바뀐다"),
            (.stability, "자동 설정이 앱을 껐다 켜도 남는다"),
            (.custom, "흔들린 컷은 건너뛰었다는 안내가 뜬다"),
            (.custom, "장표를 바꾸면 자동으로 한 장 담긴다"),
            (.stability, "담기는 동안 상단에 진행 표시가 돈다"),
            (.custom, "자동이 꺼져 있으면 지금 찍으라고 안내한다"),

            // 발표 목록
            (.stability, "직접 만든 빈 발표가 사라지지 않는다"),
            (.custom, "목록 보기와 그리드 보기가 전환된다"),
            (.custom, "행에 장표 수와 날짜가 같이 뜬다"),
            (.custom, "꾹 눌러 이름 변경과 삭제가 된다"),
            (.custom, "선택 모드로 여러 발표를 합친다"),
            (.stability, "합치기 전에 되돌릴 수 없다고 알린다"),
            (.stability, "합친 발표에 장표가 순서대로 이어 붙는다"),
            (.stability, "목록으로 돌아오면 장표 0장 발표가 정리된다"),

            // 장표 그리드
            (.stability, "장표가 없으면 촬영·가져오기 버튼이 보인다"),
            (.custom, "드래그로 장표 순서를 바꾼다"),
            (.custom, "장표마다 번호가 매겨져 보인다"),
            (.custom, "선택 모드에서 여러 장을 한 번에 지운다"),
            (.custom, "꾹 눌러 열기·공유·삭제가 뜬다"),
            (.stability, "인식 못 한 장표에 표시가 붙는다"),
            (.stability, "삭제 전에 한 번 더 확인한다"),
            (.custom, "발표 이름을 그리드에서도 바꾼다"),

            // 장표 상세와 보정
            (.stability, "모서리 조정 후 보정본이 바뀐다"),
            (.custom, "보정본과 원본을 전환해서 본다"),
            (.custom, "좌우로 밀어 다음 장표로 넘어간다"),
            (.custom, "핀치로 확대하고 더블탭으로 되돌린다"),
            (.custom, "모서리를 끌면 돋보기가 따라온다"),
            (.custom, "자동 감지와 전체 영역이 다시 잡아 준다"),
            (.custom, "가독성 보정을 켜면 글자가 또렷해진다"),
            (.stability, "가독성 보정이 썸네일에도 반영된다"),

            // 검색과 사진 가져오기
            (.custom, "텍스트 검색으로 장표를 찾는다"),
            (.custom, "검색 결과에 발표 제목과 장표 번호가 뜬다"),
            (.stability, "없는 낱말을 넣으면 결과 없음이 보인다"),
            (.custom, "사진 앱에서 여러 장을 한 번에 가져온다"),
            (.stability, "가져오는 동안 진행 수가 보인다"),
            (.stability, "가져온 사진도 같은 방식으로 펴진다"),
            (.stability, "사진 권한 없이도 가져오기가 된다"),

            // 내보내기
            (.custom, "PDF가 4장 레이아웃으로 내보내진다"),
            (.custom, "한 장과 두 장 레이아웃도 고를 수 있다"),
            (.custom, "PDF 머리글에 발표 제목과 쪽수가 찍힌다"),
            (.custom, "고른 장표만 이미지로 공유한다"),
            (.stability, "장표가 없으면 내보내기가 눌리지 않는다"),
            (.stability, "만들어진 PDF 이름이 발표 제목과 같다"),

            // 위젯과 설정
            (.platform, "잠금화면 위젯을 누르면 카메라가 열린다"),
            (.platform, "홈 화면 위젯에서도 카메라가 바로 열린다"),
            (.platform, "위젯 작은 크기에 장표 촬영 문구가 보인다"),
            (.custom, "내 정보에 이름과 이메일을 저장한다"),
            (.stability, "잘못된 이메일 형식이면 안내가 뜬다"),
            (.custom, "피드백 보내기로 의견이 전송된다"),
            (.custom, "의견 묻는 알림에 다시 묻지 않기가 있다"),
        ],
        // 징검돌
        "com.leeo.ReboundJournal": [
            // 첫 실행과 첫 만남
            (.stability, "처음 열면 조약돌이 먼저 말을 건다"),
            (.custom, "목표가 없으면 적어볼게요 하나만 뜬다"),
            (.stability, "목록도 그래프도 먼저 나오지 않는다"),
            (.custom, "조약돌 글자가 하나씩 찍히며 나온다"),
            (.custom, "화면을 누르면 타이핑을 건너뛴다"),
            (.appearance, "조약돌이 숨 쉬듯 천천히 움직인다"),

            // 조약돌 홈
            (.custom, "조약돌 표정이 요즘 기록에 따라 바뀐다"),
            (.custom, "매듭짓지 못한 기록을 먼저 물어본다"),
            (.custom, "답을 고르면 말풍선이 아래에 쌓인다"),
            (.stability, "말풍선이 늘어나도 화면이 따라 내려간다"),
            (.custom, "설정을 다녀와도 인사를 다시 찍지 않는다"),
            (.stability, "대화를 끝내면 홈 인사가 새로 만들어진다"),
            (.custom, "메뉴에서 목표 추가·남긴 것들·설정이 열린다"),
            (.accessibility, "지나온 길 버튼을 음성으로 읽어 준다"),

            // 대화
            (.stability, "조약돌 대화 기록이 재실행 후에도 남는다"),
            (.custom, "조약돌이 말하는 동안은 답할 자리가 없다"),
            (.custom, "무슨 일이 있었는지와 마음을 따로 묻는다"),
            (.custom, "감정 눈금을 고르면 낱말 칩이 나타난다"),
            (.custom, "고른 감정을 다시 눌러 되돌릴 수 있다"),
            (.stability, "감정이 가장 낮으면 더 묻지 않고 끝난다"),
            (.stability, "중간에 닫아도 여태 말한 건 남는다"),
            (.custom, "글자마다 조약돌 소리와 진동이 난다"),
            (.appearance, "화면에 실패나 리바운드 같은 낱말이 없다"),

            // 음성 입력
            (.custom, "음성으로 말한 내용이 대화에 들어간다"),
            (.stability, "마이크 권한을 거절하면 적어도 된다고 알린다"),
            (.custom, "듣는 동안 말풍선에 글이 실시간으로 쌓인다"),
            (.custom, "듣는 중에는 조약돌이 듣는 표정이 된다"),
            (.custom, "흐릿하면 이런 뜻인지 정리해서 되묻는다"),
            (.custom, "조금 다르다를 고르면 문장이 편집칸에 온다"),
            (.stability, "다시 말해 달라는 말이 나오지 않는다"),

            // 목표와 기록
            (.custom, "목표를 적으면 작은 일 하나를 되묻는다"),
            (.stability, "목표를 더하면 홈 인사가 다시 만들어진다"),
            (.custom, "남긴 것들에서 날짜순으로 훑어본다"),
            (.custom, "기록을 밀어 고치기와 지우기가 뜬다"),
            (.stability, "지우기 전에 한 번 더 확인한다"),
            (.stability, "고친 기록이 지나온 길에도 반영된다"),
            (.custom, "오늘과 어제는 날짜 대신 말로 표시된다"),
            (.stability, "기록이 없으면 아직 기록 없음이 보인다"),

            // 지나온 길
            (.stability, "지난 주 징검돌에서 기록이 보인다"),
            (.appearance, "밟지 않은 돌도 윤곽으로 남아 있다"),
            (.appearance, "못 지킨 날에 붉은색을 쓰지 않는다"),
            (.custom, "며칠이나 몇 퍼센트 같은 숫자가 없다"),
            (.custom, "목표를 고르면 조약돌이 그 얘기를 시작한다"),
            (.custom, "오래된 이야기는 접어 뒀다고 알려 준다"),
            (.accessibility, "돌 하나하나를 음성이 날짜로 읽는다"),

            // 잠금과 알림
            (.stability, "비밀번호 설정 후 재실행하면 잠긴다"),
            (.appearance, "비밀번호 숫자 버튼이 보인다"),
            (.stability, "틀린 비밀번호로는 화면이 안 열린다"),
            (.stability, "비밀번호를 지우면 다시 안 잠긴다"),
            (.stability, "백그라운드에서 돌아오면 잠금이 다시 뜬다"),
            (.platform, "알림을 켜면 시간 고르는 줄이 나타난다"),
            (.platform, "정한 시각에 하루 알림이 온다"),

            // 설정과 말투
            (.custom, "말투를 바꾸면 처음부터 다시 인사한다"),
            (.custom, "고른 말투 예시 문장이 바로 바뀐다"),
            (.custom, "말할 때 소리를 끄면 글자만 찍힌다"),
            (.custom, "말할 때 진동만 따로 켤 수 있다"),
            (.stability, "소리와 진동 설정이 재실행 후에도 남는다"),
            (.custom, "말투 안내 팁이 메뉴 위에 떠 준다"),
            (.stability, "예전 버전에서 쓴 기록이 그대로 보인다"),

            // 위젯과 언어
            (.platform, "위젯을 누르면 기록 화면이 열린다"),
            (.platform, "위젯 글이 앱 첫마디와 같다"),
            (.platform, "기록을 남기면 위젯 문구가 바뀐다"),
            (.platform, "잠금화면 위젯에도 한 마디가 뜬다"),
            (.custom, "영어 기기에서 대화가 영어로 나온다"),
            (.custom, "영어에서는 말투 고르는 칸이 감춰진다"),
        ],
        // 초견
        "com.devkoan.StaffSinger": [
            // 첫 실행과 오선
            (.stability, "처음 열면 빈 오선지만 보인다"),
            (.appearance, "음표를 넣으면 상단 버튼이 나타난다"),
            (.appearance, "가로 모드에서 두 마디가 잘리지 않는다"),
            (.appearance, "음자리표가 오선 세로 가운데에 놓인다"),
            (.appearance, "다이나믹 아일랜드에 오선이 가려지지 않는다"),
            (.custom, "음표 도구 버튼으로 하단 패널이 접힌다"),

            // 음표 입력과 편집
            (.stability, "오선을 탭하면 음표가 들어가고 소리가 난다"),
            (.custom, "누른 채 끌면 고스트 음표와 음높이가 보인다"),
            (.custom, "음표를 끌어 음높이와 박 위치를 옮긴다"),
            (.custom, "음표를 꾹 누르면 그 자리에서 지워진다"),
            (.custom, "화음 모드로 쌓은 음이 동시에 울린다"),
            (.custom, "♯·♭·8va 버튼이 고른 음표에 적용된다"),
            (.custom, "셋잇단 토글을 켜면 '3' 표시가 붙는다"),
            (.stability, "백스페이스로 방금 넣은 음표만 지워진다"),

            // 재생과 박자
            (.custom, "재생하면 카운트인 뒤 현재 음표가 강조된다"),
            (.stability, "재생 중 정지를 누르면 소리가 바로 멎는다"),
            (.custom, "메트로놈을 끄면 사선 아이콘으로 바뀐다"),
            (.custom, "템포를 바꾸면 재생 속도가 따라간다"),
            (.custom, "박자표를 6/8로 바꾸면 마디줄이 옮겨진다"),
            (.stability, "빈 박은 재생할 때 쉼표로 채워진다"),
            (.stability, "음표가 하나도 없으면 조용히 끝난다"),

            // 피아노 건반과 쉼표
            (.custom, "건반을 열면 오선이 위로 올라간다"),
            (.custom, "흰 건반을 누르면 조표가 반영된 음이 들어간다"),
            (.custom, "옥타브 화살표로 건반 음역이 바뀐다"),
            (.custom, "쉼표 다섯 종을 한 번 탭해 바로 넣는다"),
            (.custom, "건반을 열면 음표 도구 패널이 닫힌다"),
            (.custom, "점 버튼을 켜면 넣는 음이 점음표가 된다"),

            // 계이름 받아쓰기
            (.custom, "계이름을 말하면 음표로 들어간다"),
            (.platform, "마이크 권한을 거부하면 안내 문구가 뜬다"),
            (.custom, "카운트인 뒤 두 마디를 듣고 저절로 멈춘다"),
            (.custom, "들은 말과 받아쓴 음표가 나란히 보인다"),
            (.custom, "리듬 스냅을 바꾸면 박 위치가 달라진다"),
            (.custom, "옥타브 자동 맞춤을 끄면 시작 옥타브를 따른다"),
            (.stability, "비행기 모드에서도 받아쓰기가 된다"),

            // 텍스트 악보 붙여넣기
            (.custom, "텍스트 악보를 붙여넣으면 오선에 그려진다"),
            (.custom, "규칙 전문을 복사해 다른 앱에 붙인다"),
            (.custom, "현재 악보를 텍스트로 복사한다"),
            (.stability, "규칙에 안 맞는 글은 못 찾았다고 알린다"),
            (.stability, "두 마디를 넘기면 안 보일 수 있다고 알린다"),

            // 악보 사진과 자동 채보
            (.custom, "카메라로 두 마디를 창에 맞춰 찍는다"),
            (.custom, "보관함 사진을 확대·이동해 잘라 쓴다"),
            (.custom, "참고 사진을 끌어 옮기고 투명도를 바꾼다"),
            (.custom, "채보를 누르면 인식한 음표 수가 뜬다"),
            (.stability, "인식에 실패해도 원래 악보는 그대로다"),
            (.custom, "눈 감김 버튼으로 참고 사진을 잠시 숨긴다"),

            // 설정 팔레트
            (.custom, "설정을 열어도 악보가 옆에 계속 보인다"),
            (.custom, "좌우 전환 버튼으로 팔레트가 반대쪽에 붙는다"),
            (.custom, "음자리표를 바꾸면 클리프 글리프가 달라진다"),
            (.custom, "조표를 올리면 오선 앞에 ♯이 늘어난다"),
            (.stability, "전체 지우기를 누르면 오선이 비워진다"),
            (.accessibility, "팔레트 닫기 버튼에 음성 레이블이 붙는다"),

            // 데이터와 복원
            (.stability, "음길이·점음표 선택이 재실행 후에도 남는다"),
            (.stability, "팔레트 좌우 위치가 재실행 후에도 유지된다"),
            (.stability, "건반 시작 옥타브가 다시 열어도 그대로다"),
            (.stability, "백그라운드에 뒀다 돌아와도 악보가 남아 있다"),
        ],
        // 쿨타임
        "com.Ysoup.CoolTime": [
            // 첫 실행 온보딩
            (.stability, "처음 열면 온보딩 네 장이 넘어간다"),
            (.custom, "건너뛰기를 누르면 바로 홈으로 간다"),
            (.stability, "온보딩을 마치면 다시 열어도 안 뜬다"),
            (.appearance, "페이지 점이 현재 장에 맞춰 커진다"),
            (.stability, "아이템이 없으면 빈 화면 안내가 보인다"),

            // 홈 화면과 요약
            (.custom, "요약 카드에 준수율과 절약 금액이 뜬다"),
            (.custom, "사용 가능과 쿨타임 중이 따로 묶여 보인다"),
            (.custom, "검색창에 이름을 치면 목록이 걸러진다"),
            (.stability, "검색 결과가 없으면 안내 문구가 나온다"),
            (.custom, "카드를 꾹 눌러 삭제·쿨타임 리셋이 된다"),
            (.appearance, "쿨타임 원이 남은 비율만큼 채워진다"),
            (.stability, "화면을 보는 동안 남은 시간이 줄어든다"),

            // 아이템 추가
            (.stability, "이름을 비우면 추가 버튼이 눌리지 않는다"),
            (.custom, "이모지 팔레트에서 고른 아이콘이 카드에 붙는다"),
            (.custom, "3일·1주 같은 빠른 선택이 쿨타임에 들어간다"),
            (.custom, "단위를 시간으로 바꾸면 미리보기 문구가 바뀐다"),
            (.custom, "비용을 넣으면 절약 금액 계산에 들어간다"),
            (.stability, "취소로 닫으면 아무것도 추가되지 않는다"),
            (.stability, "추가한 아이템이 사용 가능 칸에 들어간다"),

            // 템플릿
            (.custom, "템플릿으로 아이템이 추가된다"),
            (.custom, "카테고리별로 추천 템플릿이 묶여 있다"),
            (.custom, "이미 담은 템플릿에는 표시가 남는다"),
            (.custom, "템플릿 카드에 쿨타임과 예상 비용이 보인다"),

            // 사용 기록
            (.stability, "아이템을 쓰면 쿨타임 원이 줄기 시작한다"),
            (.custom, "쿨타임 중에 쓰면 경고가 뜬다"),
            (.custom, "메모를 적어 사용 기록에 남긴다"),
            (.custom, "실제 비용을 넣으면 예상값 대신 쓰인다"),
            (.custom, "쿨타임을 깨면 깬 횟수가 하나 늘어난다"),
            (.custom, "쿨타임 리셋을 누르면 바로 사용 가능해진다"),

            // 통계
            (.custom, "통계에 준수율이 반영된다"),
            (.custom, "아이템별 준수율이 막대로 늘어선다"),
            (.custom, "카테고리별 개수와 준수율이 함께 나온다"),
            (.custom, "최근 활동에 날짜별 사용 횟수가 쌓인다"),
            (.stability, "기록이 없으면 활동 없음 문구가 보인다"),
            (.custom, "상세 기록에 총 사용과 추정 지출이 나온다"),

            // 알림
            (.platform, "쿨타임이 끝나면 종료 알림이 온다"),
            (.platform, "절반을 지나면 응원 알림이 온다"),
            (.platform, "알림을 밀면 지금 사용하기 버튼이 보인다"),
            (.platform, "권한을 거부하면 홈 위에 안내 띠가 뜬다"),
            (.platform, "안내 띠의 설정 버튼이 iOS 설정을 연다"),
            (.platform, "앱을 열면 아이콘 배지가 지워진다"),
            (.stability, "아이템을 지우면 예약된 알림도 사라진다"),

            // 위젯
            (.platform, "홈 위젯이 쿨타임 현황을 보여 준다"),
            (.platform, "중간 위젯에 준수율과 대기 목록이 함께 나온다"),
            (.platform, "큰 위젯에 이번 달 절약 금액이 보인다"),
            (.platform, "잠금화면 원형 위젯에 사용 가능 수가 뜬다"),
            (.platform, "잠금화면 한 줄 위젯에 남은 시간이 나온다"),
            (.platform, "아이템을 쓰면 위젯 숫자도 곧 따라 바뀐다"),
            (.stability, "아이템이 없으면 위젯이 추가 안내를 띄운다"),

            // 설정과 데이터
            (.stability, "남은 쿨타임이 재실행 후에도 맞다"),
            (.stability, "오래 백그라운드에 뒀다 와도 남은 시간이 맞다"),
            (.custom, "설정에서 개발자에게 메일을 보낼 수 있다"),
            (.custom, "설정에서 인스타그램 DM 링크가 열린다"),
            (.stability, "비행기 모드에서도 쿨타임이 정상 동작한다"),
        ],
        // 클립키보드: 전체 리그레션. ClipKeyboard 저장소의 FEATURE_MATRIX_AND_QA_4.4.4와
        // 4.4.5~5.1.5 릴리즈 노트의 실기기 확인 항목에서 뽑았다. 예전 7개 제목은 그대로 두었다.
        "com.Ysoup.TokenMemo": [
            // 첫 실행·튜토리얼
            (.stability, "첫 실행에 키보드 무대와 튜토리얼이 뜬다"),
            (.custom, "튜토리얼에서 단축어·템플릿·스택을 눌러 써 본다"),
            (.custom, "튜토리얼 끝에 연습 단축어를 치울지 한 번 묻는다"),
            (.stability, "튜토리얼을 마치고 재실행하면 다시 틀지 않는다"),
            (.custom, "설정에서 튜토리얼을 처음부터 다시 볼 수 있다"),
            (.platform, "설정 가이드대로 키보드와 전체 접근을 켤 수 있다"),
            (.platform, "키보드를 안 켰으면 키보드 화면 위에 켜기 띠가 남는다"),
            (.platform, "iOS 설정에서 키보드를 빼면 켜기 띠가 다시 뜬다"),
            (.custom, "목록 화면과 키보드 화면을 툴바로 오간다"),

            // 단축어 목록·편집
            (.stability, "새 단축어를 열자마자 이름 칸에 커서가 있다"),
            (.stability, "내용의 이메일 첫 글자가 대문자로 바뀌지 않는다"),
            (.custom, "저장하면 고른 카테고리 화면으로 옮겨 간다"),
            (.stability, "카드를 탭하면 복사되고 토스트가 탭바에 안 가린다"),
            (.custom, "카드를 꾹 눌러 수정·카테고리 이동·삭제 확인이 된다"),
            (.custom, "두 손가락 탭으로 여러 개 골라 옮기거나 지운다"),
            (.stability, "즐겨찾기해도 단축어가 원래 카테고리에 남는다"),
            (.stability, "카테고리를 좌우로 넘겨도 제목과 목록이 맞는다"),
            (.stability, "보던 카테고리가 비면 다음 카테고리로 넘어간다"),
            (.custom, "검색은 대소문자를 가리지 않고 보안 내용은 찾지 않는다"),
            (.custom, "작성 중 닫았다 다시 열면 초안이 돌아온다"),
            (.custom, "여러 줄을 붙여넣어 단축어 여러 개를 한 번에 만든다"),
            (.custom, "단축어 마트에서 빈칸만 채워 단축어를 만든다"),
            (.custom, "목록 맨 위 필수 단축어 세 칸을 채워 만든다"),
            (.custom, "앱을 열면 방금 복사한 글을 저장하자고 권한다"),
            (.stability, "카테고리를 숨길 때 안의 단축어를 한 번에 옮긴다"),

            // 템플릿·빈칸·스택
            (.custom, "템플릿 빈칸을 채워 입력된다"),
            (.custom, "빈칸에 친 값은 별을 눌러야만 저장된다"),
            (.custom, "빈칸 관리에서 끌어 바꾼 값 순서가 칩에 그대로 간다"),
            (.custom, "빈칸 이름을 바꾸면 쓰는 단축어 내용도 바뀐다"),
            (.custom, "날짜 빈칸이 설정한 날짜 형식으로 들어간다"),
            (.custom, "번호 빈칸은 다음 번호 칩이 맨 앞에 선다"),
            (.custom, "스택 칸 이름이 보이고 화살표로 값을 넘긴다"),
            (.stability, "스택 편집 중 칸을 지워도 앱이 멈추지 않는다"),

            // 사진·글자 읽기
            (.custom, "사진을 붙인 단축어를 누르면 사진이 복사된다"),
            (.custom, "글과 사진을 함께 담은 단축어가 둘 다 붙는다"),
            (.custom, "사진을 스캔해 문지른 글자만 내용으로 들어간다"),
            (.stability, "사진에서 읽은 카드번호는 보안 단축어로 잠긴다"),

            // 클립보드 기록
            (.custom, "다른 앱에서 복사한 글이 클립보드 기록에 쌓인다"),
            (.custom, "이메일·전화·주민번호가 맞는 종류로 분류된다"),
            (.custom, "클립보드 항목을 단축어로 저장할 수 있다"),
            (.custom, "설치 직후엔 붙여넣기 허용 팝업이 뜨지 않는다"),

            // 키보드 익스텐션 (실기기에서 남의 앱으로)
            (.platform, "키보드에서 단축어를 탭하면 입력된다"),
            (.platform, "앱에서 추가한 단축어가 키보드에 바로 보인다"),
            (.platform, "지구본 키로 다음 키보드로 넘어간다"),
            (.platform, "지구본을 길게 누르면 이모지 키보드를 고를 수 있다"),
            (.platform, "키보드가 마지막에 보던 카테고리로 열린다"),
            (.platform, "키를 길게 누르면 값이 크게 펼쳐지고 복사된다"),
            (.platform, "키보드 안에서 옮긴 단축어 순서가 남는다"),
            (.platform, "지우기 키를 누르고 있으면 계속 지워진다"),
            (.platform, "붙여넣기를 길게 눌러 복사한 글의 일부만 넣는다"),
            (.platform, "숫자 판이 열리고 가로에서도 아랫줄이 보인다"),
            (.platform, "키보드에서 두벌식·천지인 한글이 바르게 조합된다"),
            (.platform, "빠른 줄에 최근 쓴 것과 붙박이 단축어가 선다"),
            (.platform, "키보드에서 빈칸을 채울 때 지금 칸만 펼쳐진다"),
            (.platform, "잠긴 스택은 값을 넘길 땐 안 묻고 넣을 때 PIN을 묻는다"),
            (.platform, "키보드 높이를 시스템과 같게 하면 전환 때 안 튄다"),
            (.platform, "가로로 긴 사진 단축어가 옆 키를 가리지 않는다"),

            // 보안
            (.stability, "보안 단축어가 Face ID 후에 보인다"),
            (.stability, "Face ID를 못 쓰면 PIN으로 열 수 있다"),
            (.appearance, "잠근 단축어 카드에 자물쇠가 늘 보인다"),

            // 공유·위젯·제어센터
            (.platform, "공유 시트로 받은 글이 단축어가 된다"),
            (.platform, "즐겨찾기 위젯을 탭하면 복사된다"),
            (.platform, "제어센터 값 복사 버튼이 앱을 열지 않고 복사한다"),
            (.platform, "제어센터 빠른 메모가 앱을 열어 입력 시트를 띄운다"),

            // 데이터
            (.stability, "내보낸 .json을 가져오면 기존 데이터와 합쳐진다"),
            (.stability, "iCloud 백업을 복원하면 카테고리와 사진까지 돌아온다"),
            (.platform, "두 기기 동기화에서 수정·삭제도 반대편에 간다"),
            (.stability, "변경 기록에서 지운 단축어를 되돌린다"),
            (.stability, "모든 데이터 삭제 후 앱과 키보드가 모두 비어 있다"),
            (.stability, "구버전에서 올려도 단축어·카테고리·즐겨찾기가 남는다"),
            (.stability, "단축어가 수백 개여도 켜기·스크롤·검색이 끊기지 않는다"),
            (.stability, "기내 모드에서 로컬 기능은 되고 백업만 실패를 알린다"),

            // 설정·표시
            (.appearance, "키 컬러를 바꾸면 앱과 키보드가 함께 바뀐다"),
            (.appearance, "목록 배경에 내 사진을 넣고 지울 수 있다"),
            (.custom, "설정에서 언어를 바꾸면 재실행 없이 바뀐다"),
            (.custom, "나에게 맞추기에 판단 근거와 챙길 세 가지가 보인다"),
            (.custom, "사용 기록에서 아낀 시간을 기간별로 본다"),
            (.stability, "친구에게 알리기 영상을 만드는 동안 화면이 멈추지 않는다"),

            // 결제
            (.monetization, "무료 10개를 넘기면 결제 화면이 뜬다"),
            (.monetization, "결제 화면을 닫으면 하던 작업으로 돌아온다"),
            (.monetization, "무료 사용자에게는 iCloud 백업이 잠겨 있다"),
            (.monetization, "Pro 혜택 화면의 숫자가 실제와 맞다"),

            // 다국어·접근성·기기
            (.appearance, "영어·중국어·러시아어에서 문구가 잘리지 않는다"),
            (.appearance, "iPad에서 목록·시트·키보드가 화면에 맞게 놓인다"),
            (.accessibility, "VoiceOver가 키보드 지구본을 다음 키보드로 읽는다"),
            (.accessibility, "VoiceOver를 켜면 두 손가락 탭이 선택 화면을 안 연다"),
        ],
        // 탭클립키보드
        "com.ysoup.TokenMemo-tap": [
            // 첫 실행과 메뉴 막대
            (.stability, "첫 실행에 쓸 수 있는 예시 단축어가 깔린다"),
            (.platform, "메뉴 막대 클립보드 아이콘이 팝오버를 연다"),
            (.platform, "아이콘을 우클릭하면 짧은 메뉴가 열린다"),
            (.appearance, "아이콘에 마우스를 올리면 ⌃⇧V가 적혀 보인다"),
            (.custom, "도움말의 온보딩 보기로 환영 화면이 다시 뜬다"),
            (.stability, "창을 다 닫아도 앱이 메뉴 막대에 남는다"),
            (.platform, "스포트라이트에서 앱 이름을 치면 목록이 열린다"),

            // 메뉴바 팝오버
            (.stability, "팝오버에서 카테고리를 바꾸면 그 항목만 보인다"),
            (.stability, "팝오버를 다시 열면 방금 만든 단축어가 있다"),
            (.custom, "검색 칸에 몇 글자만 쳐도 항목이 걸린다"),
            (.platform, "⌘1~9로 그 자리 단축어가 바로 복사된다"),
            (.custom, "위아래 화살표로 고르고 Enter로 복사한다"),
            (.custom, "항목을 우클릭해 수정·삭제를 고른다"),
            (.stability, "삭제는 다른 기기에서도 사라진다고 먼저 묻는다"),
            (.stability, "팝오버 밖을 클릭하면 창이 닫힌다"),

            // 빠른 붙여넣기 창
            (.platform, "⌃⇧V로 어디서나 빠른 붙여넣기 창이 뜬다"),
            (.stability, "항목을 누르면 복사되고 ⌘V 안내 후 창이 닫힌다"),
            (.stability, "창이 떠도 쓰던 앱의 커서가 그대로 남는다"),
            (.appearance, "제목줄 없이 한 줄짜리 행으로 촘촘히 보인다"),
            (.custom, "일반 마우스 휠로도 카테고리 줄이 옆으로 굴러간다"),
            (.stability, "단축어가 없으면 비었다는 안내가 뜬다"),
            (.custom, "⌃⇧V를 한 번 더 누르면 창이 닫힌다"),
            (.stability, "보안 단축어는 Touch ID를 통과해야 복사된다"),

            // 템플릿과 스택
            (.custom, "템플릿을 고르면 값 채우기 창이 열린다"),
            (.appearance, "값 채우기 창 아래에 완성된 글이 미리 보인다"),
            (.stability, "빈 칸은 채워지지 않고 칩으로 남아 보인다"),
            (.custom, "스택은 칸을 하나씩 골라 차례로 복사한다"),
            (.appearance, "스택 창에 2/4처럼 어디까지 넣었는지 나온다"),
            (.stability, "스택 창을 닫았다 열면 넣던 칸에서 이어진다"),
            (.appearance, "값 자리에 아이폰에서 지은 칸 이름이 보인다"),

            // 단축어 목록 창
            (.monetization, "\"무료 플랜·단축어 10개\" 문구가 보이지 않는다"),
            (.custom, "끌어서 단축어 순서를 바꾼다"),
            (.stability, "검색 중에는 순서 바꾸기가 잠긴다"),
            (.custom, "같은 단축어가 있을 때만 중복 정리 단추가 뜬다"),
            (.stability, "중복 정리는 몇 개를 지울지 먼저 알려 준다"),
            (.appearance, "마우스를 올린 행에만 수정·삭제가 나타난다"),
            (.custom, "여러 값 단축어는 고르는 창이 먼저 열린다"),
            (.stability, "검색 결과가 없으면 없다고 알려 준다"),

            // 클립보드 기록
            (.stability, "방금 복사한 글이 클립보드 기록에 쌓인다"),
            (.custom, "복사한 이미지도 기록에 그림으로 남는다"),
            (.custom, "기록 항목을 눌러 단축어로 옮겨 담는다"),
            (.custom, "전체 삭제로 기록이 한 번에 비워진다"),
            (.appearance, "복사하면 아래에 알림 띠가 잠깐 떴다 진다"),
            (.stability, "기록이 없으면 빈 안내 화면이 나온다"),
            (.custom, "백그라운드 감시를 끄면 기록이 더 안 쌓인다"),

            // 아이폰 동기화
            (.platform, "아이폰에서 추가한 단축어가 맥에 동기화된다"),
            (.stability, "동기화가 꺼져 있으면 켜라는 띠가 세워진다"),
            (.custom, "\"나중에\"를 누르면 이번 실행에는 띠가 안 뜬다"),
            (.platform, "아이폰에서 지운 단축어가 맥에서도 빠진다"),
            (.custom, "iCloud 기호를 누르면 그 자리에서 다시 받아온다"),
            (.appearance, "마지막으로 확인한 시각이 툴팁에 적힌다"),
            (.platform, "아이폰에서 켜고 끈 카테고리 탭이 맥에 온다"),
            (.stability, "iCloud에 로그인하지 않으면 오류 문구가 나온다"),

            // iCloud 백업과 파일
            (.custom, "백업하면 단축어 개수와 시각이 갱신된다"),
            (.stability, "복구는 지금 데이터가 바뀐다고 먼저 묻는다"),
            (.custom, "시점 목록에서 예전 형상으로 되돌린다"),
            (.custom, "파일로 내보내고 다시 가져온다"),
            (.stability, "가져오기는 지우지 않고 합쳐 넣는다"),
            (.stability, "받아올 것이 없으면 다시 받기를 막는다"),

            // 환경설정과 단축키
            (.stability, "로그인 시 실행을 켜면 다시 켜도 유지된다"),
            (.platform, "⌃⇧N·⌃⇧H·⌃⇧B가 각 창을 연다"),
            (.custom, "순서 탭에서 화살표로 단축어를 올리고 내린다"),
            (.appearance, "단축키 탭에 조합이 표로 정리돼 있다"),
            (.custom, "정보 탭에서 사용 설명서와 피드백이 열린다"),
            (.appearance, "정보 탭의 버전이 지금 앱 버전과 같다"),
            (.stability, "이 맥 것을 지우고 다시 받기는 앱을 다시 켠다"),
        ],
        // 퇴사각
        "com.devkoan.FireTracker": [
            // 첫 실행과 데모
            (.stability, "자산이 없으면 시작 안내 카드가 보인다"),
            (.custom, "자산 등록하기 버튼이 자산 탭으로 데려간다"),
            (.custom, "데모 데이터를 켜면 예시 자산이 채워진다"),
            (.stability, "데모를 끄면 원래 데이터로 돌아온다"),
            (.platform, "다섯 개 탭이 아래에 나란히 보인다"),

            // 대시보드
            (.stability, "첫 화면 맨 위에 FIRE 달성률이 보인다"),
            (.custom, "지난주 대비 순자산 변화가 나온다"),
            (.custom, "쓸 수 있는 돈과 묶인 돈이 나뉘어 보인다"),
            (.custom, "패시브 인컴이 월·주·연으로 환산된다"),
            (.custom, "재정 신호등이 색으로 상태를 알려 준다"),
            (.custom, "올해 말 예측에서 근거 보기가 열린다"),
            (.custom, "만약에 화면에서 빚 갚기와 투자가 비교된다"),
            (.custom, "카드의 ⓘ를 누르면 계산 설명이 열린다"),

            // 자산 탭
            (.appearance, "총자산/순자산 토글이 도넛 그래프에 반영된다"),
            (.custom, "부채가 도넛에 빨간 조각으로 들어간다"),
            (.custom, "카테고리를 접었다 펴면 종목이 숨는다"),
            (.custom, "자산 도감에서 종류별 설명을 본다"),
            (.stability, "자산을 지우면 도넛에서도 빠진다"),
            (.appearance, "도넛 가운데 금액이 조각에 가려지지 않는다"),

            // 자산 편집과 시세
            (.custom, "새로고침을 누르면 시세와 배당이 갱신된다"),
            (.stability, "네트워크가 없으면 시세가 예전 값으로 남는다"),
            (.custom, "눈금 룰러를 밀면 평가액이 단위만큼 오른다"),
            (.custom, "배당률 칩을 누르면 월 수입이 자동 계산된다"),
            (.custom, "매매 원장에 기록하면 평단이 다시 계산된다"),
            (.custom, "스크린샷으로 추가에서 종목과 금액이 읽힌다"),
            (.stability, "자산을 고치면 그날 기록이 자동 저장된다"),

            // 여정 탭
            (.custom, "여정 탭 과녁 아이콘에서 목표를 고칠 수 있다"),
            (.custom, "차트 모드 세그먼트가 차트 위에 놓인다"),
            (.custom, "순자산 선 위에 목표선이 함께 그려진다"),
            (.custom, "그래프의 점을 탭하면 날짜와 값이 뜬다"),
            (.custom, "기간별 목표가 이번달·올해·5년으로 나뉜다"),
            (.custom, "목표까지 필요한 저축액이 계산돼 나온다"),
            (.stability, "나이를 안 넣으면 기간별 목표 안내가 뜬다"),

            // 기록(스냅샷)
            (.stability, "추가한 스냅샷이 재실행 후에도 남는다"),
            (.custom, "시계 아이콘에서 기록 목록이 열린다"),
            (.custom, "기록 상세에서 패시브 인컴을 고칠 수 있다"),
            (.custom, "이 기록 삭제를 누르면 그래프에서도 빠진다"),
            (.stability, "기록이 없으면 자동으로 쌓인다고 안내한다"),
            (.custom, "목록에 저축률과 자산 개수가 함께 보인다"),

            // 계산 탭
            (.custom, "여섯 가지 계산기가 가로 칩으로 넘어간다"),
            (.custom, "대출 종류를 고르면 금리·기간이 자동 입력된다"),
            (.custom, "상환 방식을 바꾸면 월 상환액이 달라진다"),
            (.custom, "생애주기 차트에 돈이 일하는 지점이 표시된다"),
            (.custom, "투자 예측이 보수적부터 낙관까지 범위를 그린다"),
            (.custom, "구매력 탭에서 과거 월급의 실질 가치가 나온다"),
            (.custom, "시간 탭이 시급 세 가지를 나눠 보여 준다"),
            (.stability, "입력을 비우면 참고용 안내만 남는다"),

            // 저장한 계산
            (.stability, "저장한 계산을 불러오면 그때 입력값이 복원된다"),
            (.custom, "결과 아래 저장 버튼으로 이름을 붙여 담는다"),
            (.custom, "책갈피 아이콘에서 저장한 계산이 모여 보인다"),
            (.stability, "저장한 게 없으면 빈 상태 안내가 보인다"),
            (.custom, "저장한 계산을 지우면 목록에서 사라진다"),

            // 설정과 보안
            (.custom, "앱 잠금을 켜면 Face ID를 통과해야 열린다"),
            (.stability, "앱 전환기로 넘기면 자산 화면이 가려진다"),
            (.custom, "숫자로만 표시를 켜면 억·만 표기가 사라진다"),
            (.custom, "시세 API 키를 넣으면 입력됨 표시가 뜬다"),
            (.custom, "사용 통계 칸에 마지막 전송 시각이 나온다"),
            (.stability, "생체인증이 없으면 기기 암호로 넘어간다"),

            // 백업과 데이터
            (.stability, "백업 파일로 복원하면 기록이 되돌아온다"),
            (.custom, "백업 파일 내보내기로 공유 시트가 열린다"),
            (.custom, "자동 백업 목록에서 최근 것이 맨 위에 있다"),
            (.stability, "복원 전에 데이터가 교체된다고 경고한다"),
            (.stability, "잘못된 파일을 고르면 실패 알림만 뜬다"),
            (.stability, "앱을 껐다 켜도 자산과 설정이 그대로다"),
        ],
        // 팔랑북
        "com.leeo.RotoscopeiPad": [
            // 홈 화면
            (.custom, "따라 그리기 세트를 고르면 가이드 선이 보인다"),
            (.appearance, "카드가 한 화면에 꽉 차게 배치된다"),
            (.custom, "하얀 종이 카드를 누르면 빈 프로젝트가 열린다"),
            (.custom, "저장된 프로젝트에 마지막 그림이 썸네일로 뜬다"),
            (.custom, "프로젝트를 꾹 누르면 지우기가 나온다"),
            (.custom, "오른쪽 위 톱니로 지원 화면이 열린다"),
            (.stability, "프로젝트가 많아지면 홈이 세로로 스크롤된다"),

            // 동영상 가져오기
            (.platform, "카메라 카드로 그 자리에서 영상을 찍는다"),
            (.platform, "보관함에서 고른 영상이 페이지로 나뉜다"),
            (.custom, "링크 카드에 영상 주소를 붙여 열 수 있다"),
            (.stability, "네트워크가 없으면 링크 열기가 실패로 끝난다"),
            (.stability, "가져온 영상은 복사돼 오프라인에서도 열린다"),

            // 그리기 도구
            (.platform, "펜슬 전용 모드에서는 손가락으로 그려지지 않는다"),
            (.custom, "붓과 지우개를 번갈아 골라 쓴다"),
            (.custom, "색 동그라미를 고르면 자동으로 붓으로 바뀐다"),
            (.custom, "붓 굵기 점 세 개로 선 두께가 달라진다"),
            (.custom, "되돌리기로 방금 그은 선만 사라진다"),
            (.custom, "휴지통으로 지운 페이지가 되돌리기로 돌아온다"),
            (.appearance, "펜슬을 가까이 대면 닿을 자리가 미리 보인다"),

            // 도장
            (.custom, "도장을 탭하면 고른 크기로 찍힌다"),
            (.custom, "양옆 검은 띠가 도장 팔레트로 쓰인다"),
            (.custom, "도장 여덟 가지를 골라 바꿀 수 있다"),
            (.custom, "도장 크기 점이 붓 굵기와 따로 조절된다"),
            (.custom, "색을 바꾸면 도장도 그 색으로 찍힌다"),
            (.custom, "찍은 도장도 지우개와 되돌리기가 먹는다"),

            // 페이지 넘기기
            (.custom, "다음 그림 버튼으로 페이지가 넘어간다"),
            (.appearance, "페이지를 넘기면 가운데 쪽수가 잠깐 뜬다"),
            (.custom, "첫 페이지에서는 이전 버튼이 눌리지 않는다"),
            (.custom, "빈 프로젝트는 다음을 누를수록 페이지가 늘어난다"),
            (.custom, "슬라이더를 끌면 원하는 페이지로 이동한다"),
            (.custom, "⌄ 버튼으로 재생 바를 내려 캔버스를 넓게 쓴다"),

            // 재생과 녹음
            (.custom, "길게 녹음한 목소리가 재생할 때 끝까지 나온다"),
            (.custom, "재생을 누르면 그린 페이지만 이어서 돈다"),
            (.custom, "녹음 중에는 애니메이션이 계속 반복된다"),
            (.custom, "정지 버튼을 누르면 녹음이 끝난다"),
            (.platform, "마이크 권한을 거부하면 녹음이 시작되지 않는다"),
            (.appearance, "녹음 중 마이크 버튼이 빨갛게 바뀐다"),

            // 배경 가리기와 내보내기
            (.custom, "배경 가리기를 켜면 그림만 흰 종이에 남는다"),
            (.stability, "배경 가리기 설정이 프로젝트마다 저장된다"),
            (.stability, "내보낸 결과가 사진 앱에 저장된다"),
            (.appearance, "내보내는 동안 진행 막대가 찬다"),
            (.appearance, "끝나면 큰 체크 표시가 잠깐 뜬다"),
            (.platform, "사진 권한을 거부하면 저장이 실패로 끝난다"),

            // 저장과 복원
            (.stability, "저장한 프로젝트를 다시 열면 그림이 남아 있다"),
            (.stability, "앱을 다시 열면 마지막 프로젝트가 바로 열린다"),
            (.stability, "집 버튼으로 나가도 그림이 저장돼 있다"),
            (.stability, "프로젝트를 지우면 영상 캐시도 함께 사라진다"),
            (.appearance, "아이패드를 돌려도 그림 위치가 어긋나지 않는다"),
        ],
        // 펀칭
        "com.devkoan.StampCamera": [
            // 카메라 화면
            (.platform, "카메라 권한을 거부하면 안내 화면이 뜬다"),
            (.custom, "좌상단 칩에 담길 수집함 이름이 보인다"),
            (.custom, "칩 메뉴에서 새 수집함을 만들 수 있다"),
            (.custom, "셀피 버튼을 누르면 화면이 빙글 돌며 바뀐다"),
            (.custom, "화면을 탭하면 노란 초점 링이 생긴다"),
            (.custom, "두 손가락으로 벌리면 배율 숫자가 뜬다"),
            (.custom, "배율 슬라이더로 줌을 미세하게 맞춘다"),
            (.appearance, "프레임 바깥이 흐려져 창 안만 또렷하다"),

            // 촬영과 리빌
            (.stability, "촬영하면 우표 모양으로 잘려 수집함에 담긴다"),
            (.custom, "프레임을 누르면 눌렸다 떼며 펀칭 소리가 난다"),
            (.appearance, "잘린 우표가 떨어져 통으로 빨려 들어간다"),
            (.custom, "리빌 카드를 옆으로 끌면 뒷면 소인이 보인다"),
            (.custom, "리빌 화면에서 캡션을 바로 적어 담는다"),
            (.custom, "버리기를 누르면 카드가 굴러떨어지며 지워진다"),
            (.stability, "위치 권한을 거부해도 촬영은 그대로 된다"),
            (.custom, "전면 카메라로 찍어도 좌우가 뒤집히지 않는다"),

            // 수집함
            (.appearance, "우표가 3×4 포켓에 꽂히고 옆으로 넘어간다"),
            (.custom, "하루 안에 찍은 우표에 NEW 배지가 붙는다"),
            (.custom, "움직이는 우표에는 라이브 심볼이 보인다"),
            (.custom, "깔때기 메뉴로 움직이는 우표만 걸러 본다"),
            (.custom, "선택 모드에서 여러 장을 한 번에 지운다"),
            (.custom, "수집함 이름을 바꾸면 우표가 따라 옮겨진다"),
            (.stability, "빈 수집함에는 안내 문구가 보인다"),
            (.appearance, "페이지 아래에 쪽수 표시가 나온다"),

            // 우표 상세와 꾸미기
            (.custom, "우표를 꾹 누르면 크롭 전 원본 사진이 보인다"),
            (.custom, "마감 여섯 가지를 골라 색감을 바꾼다"),
            (.custom, "모습을 정지 우표와 라이브 우표로 바꾼다"),
            (.stability, "움직임이 없는 우표는 라이브 모습이 꺼져 있다"),
            (.custom, "캡션을 고치면 목록에도 바로 반영된다"),
            (.custom, "장소 이름이 우표 아래에 핀과 함께 뜬다"),
            (.custom, "복사를 누르면 복사됨 표시로 바뀐다"),

            // 컬렉션
            (.custom, "우표를 길게 눌러 전시 벽에 걸 수 있다"),
            (.custom, "새 컬렉션을 만들 때 속지 배경 열 가지를 고른다"),
            (.custom, "걸어 둔 우표를 끌어 순서를 바꾼다"),
            (.custom, "수집함으로 되돌리기로 우표가 원래 자리로 간다"),
            (.custom, "왼손잡이 모드로 부채가 반대쪽에서 펼쳐진다"),
            (.stability, "컬렉션을 지워도 우표는 수집함에 남는다"),
            (.stability, "걸린 우표가 없으면 비어 있다고 알려 준다"),

            // 인쇄와 내보내기
            (.custom, "컬렉션에서 인쇄용 스티커 시트 PDF가 만들어진다"),
            (.custom, "인쇄 가이드가 여섯 단계로 설명해 준다"),
            (.custom, "컬렉션을 한 장 이미지로 공유할 수 있다"),
            (.custom, "우표를 투명 배경 PNG로 내보낸다"),
            (.stability, "원본이 있는 우표는 인쇄 때 더 또렷하게 나온다"),

            // 메시지 스티커와 키보드
            (.platform, "메시지 스티커 서랍에 모은 우표가 보인다"),
            (.platform, "서랍 위 칩으로 컬렉션별로 걸러 본다"),
            (.platform, "라이브 우표가 서랍에서도 움직인다"),
            (.platform, "스티커 키보드에서 우표를 누르면 복사된다"),
            (.platform, "키보드에 우표가 없으면 안내 문구가 뜬다"),
            (.custom, "스티커 점검에서 안 움직이는 항목을 바로잡는다"),
            (.stability, "우표를 지우면 스티커 서랍에서도 빠진다"),

            // Siri와 제어센터
            (.platform, "Siri에 \"꾹 열기\"라고 하면 카메라가 바로 열린다"),
            (.platform, "제어센터 버튼을 누르면 카메라로 바로 간다"),
            (.platform, "잠금화면 버튼에서도 카메라가 열린다"),
            (.platform, "모음 화면이 열려 있으면 닫고 카메라로 돌아간다"),
            (.platform, "단축어 앱에서 꾹 카메라 열기가 검색된다"),

            // 설정과 데이터
            (.stability, "재실행해도 우표와 캡션이 남아 있다"),
            (.custom, "움직임 자동 재생을 끄면 어디서나 정지로 보인다"),
            (.custom, "책처럼 넘기기를 켜면 페이지가 넘어가듯 돈다"),
            (.custom, "펀칭 프레임 모양을 골라 바꿀 수 있다"),
            (.custom, "보관함 사진을 가져와 우표로 만든다"),
            (.stability, "모음 화면을 열면 카메라 초록불이 꺼진다"),
            (.stability, "비행기 모드에서도 촬영과 수집이 된다"),
        ],
        // 픽셀미미
        "com.leeo.PixelMe": [
            // 첫 실행과 홈
            (.stability, "첫 실행에 온보딩 네 장이 넘어간다"),
            (.custom, "온보딩에서 건너뛰기를 누르면 홈으로 간다"),
            (.stability, "온보딩은 다시 켜도 나오지 않는다"),
            (.custom, "홈에서 네 가지 만들기 방식이 보인다"),
            (.appearance, "홈의 픽셀 아이콘 샘플이 움직인다"),
            (.accessibility, "홈 버튼마다 VoiceOver 이름이 읽힌다"),

            // 사진 픽셀화
            (.stability, "사진을 고르면 픽셀 아트로 바뀐다"),
            (.stability, "사진 권한을 거부하면 안내가 뜬다"),
            (.custom, "픽셀 크기를 바꾸면 결과가 즉시 갱신된다"),
            (.custom, "배경 지우기로 인물만 남길 수 있다"),
            (.custom, "배경 지운 뒤 둘 중 하나를 고를 수 있다"),
            (.custom, "배경색을 바꾸면 빈 곳이 그 색이 된다"),
            (.stability, "변환 중 화면을 돌려도 결과가 남는다"),
            (.stability, "큰 사진을 넣어도 앱이 버틴다"),

            // 팔레트·필터·디더링
            (.custom, "팔레트를 고르면 색이 그 팔레트로 줄어든다"),
            (.custom, "필터 세기 슬라이더가 결과에 반영된다"),
            (.custom, "디더링 방식을 바꾸면 무늬가 달라진다"),
            (.custom, "빠른 프리셋 하나로 전체 설정이 바뀐다"),
            (.monetization, "잠긴 팔레트에는 자물쇠가 보인다"),
            (.monetization, "필터팩 상점에서 팩별 가격이 보인다"),
            (.monetization, "필터팩을 사면 소유 표시로 바뀐다"),

            // 픽셀 에디터
            (.custom, "픽셀 에디터에서 되돌리기가 된다"),
            (.custom, "연필·지우개·채우기·스포이드가 각각 동작한다"),
            (.custom, "직선·사각형·원 도구로 도형이 그려진다"),
            (.custom, "대칭 모드를 켜면 반대편도 같이 그려진다"),
            (.custom, "브러시 크기를 키우면 한 번에 넓게 칠해진다"),
            (.custom, "캔버스 크기 변경 전에 경고가 뜬다"),
            (.stability, "저장 없이 나가려 하면 확인을 묻는다"),
            (.custom, "레이어 불투명도를 줄이면 아래가 비친다"),

            // 애니메이션과 내보내기
            (.custom, "Pro에서 GIF 내보내기가 된다"),
            (.custom, "타임라인에 프레임을 추가하면 개수가 늘어난다"),
            (.custom, "FPS를 바꾸면 재생 길이가 다시 계산된다"),
            (.custom, "프레임 미리보기가 순서대로 재생된다"),
            (.custom, "배율을 고르면 내보낼 크기가 미리 보인다"),
            (.custom, "아세프라이트 파일을 열면 프레임이 들어온다"),
            (.stability, "깨진 파일을 열면 오류 안내가 뜬다"),

            // 일괄·템플릿·레이어
            (.custom, "여러 장을 골라 같은 설정으로 변환된다"),
            (.appearance, "일괄 처리 중 진행 개수가 보인다"),
            (.stability, "이미지를 안 고르면 빈 상태 안내가 보인다"),
            (.custom, "템플릿을 고르면 해당 크기로 잘린다"),
            (.custom, "레이어를 합치면 한 장으로 남는다"),
            (.custom, "레이어 숨김을 켜면 결과에서 사라진다"),

            // 공유와 프로필
            (.custom, "SNS 크기를 고르면 그 비율로 잘려 공유된다"),
            (.custom, "비포/애프터 이미지로 공유할 수 있다"),
            (.custom, "얼굴이 없는 사진에는 못 찾았다고 뜬다"),
            (.custom, "프로필 픽셀 결과를 에디터로 넘길 수 있다"),
            (.stability, "공유 시트를 취소해도 결과가 남아 있다"),
            (.platform, "아이패드에서 공유 시트가 화면 안에 뜬다"),

            // 결제와 무료 제한
            (.monetization, "무료 사용자는 하루 3회 변환 후 막힌다"),
            (.monetization, "앱을 다시 켜도 무료 변환 횟수가 유지된다"),
            (.monetization, "무료로 저장하면 워터마크가 찍힌다"),
            (.monetization, "무료는 원본 팔레트와 CRT 필터만 쓸 수 있다"),
            (.monetization, "무료는 작은 보드 크기 세 개만 고를 수 있다"),
            (.monetization, "Pro가 되면 워터마크 없이 4K로 저장된다"),

            // 설정과 데이터
            (.custom, "그리드 선 표시를 끄면 격자가 사라진다"),
            (.custom, "그리드 선 색 반전이 바로 보인다"),
            (.custom, "내 워터마크 이미지를 지정할 수 있다"),
            (.custom, "보드 배경색 변경이 캔버스에 적용된다"),
            (.stability, "저전력 모드에서도 변환이 끝까지 된다"),
            (.stability, "앱을 껐다 켜면 마지막 캔버스를 복구할 수 있다"),
        ],
        // 하늘색
        "com.leeo.SkyDex": [
            // 첫 실행과 판
            (.appearance, "처음 판은 마흔여덟 개 빈 고리로 보인다"),
            (.stability, "하늘을 찍으면 그 시각의 칸이 채워진다"),
            (.stability, "재실행해도 찍은 하늘이 판에 남아 있다"),
            (.appearance, "지금 시각 칸에 테두리 고리가 걸려 있다"),
            (.custom, "판을 누른 채 끌면 지나는 구슬이 커진다"),
            (.custom, "판 밖에서 손을 떼면 아무것도 안 골라진다"),
            (.custom, "빈 칸을 누르면 그 시각 색과 헥스가 나온다"),
            (.appearance, "판 전체가 스크롤 없이 한 화면에 들어온다"),

            // 촬영
            (.stability, "카메라를 켜자마자 셔터를 눌러도 안 꺼진다"),
            (.custom, "촬영 중 핀치로 확대되고 배율이 보인다"),
            (.appearance, "뷰파인더가 정사각이고 결과도 같은 정사각이다"),
            (.custom, "카메라 안에서 앨범 사진을 가져올 수 있다"),
            (.stability, "카메라 권한을 거부하면 안내가 보인다"),
            (.custom, "전면 카메라에서 좌우가 뒤집혀 보인다"),
            (.stability, "통화나 홈 이동 후 돌아와도 촬영이 된다"),
            (.custom, "이미 채운 칸을 다시 찍으면 새 사진으로 바뀐다"),
            (.custom, "셔터에 오늘 나가기 좋은 시각 팁이 한 번 뜬다"),

            // 한 마디 카드
            (.stability, "찍고 나면 한 마디 카드가 바로 열린다"),
            (.custom, "비어 있으면 키보드가 저절로 올라온다"),
            (.custom, "한 글자라도 쓰면 닫기가 체크로 바뀐다"),
            (.stability, "카드를 닫았다 다시 열면 쓴 글이 남아 있다"),
            (.custom, "사진 상세에 하늘색 이름이 보인다"),
            (.custom, "사진을 지우면 판의 칸도 함께 빈다"),
            (.appearance, "판에 깔린 사진 위로 한 마디가 한 글자씩 찍힌다"),

            // 다른 날의 하늘로
            (.custom, "칩을 고르면 사진의 하늘만 그 색이 된다"),
            (.custom, "세기 슬라이더로 빌린 색이 옅어진다"),
            (.custom, "원래 하늘로 버튼을 누르면 되돌아온다"),
            (.custom, "촬영 전 뷰파인더에도 고른 하늘이 적용된다"),
            (.stability, "필터를 걸어도 저장되는 색은 원본 그대로다"),
            (.stability, "카드를 닫으면 빌린 하늘이 풀린다"),

            // 기록·팔레트·되풀이
            (.stability, "아무것도 없으면 기록에 빈 상태가 보인다"),
            (.custom, "기록이 계절별로 묶여 나온다"),
            (.custom, "팔레트에서 색이 계열별로 묶여 보인다"),
            (.appearance, "팔레트 칩 크기가 계열마다 같다"),
            (.custom, "되풀이에 같은 시각의 다른 날이 나란히 선다"),
            (.custom, "되풀이에 가장 비슷했던 두 하늘이 보인다"),
            (.custom, "구슬을 누르면 그 시각의 예전 사진들이 나온다"),

            // 위젯
            (.platform, "\"오늘의 판\" 위젯에 채운 칸이 보인다"),
            (.platform, "\"오늘의 하늘\" 위젯에 오늘 사진과 색이 보인다"),
            (.platform, "오늘 안 찍었으면 위젯이 아직 없다고 말한다"),
            (.platform, "잠금화면 위젯에 색 이름과 시각이 보인다"),
            (.platform, "새 하늘을 찍으면 위젯이 곧 갱신된다"),
            (.appearance, "큰 판 위젯에는 글자가 하나도 없다"),

            // 설정과 보관
            (.custom, "설정에 보관·지원·버전 세 줄만 있다"),
            (.platform, "iCloud 스위치는 다음 실행부터 적용된다고 알린다"),
            (.stability, "기록 맨 아래에 마지막 동기화 시각이 보인다"),
            (.stability, "네트워크가 없으면 예보 없이 맑은 판으로 그린다"),
            (.custom, "버전을 일곱 번 누르면 개발자 모드가 열린다"),
            (.stability, "위치 권한을 거부해도 판이 그려진다"),
        ],
        // 한국길찾기
        "com.kora.leeo": [
            // 역 고르기
            (.stability, "출발역과 도착역을 고르면 경로가 나온다"),
            (.stability, "재실행해도 출발역과 도착역이 남아 있다"),
            (.custom, "역 이름을 검색하면 전국에서 찾아진다"),
            (.custom, "지역 칩을 바꾸면 그 지역 노선만 둘러보인다"),
            (.stability, "고른 지역 칩이 다시 켜도 그대로 남는다"),
            (.custom, "없는 역을 검색하면 결과 없음이 보인다"),
            (.platform, "위치를 허용하면 가까운 역이 먼저 제안된다"),
            (.stability, "위치 권한을 거부해도 직접 고르면 된다"),

            // 경로 확인
            (.custom, "환승 횟수와 정거장 수가 한 줄로 보인다"),
            (.custom, "노선 색으로 구간이 구분되어 보인다"),
            (.custom, "내릴 역에 핀이 표시된다"),
            (.custom, "도착까지 예상 시간이 나온다"),
            (.custom, "다시 시작으로 두 역을 지울 수 있다"),
            (.stability, "같은 역을 출발·도착으로 고르면 막힌다"),
            (.stability, "네트워크가 없어도 경로 자체는 나온다"),

            // 탑승과 진행
            (.custom, "슬라이더를 밀면 탑승 중으로 바뀐다"),
            (.appearance, "탑승 중 긴 역 이름이 잘리지 않는다"),
            (.custom, "탑승 전 배지에 호선과 행선지가 보인다"),
            (.custom, "남은 정거장 수가 한 정거장씩 줄어든다"),
            (.custom, "내릴 역이 가까우면 준비 안내로 바뀐다"),
            (.custom, "도착하면 다 왔다는 화면이 나온다"),
            (.platform, "실시간 도착이 있으면 다음 역 접근 배지가 뜬다"),
            (.stability, "앱을 껐다 켜도 탑승 중 상태가 이어진다"),

            // 환승
            (.custom, "환승 구간에서 밀어서 환승 슬라이더가 나온다"),
            (.custom, "환승하면 다음 노선 화면으로 넘어간다"),
            (.appearance, "페이지 점으로 현재 구간이 표시된다"),
            (.custom, "환승 없는 경로는 환승 없음으로 적힌다"),
            (.stability, "환승 도중 뒤로 가도 진행이 깨지지 않는다"),

            // 위치 보정과 스캔
            (.custom, "승강장 표지판을 스캔하면 방향이 맞게 나온다"),
            (.custom, "방향이 반대면 틀렸다고 알려준다"),
            (.stability, "카메라 권한을 거부하면 스캔 불가 안내가 뜬다"),
            (.custom, "위치 보정 시트에서 현재 역을 바꿀 수 있다"),
            (.custom, "위치가 불확실하면 보정 안내가 보인다"),
            (.platform, "안내방송을 들으면 내릴 문 방향이 나온다"),
            (.stability, "마이크 권한을 거부해도 안내는 계속된다"),

            // 라이브 액티비티와 위젯
            (.platform, "탑승 중 잠금화면에 라이브 액티비티가 뜬다"),
            (.platform, "앱을 스와이프로 종료하면 라이브 액티비티도 사라진다"),
            (.platform, "다이나믹 아일랜드에 다음 역이 보인다"),
            (.platform, "홈 위젯을 누르면 지하철 안내가 바로 열린다"),
            (.platform, "제어 센터 버튼으로 안내를 열 수 있다"),
            (.platform, "라이브 액티비티의 역 이름이 앱 언어를 따른다"),

            // 언어와 설정
            (.custom, "표시 언어를 바꾸면 역 이름도 바뀐다"),
            (.custom, "언어를 다섯 개 중에 고를 수 있다"),
            (.stability, "고른 표시 언어가 재실행 후에도 남는다"),
            (.appearance, "번체 기기에서 역 이름이 번체로 나온다"),
            (.appearance, "노선 이름이 고른 언어로 번역되어 보인다"),
            (.custom, "길게 눌러 언어를 바꾸는 힌트가 보인다"),
        ],
        // 햇빛바라기
        "com.leeo.sunflower": [
            // 오늘 화면과 트래킹
            (.stability, "밝은 실외에 있으면 햇빛 기록이 자동으로 시작된다"),
            (.custom, "조도 값이 Lux로 실시간 갱신된다"),
            (.custom, "트래킹 중 경과 분이 올라간다"),
            (.custom, "더블탭으로 트래킹을 끝낼 수 있다"),
            (.stability, "카메라 권한을 거부하면 자동 감지가 멈춘다"),
            (.custom, "목표를 채우면 달성 표시가 뜬다"),
            (.custom, "야외 시간을 수동으로 더할 수 있다"),
            (.stability, "백그라운드에 뒀다 돌아와도 시간이 이어진다"),

            // 배터리와 해바라기
            (.custom, "세션이 끝나면 배터리가 실제로 충전된다"),
            (.custom, "트래킹 중 예상 충전량이 미리 보인다"),
            (.stability, "배터리가 1% 미만일 때 전달 슬라이더를 움직여도 안 꺼진다"),
            (.custom, "전체 버튼으로 남은 배터리를 다 전달한다"),
            (.custom, "전달하면 해바라기 에너지가 그만큼 오른다"),
            (.appearance, "에너지가 낮으면 해바라기가 시든 모습이 된다"),
            (.appearance, "트래킹 중 해바라기가 선글라스를 쓴다"),
            (.stability, "재실행해도 해바라기 상태와 배터리가 남아 있다"),

            // 기록 탭
            (.custom, "기록 탭 캘린더에 기분 이모지가 남아 있다"),
            (.custom, "통계와 캘린더를 세그먼트로 오간다"),
            (.custom, "날짜를 누르면 그날 분·세션·최고 조도가 나온다"),
            (.custom, "기분과 한마디 메모를 저장할 수 있다"),
            (.stability, "기록이 없는 날은 없다고 표시된다"),
            (.custom, "이번 달 요약이 위에 보인다"),
            (.custom, "일별 막대 그래프로 분이 비교된다"),

            // 친구
            (.platform, "가까이 있는 친구가 자동으로 연결된다"),
            (.custom, "처음 만난 상대는 수락해야 함께 기록된다"),
            (.custom, "거절하면 그 세션 동안 다시 묻지 않는다"),
            (.appearance, "함께 받는 중에는 꽃잎이 알록달록해진다"),
            (.custom, "친구 관계도에 만난 횟수가 굵기로 보인다"),
            (.stability, "친구가 없으면 빈 상태 안내가 보인다"),

            // 위젯·라이브 액티비티·알림
            (.platform, "홈 위젯에 해바라기와 배터리가 보인다"),
            (.platform, "트래킹 중 다이나믹 아일랜드에 충전량이 보인다"),
            (.platform, "정해 둔 시간에 햇빛 알림이 온다"),
            (.platform, "위젯이 다섯 종류로 나뉘어 추가된다"),
            (.platform, "잠금화면에 조도와 예상 충전량이 보인다"),
            (.platform, "목표를 채우면 달성 알림이 온다"),
            (.stability, "알림 권한을 거부해도 앱은 그대로 쓰인다"),

            // 설정
            (.custom, "일일 목표 분을 슬라이더로 바꾼다"),
            (.custom, "실외 판정 조도 기준을 조절할 수 있다"),
            (.custom, "자동 트래킹을 끄면 수동으로만 시작된다"),
            (.custom, "함께 트래킹을 끄면 친구 연결이 멈춘다"),
            (.custom, "조도 참고표를 열어 볼 수 있다"),
            (.custom, "설정에서 앱 이름과 버전이 보인다"),
            (.custom, "햇빛의 건강 효과 설명을 볼 수 있다"),

            // 데이터와 예외
            (.stability, "데이터 초기화 전에 확인을 묻는다"),
            (.stability, "초기화하면 모든 기록이 사라진다"),
            (.stability, "실내로 들어가면 트래킹이 멈춘다"),
            (.stability, "자정이 지나면 오늘 분이 0부터 시작한다"),
            (.custom, "건강 팁을 새로고침하면 다른 팁이 나온다"),
            (.stability, "기기를 회전해도 대시보드가 깨지지 않는다"),
        ],
        // Externalize
        "com.devkoan.externalize": [
            // 첫 실행과 온보딩
            (.stability, "첫 실행에 다섯 장짜리 안내가 뜬다"),
            (.custom, "건너뛰기를 누르면 바로 목록으로 간다"),
            (.stability, "마지막 장에서는 건너뛰기가 사라진다"),
            (.custom, "물음표 단추로 안내를 다시 본다"),
            (.stability, "안내를 끝내면 다시 켜도 안 뜬다"),
            (.appearance, "한국어 기기에서 앱 이름이 뇌모리로 보인다"),

            // 메모 저장
            (.stability, "메모가 없으면 기억할 것 없다는 화면이 나온다"),
            (.stability, "저장 창을 열면 값 칸에 커서가 있다"),
            (.custom, "주차·사물함·방·번호·기타 칩 중에 고른다"),
            (.platform, "사물함·방·번호를 고르면 숫자 자판이 올라온다"),
            (.custom, "이름표를 비우면 종류 이름이 대신 쓰인다"),
            (.stability, "값이 비면 저장 단추가 눌리지 않는다"),
            (.custom, "1시간·8시간·24시간 중 만료를 고른다"),

            // 목록과 카드
            (.custom, "메모를 탭하면 값이 복사된다"),
            (.appearance, "복사한 카드에 초록 체크가 잠깐 뜬다"),
            (.platform, "복사할 때 짧은 진동이 함께 온다"),
            (.appearance, "값이 크고 둥근 글씨로 한눈에 읽힌다"),
            (.appearance, "긴 값은 두 줄로 줄어들어 담긴다"),
            (.appearance, "남은 시간 막대가 줄어들며 보인다"),
            (.appearance, "15% 아래로 떨어지면 막대가 주황이 된다"),
            (.accessibility, "VoiceOver가 카드를 한 덩어리로 읽어 준다"),

            // 만료와 시간 연장
            (.stability, "재실행해도 아직 만료되지 않은 메모가 남아 있다"),
            (.custom, "스와이프하면 만료가 24시간 늘어난다"),
            (.custom, "만료 시간이 지난 메모는 목록에서 사라진다"),
            (.custom, "왼쪽으로 밀면 그 자리에서 지워진다"),
            (.stability, "화면을 켜 둔 채로도 만료된 카드가 사라진다"),
            (.stability, "홈으로 나갔다 돌아오면 목록이 최신이 된다"),

            // 위젯
            (.platform, "잠금화면 위젯에 저장한 메모가 보인다"),
            (.platform, "원형 위젯에 값 한 줄이 크게 나온다"),
            (.platform, "한 줄 위젯에 값들이 가운뎃점으로 이어진다"),
            (.platform, "사각 위젯에서 남은 시간이 초 단위로 준다"),
            (.platform, "메모가 여럿이면 +1처럼 개수가 붙는다"),
            (.platform, "홈 화면 작은 위젯에 최대 세 개가 보인다"),
            (.stability, "만료 시각이 되면 위젯에서도 메모가 빠진다"),
            (.stability, "메모가 없으면 위젯이 빈 안내로 바뀐다"),

            // Siri와 단축어
            (.platform, "Siri에 \"뇌모리에 기억해\"라고 하면 저장된다"),
            (.platform, "값을 말하지 않으면 무엇을 기억할지 되묻는다"),
            (.platform, "단축어 앱에서 종류와 이름표까지 지정한다"),
            (.platform, "액션 버튼에 걸면 앱을 열지 않고 저장된다"),
            (.stability, "Siri로 저장한 메모가 앱을 열면 목록에 있다"),
            (.monetization, "칸이 찼으면 Siri가 앱을 열라고 답한다"),

            // 무료 한도와 구매
            (.monetization, "무료 사용자는 3번째 메모를 저장할 수 없다"),
            (.monetization, "왼쪽 위에 쓴 칸 수가 0/2로 세어진다"),
            (.monetization, "칸 수를 누르면 한도 안내가 열린다"),
            (.monetization, "잠금을 풀면 칸 수 표시가 사라진다"),
            (.stability, "네트워크가 없으면 가격 대신 오류 문구가 나온다"),
            (.monetization, "잠금을 푼 뒤에는 셋째 메모가 그냥 저장된다"),
        ],
        // 꿈을 찾아서 (BucketClimb)
        "com.bucketclimb.app": [
            // 첫 실행·온보딩
            (.custom, "온보딩 5장이 넘겨지고 시작하기가 뜬다"),
            (.custom, "건너뛰기를 누르면 바로 창고로 간다"),
            (.stability, "온보딩을 마치면 다시 뜨지 않는다"),
            (.custom, "설정에서 튜토리얼 다시 보기가 열린다"),
            (.appearance, "첫 창고 화면에 빈 상태 안내가 보인다"),
            (.custom, "빈 창고의 꿈 둘러보기가 바다 탭을 연다"),

            // 내 창고
            (.custom, "새 꿈과 단계를 추가하면 창고에 뜬다"),
            (.stability, "제목이 비면 추가 버튼이 눌리지 않는다"),
            (.custom, "단계 칸을 더하고 빼며 여러 개 넣는다"),
            (.custom, "카테고리를 고르면 카드에 아이콘이 붙는다"),
            (.appearance, "카드에 톱니 완성 개수가 표시된다"),
            (.custom, "카드를 누르면 열쇠 제작 화면이 열린다"),
            (.stability, "꿈이 많아도 창고 목록이 부드럽게 넘어간다"),

            // 꿈의 바다
            (.custom, "꿈의 바다 상자를 창고로 가져온다"),
            (.custom, "바다 상자를 누르면 톱니 목록이 보인다"),
            (.appearance, "바다 배경 물결이 계속 움직인다"),
            (.stability, "가져온 상자는 바다 목록에서 빠진다"),
            (.custom, "상자 상세의 닫기로 원래 목록에 돌아온다"),

            // 열쇠 제작
            (.custom, "단계 완료 시 톱니 진행률이 오른다"),
            (.custom, "질문에 답을 적고 기록하기로 저장한다"),
            (.custom, "완료했어요를 누르면 단계가 건너뛰어진다"),
            (.custom, "전체 단계 목록에서 남은 단계를 본다"),
            (.custom, "대표 사진을 바꾸면 재실행 후 유지된다"),
            (.custom, "원래 이미지로 복구하면 기본 사진이 온다"),
            (.custom, "이 꿈 포기하기를 누르면 확인창이 뜬다"),
            (.appearance, "모든 톱니를 채우면 상자 열기가 나타난다"),

            // 기록·타임라인
            (.custom, "내 기록에 답변이 날짜와 함께 쌓인다"),
            (.custom, "전체 기록 보기에서 지난 답이 다 보인다"),
            (.custom, "진행 기록에 사진을 붙여 저장한다"),
            (.custom, "타임라인 사진을 탭하면 크게 보인다"),
            (.stability, "사진 권한을 막으면 안내 후 되돌아온다"),
            (.stability, "기록이 없으면 빈 타임라인 문구가 뜬다"),

            // 보물함
            (.appearance, "상자 열기 애니메이션 뒤 축하가 뜬다"),
            (.custom, "연 상자가 달성한 꿈 칸에 모인다"),
            (.custom, "포기한 꿈이 잠시 멈춘 꿈으로 남는다"),
            (.custom, "멈춘 꿈을 다시 시작하면 창고로 돌아온다"),
            (.stability, "완전히 삭제를 고르면 목록에서 사라진다"),

            // 지도
            (.platform, "지도 탭에 위치 지정한 꿈이 표시된다"),
            (.custom, "장소 검색으로 꿈에 위치를 붙인다"),
            (.stability, "검색 결과가 없으면 없다고 알려 준다"),
            (.stability, "네트워크가 없으면 지도 검색이 실패한다"),
            (.appearance, "위치가 없으면 지도에 안내 문구가 보인다"),

            // 클래식 모드
            (.custom, "클래식 모드를 켜면 꿈 키우기 탭으로 바뀐다"),
            (.custom, "추천 꿈을 담으면 내 버킷리스트에 들어간다"),
            (.custom, "마일스톤 체크 시 산 등반률이 오른다"),
            (.custom, "오늘의 진행을 적으면 등반 거리가 계산된다"),
            (.custom, "아카이브 탭 표시를 켜면 탭이 하나 는다"),

            // 설정·데이터
            (.stability, "꿈과 진행 기록이 재실행 후에도 남는다"),
            (.custom, "설정 통계가 실제 꿈 개수와 맞는다"),
            (.custom, "앱 공유하기로 공유 시트가 열린다"),
            (.custom, "피드백 보내기가 메일 앱을 띄운다"),
            (.stability, "백그라운드 복귀 후 보던 화면이 그대로다"),
            (.appearance, "가로로 돌려도 카드가 잘리지 않는다"),
        ],
        // 라포맵 (RapportMap)
        "com.ysoup.RapportMap": [
            // 사람 추가·목록
            (.platform, "연락처에서 선택해 사람을 추가한다"),
            (.stability, "이름 없이 추가하면 저장이 막힌다"),
            (.platform, "이름으로 자동 찾기가 실패하면 안내가 뜬다"),
            (.stability, "연락처 권한을 막으면 수동 입력으로 진행된다"),
            (.custom, "검색창에 이름을 넣으면 목록이 좁혀진다"),
            (.custom, "필터에서 정렬과 접촉 기준을 바꾼다"),
            (.custom, "필터 초기화를 누르면 전체가 다시 보인다"),
            (.appearance, "카드에 마지막 만남이 상대 시간으로 뜬다"),

            // 사람 상세·기록
            (.custom, "빠른 기록이 사람 타임라인에 쌓인다"),
            (.custom, "고민·질문·약속 타입을 골라 기록한다"),
            (.custom, "빠른 기록에 사진을 여러 장 붙인다"),
            (.custom, "연락 날짜를 과거로 바꿔 저장한다"),
            (.custom, "프로필 사진을 고르면 원형으로 들어간다"),
            (.custom, "기록·타임라인·활동·정보 탭이 전환된다"),
            (.custom, "미해결 대화 개수가 기록 수와 맞는다"),
            (.stability, "기록이 없으면 빈 안내 문구가 보인다"),

            // 음성 녹음
            (.platform, "만남 녹음이 텍스트로 변환된다"),
            (.stability, "마이크 권한을 막으면 녹음이 막힌다"),
            (.platform, "녹음 중 레벨 표시가 소리에 따라 움직인다"),
            (.custom, "상호작용 유형을 골라 녹음을 저장한다"),
            (.platform, "전사 진행률이 퍼센트로 표시된다"),
            (.custom, "녹음 파일을 다른 앱으로 공유한다"),
            (.stability, "비행기 모드에서 전사 실패가 안내된다"),

            // 타임라인
            (.custom, "타임라인 필터로 중요 기록만 본다"),
            (.appearance, "기록이 오늘·이번 주로 묶여 보인다"),
            (.custom, "항목을 누르면 상세와 첨부가 열린다"),
            (.custom, "첨부 사진 개수가 상세에 표시된다"),
            (.stability, "삭제한 기록이 타임라인에서 사라진다"),

            // 액션·체크리스트
            (.custom, "라포 액션 체크리스트에서 단계를 바꾼다"),
            (.custom, "액션을 완료하면 달성률이 올라간다"),
            (.custom, "커스텀 액션을 직접 추가한다"),
            (.custom, "눈 모양 버튼으로 상세 화면에 노출한다"),
            (.custom, "놓치면 안 되는 것 목록에 항목을 더한다"),
            (.stability, "스와이프로 추가한 액션을 지운다"),
            (.appearance, "관계 단계 이모지가 현재 단계와 맞는다"),

            // 알림
            (.platform, "액션 리마인더 알림이 제시간에 온다"),
            (.platform, "알림 권한을 막으면 예약이 되지 않는다"),
            (.custom, "받은 알림이 알림 히스토리에 남는다"),
            (.custom, "스와이프로 읽음 표시와 삭제가 된다"),
            (.custom, "읽지 않은 알림 배지 숫자가 맞는다"),
            (.custom, "모두 읽음을 누르면 배지가 사라진다"),

            // 캘린더·멘토링
            (.platform, "캘린더에 미팅 일정이 추가된다"),
            (.platform, "캘린더 권한을 막으면 요청 버튼이 뜬다"),
            (.custom, "다가오는 일정이 상세에 목록으로 보인다"),
            (.custom, "멘토링 탭에서 세션 목록이 열린다"),
            (.custom, "텍스트 Import로 멘토링 내용이 들어온다"),
            (.custom, "멘토링 분석 단계가 순서대로 진행된다"),

            // iPad·화면
            (.platform, "아이패드에서 관계 맵 화면이 열린다"),
            (.platform, "관계 맵을 확대·축소하면 배율이 표시된다"),
            (.platform, "맵 노드를 고르면 미팅·대화 수가 보인다"),
            (.appearance, "가로로 돌려도 목록이 잘리지 않는다"),
            (.stability, "사람이 없으면 관계 맵이 빈 화면을 보여 준다"),

            // 설정·데이터
            (.stability, "iCloud 백업 후 복원(병합)이 된다"),
            (.stability, "재실행하면 보던 사람 화면이 복원된다"),
            (.custom, "설정에 마지막 백업 시각이 표시된다"),
            (.stability, "교체 복원은 확인 후에만 실행된다"),
            (.stability, "iCloud 로그아웃이면 백업 실패가 안내된다"),
            (.stability, "로컬 데이터만 삭제가 목록을 비운다"),
            (.stability, "목록으로 버튼이 복원된 화면을 닫는다"),
        ],
        // 링롱 (FindMe)
        "com.leeo.FindMe": [
            // 만들기
            (.custom, "제목과 메모를 넣으면 QR이 생성된다"),
            (.stability, "제목이 비면 QR 미리보기가 안 뜬다"),
            (.custom, "내 이름을 적으면 바로 저장된다"),
            (.custom, "메모 칸이 줄 수에 맞춰 늘어난다"),
            (.custom, "빈 곳을 탭하면 키보드가 내려간다"),
            (.custom, "북마크를 누르면 저장됨 표시가 뜬다"),
            (.stability, "내용을 고치면 저장됨 표시가 풀린다"),
            (.custom, "저장 성공 때 짧은 진동이 울린다"),
            (.custom, "저장을 반복하면 평가 요청이 한 번 뜬다"),

            // QR 상세·공유
            (.custom, "QR 사진 저장과 링크 복사가 된다"),
            (.custom, "QR 보기에서 링크 전체가 함께 보인다"),
            (.custom, "공유 시트에 메모 문구와 이미지가 담긴다"),
            (.stability, "사진 권한을 막으면 저장이 실패한다"),
            (.appearance, "복사 후 아이콘이 체크로 잠깐 바뀐다"),
            (.custom, "사용 방법 안내 4단계가 아래에 보인다"),

            // 저장됨
            (.stability, "저장한 메모가 재실행 후에도 남는다"),
            (.custom, "스와이프로 즐겨찾기와 삭제가 된다"),
            (.appearance, "즐겨찾기가 최근보다 위에 묶여 보인다"),
            (.custom, "저장 항목을 누르면 QR이 다시 열린다"),
            (.stability, "저장이 없으면 빈 목록 안내가 보인다"),
            (.stability, "많이 저장해도 최근 50개까지 남는다"),

            // App Clip
            (.platform, "QR을 스캔하면 App Clip이 메모를 보여 준다"),
            (.custom, "설정한 내 이름이 클립 화면에 표시된다"),
            (.platform, "클립에서 띵똥을 누르면 주인에게 푸시가 온다"),
            (.platform, "내 이름으로 띵똥은 이름을 적어야 보낸다"),
            (.stability, "한 번 보낸 뒤 띵똥 버튼이 잠긴다"),
            (.stability, "네트워크가 없으면 전송 실패가 뜬다"),
            (.platform, "전체 앱 다운로드가 스토어를 연다"),

            // 알림
            (.custom, "받은 확인 알림이 알림 탭에 쌓인다"),
            (.platform, "알림 권한을 막으면 푸시가 오지 않는다"),
            (.appearance, "알림이 오늘·어제·이전으로 묶인다"),
            (.custom, "탭 배지 숫자가 알림 개수와 맞는다"),
            (.custom, "지우기로 알림이 모두 사라진다"),
            (.platform, "앱을 켠 상태에서도 배너가 뜬다"),

            // 설정·데이터
            (.custom, "QR 크기를 바꾸면 다음 QR에 적용된다"),
            (.custom, "사용 방법과 App Clip 가이드가 열린다"),
            (.custom, "설정에서 이름을 바꾸면 만들기에도 반영된다"),
            (.custom, "지원 및 문의 링크가 사파리로 열린다"),
            (.stability, "모든 데이터 삭제가 저장 목록을 비운다"),
            (.stability, "삭제는 확인창을 거쳐야 실행된다"),
            (.stability, "백그라운드 복귀 후 입력 내용이 남아 있다"),
        ],
        // 사노라면 (IndonesianSmallTalk)
        "com.devkoan.IndonesianSmallTalk": [
            // 첫 실행·홈
            (.custom, "온보딩 4장을 넘기면 홈이 열린다"),
            (.stability, "온보딩 건너뛰기 후 다시 뜨지 않는다"),
            (.custom, "홈 상단 통계가 시나리오 수와 맞는다"),
            (.custom, "아래 + 버튼에서 두 가지 만들기가 열린다"),
            (.appearance, "탭은 텍스트, 마이크는 음성 안내가 보인다"),
            (.custom, "홈에서 온보딩 다시 보기가 열린다"),

            // 텍스트 연습
            (.custom, "시나리오를 탭하면 텍스트 모드가 시작된다"),
            (.platform, "상대 말풍선이 자동으로 읽어 준다"),
            (.custom, "말풍선을 탭하면 한국어 번역이 보인다"),
            (.custom, "선택지를 고르면 다음 대화로 넘어간다"),
            (.custom, "선택지를 꾹 누르면 수정·삭제가 뜬다"),
            (.custom, "같은 위치에 내 답변을 새로 추가한다"),
            (.appearance, "긍정·부정 선택에 따라 점수가 오르내린다"),
            (.custom, "대화가 끝나면 카운트다운 후 결과로 간다"),

            // 음성 연습
            (.platform, "음성 모드에서 말하면 인식되어 매칭된다"),
            (.stability, "마이크 권한을 막으면 녹음이 시작되지 않는다"),
            (.appearance, "듣는 중 상태가 화면에 표시된다"),
            (.custom, "매칭에 실패하면 다시 말하기로 이어간다"),
            (.custom, "선택지 미리듣기로 답을 먼저 들어본다"),
            (.stability, "인식 권한을 거부해도 텍스트 모드는 된다"),

            // 결과·트리
            (.custom, "트리 대화를 끝내면 결과와 복습이 나온다"),
            (.custom, "복습 줄을 탭하면 번역이 펼쳐진다"),
            (.custom, "결과에서 다시 연습하기로 처음부터 한다"),
            (.custom, "대화 트리 보기에서 분기를 따라간다"),
            (.custom, "트리 탐색 중 이전으로 돌아갈 수 있다"),
            (.custom, "코치 팁 화면에 뉘앙스 설명이 보인다"),

            // 시나리오 만들기
            (.custom, "직접 만들기로 첫 마디 시나리오를 넣는다"),
            (.platform, "받아쓰기 버튼으로 말해서 입력한다"),
            (.stability, "제목이 비면 저장 버튼이 꺼져 있다"),
            (.custom, "이모지를 골라 카드에 붙인다"),
            (.custom, "내 표현에서 고르기로 답변을 채운다"),
            (.stability, "만든 시나리오가 내 스몰토크에 남는다"),

            // AI 생성
            (.custom, "AI로 만든 시나리오가 홈에 추가된다"),
            (.stability, "API 키가 없으면 생성이 막힌다"),
            (.custom, "추천 주제를 누르면 입력칸에 채워진다"),
            (.custom, "HTML 붙여넣기 모드로도 생성된다"),
            (.stability, "네트워크가 없으면 생성 실패가 뜬다"),
            (.custom, "생성 결과의 단어장·문법이 함께 저장된다"),

            // 공유
            (.platform, "친구와 공유한 스몰토크가 양쪽에 동기화된다"),
            (.platform, "공유 링크를 메시지로 보낼 수 있다"),
            (.custom, "공유를 중지하면 카드 표시가 사라진다"),
            (.stability, "iCloud 로그아웃이면 공유 실패가 안내된다"),
            (.custom, "함께 보는 스몰토크가 홈에 따로 묶인다"),

            // 내 표현·단어장
            (.custom, "새 표현을 뉘앙스와 함께 저장한다"),
            (.custom, "표현 따라말하기에서 일치율이 나온다"),
            (.platform, "표현 듣기 버튼이 인니어를 읽어 준다"),
            (.custom, "단어장에서 단어 발음이 재생된다"),
            (.custom, "플래시카드를 뒤집어 뜻을 확인한다"),
            (.custom, "알아요로 표시한 단어가 학습 완료로 간다"),
            (.custom, "회화 모드에서 예문이 하나씩 열린다"),
            (.stability, "표현이 없으면 빈 안내 문구가 보인다"),

            // 키보드·슬랭
            (.platform, "표현 키보드가 인니어를 입력한다"),
            (.stability, "새로 추가한 내 표현이 키보드에 반영된다"),
            (.platform, "키보드 필터로 긍정·부정·슬랭을 고른다"),
            (.platform, "방향 전환 칩으로 한↔인 입력이 바뀐다"),
            (.platform, "지구본 버튼으로 다른 키보드로 넘어간다"),
            (.custom, "표시 토글을 끄면 키보드에서 사라진다"),
            (.custom, "슬랭 기본값 복원이 확인 후 실행된다"),
        ],
        // 스픽플로우 (ConversationPractice)
        "com.leeo.ConversationPractice": [
            // 첫 실행·언어 선택
            (.custom, "첫 실행에 모국어와 학습 언어를 고른다"),
            (.stability, "선택한 언어가 재실행 후에도 유지된다"),
            (.custom, "모국어를 바꾸면 화면 문구가 함께 바뀐다"),
            (.stability, "모국어와 같은 언어는 학습 목록에서 빠진다"),
            (.custom, "설정 초기화 후 언어 선택이 다시 뜬다"),
            (.custom, "학습 언어 옆에 시나리오 개수가 보인다"),

            // 카테고리·목록
            (.custom, "카테고리를 누르면 시나리오 목록이 열린다"),
            (.custom, "내 시나리오가 기본 시나리오 위에 보인다"),
            (.custom, "내 시나리오는 스와이프로 삭제된다"),
            (.appearance, "카드 너비가 두 목록에서 같게 보인다"),
            (.custom, "언어를 바꾸면 목록이 그 언어로 채워진다"),
            (.stability, "내 시나리오가 없으면 그 칸이 숨는다"),

            // 시나리오 대화
            (.custom, "시나리오 대화가 음성 인식으로 진행된다"),
            (.custom, "TTS 재생 중이라는 상태가 표시된다"),
            (.stability, "마이크 권한을 막으면 녹음이 시작되지 않는다"),
            (.custom, "표시 모드를 바꾸면 번역 노출이 달라진다"),
            (.custom, "말풍선을 탭하면 그 문장을 다시 읽어 준다"),
            (.custom, "구두점이 달라도 문장이 인식된다"),
            (.custom, "대화가 끝나면 다시 연습하기가 나온다"),
            (.stability, "새 말풍선이 생기면 화면이 아래로 따라간다"),

            // 스크립트·미리보기
            (.custom, "스크립트 보기로 전체 대화를 미리 읽는다"),
            (.custom, "스크립트에서 표시 언어를 바꿔 본다"),
            (.custom, "분기가 있는 대화는 갈래가 나뉘어 보인다"),
            (.appearance, "메신저 형태 스크립트가 좌우로 나뉜다"),
            (.stability, "대화 데이터가 없으면 안내 문구가 뜬다"),

            // 발음 연습
            (.custom, "발음 연습 후 점수가 표시된다"),
            (.appearance, "단어별 정확도가 색으로 구분된다"),
            (.custom, "번역 보기를 끄면 원문만 남는다"),
            (.custom, "다음 문장으로 넘기면 번호가 올라간다"),
            (.stability, "말하지 않고 멈추면 점수가 0으로 나온다"),
            (.stability, "소음이 큰 곳에서도 화면이 멈추지 않는다"),

            // 자유 대화
            (.custom, "자유 대화에서 번역이 함께 보인다"),
            (.custom, "녹음 버튼을 눌러 한 마디씩 주고받는다"),
            (.stability, "네트워크가 없으면 응답 실패가 안내된다"),
            (.appearance, "내 말과 상대 말이 좌우로 나뉜다"),
            (.custom, "대화를 이어가도 이전 말풍선이 남는다"),

            // 만들기·가져오기
            (.custom, "JSON 가져오기로 시나리오가 추가된다"),
            (.custom, "프롬프트 생성 후 복사가 된다"),
            (.stability, "잘못된 JSON은 오류 메시지를 보여 준다"),
            (.custom, "가져오기 전 미리보기로 턴 수를 확인한다"),
            (.custom, "직접 만들기로 제목·아이콘·턴을 넣는다"),
            (.custom, "한 턴에 선택지를 여러 개 추가한다"),
            (.stability, "내용이 비면 저장이 막힌다"),

            // 기록·설정
            (.stability, "내 시나리오와 대화 기록이 재실행 후에도 남는다"),
            (.custom, "기록이 시나리오별로 묶여 보인다"),
            (.custom, "기록 상세에서 주고받은 대화를 다시 본다"),
            (.custom, "전체 지우기로 기록이 모두 사라진다"),
            (.stability, "기록이 없으면 빈 안내가 보인다"),
            (.custom, "설정에서 개발자 문의 링크가 열린다"),
            (.stability, "백그라운드 복귀 후 대화가 이어진다"),
        ],
        // 실수 100 (JuniorDevMistakes)
        "com.leeo.JuniorDevMistakes": [
            // 첫 실행 온보딩
            (.custom, "첫 실행에 온보딩 4장이 뜬다"),
            (.custom, "건너뛰기를 누르면 바로 시작된다"),
            (.custom, "좌우로 쓸어 페이지를 넘길 수 있다"),
            (.appearance, "마지막 장에서만 시작하기로 바뀐다"),
            (.appearance, "현재 페이지 점이 길쭉하게 늘어난다"),
            (.stability, "온보딩을 마치면 다시 뜨지 않는다"),

            // 카테고리 목록
            (.monetization, "무료는 앞 3개 카테고리만 열린다"),
            (.monetization, "잠긴 카테고리를 누르면 결제 화면이 뜬다"),
            (.monetization, "잠긴 카테고리 수가 배너에 숫자로 나온다"),
            (.appearance, "잠긴 카드에 자물쇠와 PRO 딱지가 붙는다"),
            (.appearance, "카테고리마다 색과 진행 막대가 다르다"),
            (.custom, "상단 카드에 전체 진행률 퍼센트가 보인다"),
            (.custom, "카드의 완료 수가 체크한 만큼 오른다"),

            // 실수 목록·체크
            (.custom, "카테고리 상단에 소개 문단이 보인다"),
            (.custom, "행의 원을 눌러 바로 체크할 수 있다"),
            (.custom, "목록 머리에 완료 개수가 함께 나온다"),
            (.appearance, "체크한 실수는 제목에 줄이 그어진다"),
            (.appearance, "회고를 쓴 항목에 회고 완료 띠가 붙는다"),
            (.appearance, "북마크한 항목에 갈피 아이콘이 보인다"),

            // 실수 상세
            (.appearance, "상단에 #001 형식 번호가 보인다"),
            (.custom, "상황·조치·성장 세 단락이 순서대로 나온다"),
            (.custom, "툴바 체크를 누르면 경험 완료 띠가 뜬다"),
            (.custom, "툴바 갈피를 눌러 북마크를 켜고 끈다"),
            (.custom, "회고 질문 카드가 Q1부터 나열된다"),
            (.appearance, "답한 질문 카드에 체크 표시가 남는다"),
            (.stability, "체크·북마크가 재실행 후에도 남는다"),

            // 회고 작성·일지
            (.stability, "회고 시트를 열면 입력칸에 커서가 있다"),
            (.stability, "공백만 쓰면 저장 버튼이 눌리지 않는다"),
            (.custom, "회고 답변 저장 후 회고 일지에 보인다"),
            (.custom, "상세 아래 나의 회고 기록에 개수가 나온다"),
            (.custom, "회고 일지 항목을 삭제할 수 있다"),
            (.custom, "일지에서 옆으로 밀면 삭제 버튼이 나온다"),
            (.custom, "카테고리 칩으로 일지를 걸러낸다"),
            (.custom, "일지 카드에 작성 날짜가 함께 남는다"),
            (.stability, "회고가 없으면 빈 화면 안내가 보인다"),

            // 북마크
            (.stability, "북마크가 없으면 빈 화면 안내가 보인다"),
            (.custom, "북마크 탭에 담아둔 실수만 모인다"),
            (.custom, "북마크를 풀면 목록에서 바로 빠진다"),
            (.custom, "북마크 목록에서 상세로 바로 들어간다"),
            (.custom, "북마크 행에서도 체크를 켤 수 있다"),

            // 진행률
            (.custom, "실수 체크 시 진행률 탭 수치가 오른다"),
            (.appearance, "가운데 원 그래프가 채워지며 돈다"),
            (.custom, "진행 개수에 따라 응원 문구가 바뀐다"),
            (.custom, "통계 카드 네 개가 각각 수를 센다"),
            (.appearance, "100칸 격자가 체크한 칸만 진해진다"),
            (.custom, "진행률 화면에서도 초기화를 할 수 있다"),
            (.stability, "데이터 초기화 후 진행률이 0이 된다"),

            // 결제·잠금
            (.monetization, "결제 화면에 세 가지 플랜이 나온다"),
            (.monetization, "연간 플랜에 인기 딱지가 붙는다"),
            (.monetization, "플랜을 고르면 테두리가 옮겨 붙는다"),
            (.monetization, "네트워크가 없으면 다시 시도가 뜬다"),
            (.monetization, "상품을 못 불러오면 시작 버튼이 잠긴다"),
            (.monetization, "결제 화면에 약관·개인정보 링크가 있다"),
            (.monetization, "구매를 취소해도 오류 알림이 안 뜬다"),
            (.monetization, "구매가 끝나면 결제 화면이 저절로 닫힌다"),

            // 설정·데이터
            (.monetization, "구매 후 설정에 PRO 이용 중이 뜬다"),
            (.custom, "설정에 현재 버전 번호가 보인다"),
            (.stability, "초기화는 확인 알림을 거쳐야 실행된다"),
            (.stability, "초기화하면 체크·회고·북마크가 다 지워진다"),
            (.stability, "인터넷 없이도 실수 본문이 다 열린다"),
            (.stability, "백그라운드 갔다 와도 탭 위치가 유지된다"),
            (.platform, "다른 기기에서 체크한 것이 따라온다"),
            (.platform, "맥에서는 탭이 사이드바로 바뀐다"),
        ],
        // 앱 디자인 실습 (HigGym)
        "com.leeo.higgym": [
            // 첫 화면과 코스
            (.custom, "첫 화면이 목록이 아니라 코스로 열린다"),
            (.custom, "맨 위 카드에 이어서 하기가 보인다"),
            (.custom, "시작 전에는 1편부터 시작하기가 나온다"),
            (.appearance, "마친 편수만큼 진행 막대가 찬다"),
            (.appearance, "마친 레슨 행에 마침 표시가 붙는다"),
            (.appearance, "쓰다 만 레슨에 쓰는 중 표시가 뜬다"),

            // 레슨 5단계
            (.custom, "레슨 5단계를 끝까지 넘길 수 있다"),
            (.custom, "써보기에서 실제 크기 화면을 만진다"),
            (.custom, "적어보기 칸을 비워도 다음으로 넘어간다"),
            (.custom, "이유 확인에 어긴 쪽이었다는 배너가 뜬다"),
            (.custom, "내가 쓴 글이 기준 설명 위에 나란히 보인다"),
            (.custom, "비교 단계에서 고친·어긴 화면이 바뀐다"),
            (.custom, "레슨 중 책 아이콘으로 근거 본문이 열린다"),
            (.stability, "단계를 오가도 쓴 글이 지워지지 않는다"),

            // 노트
            (.stability, "열어만 본 레슨은 빈 노트를 안 남긴다"),
            (.stability, "레슨 진도·노트가 재실행 후에도 남는다"),
            (.platform, "노트를 마크다운 파일로 공유할 수 있다"),
            (.custom, "노트를 텍스트로 복사하면 복사됨으로 바뀐다"),
            (.custom, "노트에서 다시 하기로 그 레슨이 열린다"),
            (.custom, "노트 위에 쓴 편수·마친 편수가 나온다"),
            (.stability, "쓴 글이 없으면 노트 탭이 빈 안내만 보인다"),

            // 자료 — 항목 68
            (.custom, "자료 탭에 항목·실수·원칙·실습 수가 보인다"),
            (.custom, "목업 부위를 누르면 설명이 나온다"),
            (.custom, "같은 부위를 다시 누르면 설명이 닫힌다"),
            (.custom, "항목 검색으로 번호와 상황을 찾는다"),
            (.custom, "항목 상세에 여기서 나온 실수 목록이 붙는다"),
            (.platform, "참고 자료 링크가 사파리로 열린다"),
            (.stability, "검색 결과가 없으면 빈 목록만 남는다"),

            // 예제 보기
            (.custom, "예제를 처음 열면 해보세요 안내가 뜬다"),
            (.custom, "안내 시트를 연 채 뒤 화면을 만질 수 있다"),
            (.custom, "가장자리 캡슐로 예제를 닫거나 안내를 다시 연다"),
            (.custom, "안내에 그 화면의 SwiftUI 코드가 붙는다"),
            (.custom, "툴바 아이템을 늘리면 나머지가 …로 접힌다"),
            (.stability, "같은 예제를 다시 열면 안내가 자동으로 안 뜬다"),

            // 실수 100과 회고
            (.custom, "실수 목록을 분류 칩으로 걸러낸다"),
            (.custom, "회고 있는 것만 체크하면 목록이 줄어든다"),
            (.custom, "안 본 것만으로 체크한 실수를 숨긴다"),
            (.custom, "이건 나도 해봤다를 누르면 진행도가 오른다"),
            (.custom, "회고 읽기에서 증상부터 기준까지 이어진다"),
            (.custom, "전용 예제에서 어긴·고친 화면을 갈아 끼운다"),
            (.custom, "실수 상세의 근거 항목으로 바로 넘어간다"),
            (.appearance, "체크한 실수는 번호와 제목이 흐려진다"),

            // 실습실
            (.custom, "실습 설정을 바꾸면 목업이 즉시 다시 그려진다"),
            (.custom, "실시간 판정에 근거 항목 번호가 함께 붙는다"),
            (.custom, "생성된 SwiftUI 코드가 설정 따라 바뀐다"),
            (.custom, "전체 화면 실행 중에도 설정 시트를 연다"),
            (.custom, "탭을 6개로 늘리면 판정이 어김으로 바뀐다"),
            (.custom, "텍스트 실습에서 폭을 줄이면 값이 잘린다"),
            (.custom, "스크롤 엣지 실습은 배경을 사진으로도 바꾼다"),
            (.custom, "다녀온 실습에 체크 표시가 남는다"),
            (.accessibility, "글자 크기 칸을 올리면 그 자리가 무너진다"),

            // 퀴즈와 실전
            (.custom, "퀴즈 오답이 복습 목록에 쌓인다"),
            (.custom, "챕터별 숙련도 막대가 정답률만큼 찬다"),
            (.custom, "채점 전에는 근거 항목 번호가 가려져 있다"),
            (.custom, "그림 보기 문제는 채점 후에 이름이 드러난다"),
            (.custom, "해설에서 관련 실습으로 바로 넘어간다"),
            (.custom, "실전 문제는 고르기 전에 두 화면을 만져본다"),
            (.custom, "채점 후 크게 보기로 실제 크기에서 본다"),
            (.custom, "샘플 앱에서 결정을 누르면 테두리가 켜진다"),
            (.stability, "오답이 없으면 복습 모드가 목록에 없다"),

            // 데이터·복원
            (.stability, "인터넷 없이도 모든 자료가 열린다"),
            (.stability, "실수 체크와 퀴즈 성적이 재실행 후 남는다"),
            (.stability, "전체 화면 예제에서 닫기가 항상 보인다"),
            (.stability, "자료 탭에서 닫기를 누르면 제자리로 돌아온다"),
            (.stability, "백그라운드에서 돌아와도 보던 단계가 그대로다"),
            (.platform, "세로 고정이라 돌려도 화면이 눕지 않는다"),
        ],
        // 오늘의 주접 (Jujob)
        "com.leeo.JuJob": [
            // 첫 화면과 미리보기
            (.custom, "가운데 미리보기에 지금 주접이 보인다"),
            (.custom, "미리보기를 옆으로 밀면 위젯 크기가 바뀐다"),
            (.custom, "다음 주접 누르면 새 문구로 바뀐다"),
            (.appearance, "상단 배지에 현재 카테고리 이모지가 뜬다"),
            (.appearance, "아래 설정 패널이 둥근 모서리로 올라온다"),
            (.stability, "다음 주접을 연타해도 문구가 계속 바뀐다"),

            // 주접 고르기
            (.custom, "기분 선택에 15개 카테고리가 나온다"),
            (.custom, "카테고리를 고르면 시트가 바로 닫힌다"),
            (.custom, "카테고리를 바꾸면 미리보기 문구도 바뀐다"),
            (.custom, "본 주접이 히스토리에 기록된다"),
            (.appearance, "고른 카테고리 카드에 흰 테두리가 생긴다"),
            (.monetization, "무료는 앞 2개 카테고리만 열린다"),
            (.appearance, "잠긴 카테고리는 어둡게 덮이고 자물쇠가 뜬다"),

            // 즐겨찾기·히스토리
            (.custom, "별을 눌러 주접을 즐겨찾기에 담는다"),
            (.stability, "즐겨찾기가 재실행 후에도 남는다"),
            (.custom, "즐겨찾기에서 이 주접 사용을 누르면 반영된다"),
            (.stability, "즐겨찾기가 비면 안내 화면이 보인다"),
            (.custom, "히스토리 최신 항목이 맨 위에 쌓인다"),
            (.appearance, "히스토리 카드에 카테고리와 시각이 붙는다"),
            (.custom, "히스토리에서도 별로 즐겨찾기를 켠다"),
            (.stability, "히스토리가 비면 안내 화면이 보인다"),

            // 나의 기록·이름
            (.custom, "나의 기록에 현재 연속 일수가 나온다"),
            (.custom, "최고 기록과 총 확인 일수가 함께 보인다"),
            (.custom, "달성한 업적에 달성 날짜가 남는다"),
            (.appearance, "잠긴 업적은 자물쇠와 목표 일수만 보인다"),
            (.custom, "이름 설정 미리보기에 내 이름이 뜬다"),
            (.custom, "이름을 비우면 미리보기가 당신으로 돌아온다"),
            (.custom, "이름을 넣으면 주접의 당신이 바뀐다"),
            (.stability, "저장한 이름이 재실행 후에도 남는다"),

            // 위젯 꾸미기
            (.custom, "색 두 개를 고르면 미리보기가 그라데이션이 된다"),
            (.custom, "배경이미지를 고르면 미리보기에 깔린다"),
            (.custom, "휴지통을 누르면 배경이미지가 지워진다"),
            (.custom, "폰트를 바꾸면 미리보기 글씨체가 바뀐다"),
            (.custom, "행간 슬라이더를 올리면 줄 간격이 벌어진다"),
            (.custom, "글자 색을 바꾸면 미리보기에 바로 반영된다"),
            (.custom, "정렬을 왼쪽·중앙·오른쪽으로 바꾼다"),
            (.custom, "제안한 사람을 켜면 이름 줄이 나온다"),
            (.custom, "새로고침 주기를 5·15·30분으로 고른다"),

            // 위젯
            (.platform, "홈 화면 위젯에 주접 문구가 보인다"),
            (.platform, "배경·글꼴을 바꾸면 위젯에 반영된다"),
            (.platform, "위젯 추가 목록에 주접 위젯이 나온다"),
            (.platform, "앱을 나가면 위젯이 곧 갱신된다"),
            (.platform, "위젯이 정해진 주기마다 문구를 바꾼다"),
            (.platform, "제안한 사람을 켜면 위젯에도 줄이 붙는다"),
            (.platform, "긴 주접도 위젯 안에서 줄어들어 다 보인다"),

            // 안내·문의
            (.custom, "물음표를 누르면 위젯 추가 방법이 나온다"),
            (.custom, "안내가 1·2·3단계 순서로 보인다"),
            (.platform, "이메일 문의를 누르면 메일 앱이 열린다"),
            (.platform, "인스타 링크가 앱이나 사파리로 열린다"),
            (.stability, "안내 시트를 내리면 원래 화면으로 돌아온다"),

            // 데이터·복원
            (.stability, "설정한 색·폰트가 재실행 후에도 남는다"),
            (.stability, "인터넷 없이도 주접이 계속 나온다"),
            (.stability, "히스토리는 최근 100개까지만 쌓인다"),
            (.stability, "백그라운드에서 돌아와도 설정이 그대로다"),
            (.stability, "연속 기록이 하루 건너뛰면 1로 돌아간다"),
            (.stability, "같은 날 여러 번 봐도 연속이 한 번만 오른다"),
        ],
        // 확률계산기 (LottoChecker)
        "com.lottochecker.app": [
            // 첫 실행
            (.stability, "첫 실행에 면책 안내가 한 번만 뜬다"),
            (.custom, "면책 화면의 확인을 눌러야 넘어간다"),
            (.custom, "탭 세 개가 아래에 보인다"),
            (.stability, "앱을 켜면 최신 회차가 자동으로 불려온다"),
            (.stability, "불러오는 동안 로딩 표시가 보인다"),

            // 계산기 탭
            (.custom, "번호 선택 시 현재 확률이 바로 바뀐다"),
            (.custom, "여섯 개를 다 고르면 일치 개수가 나온다"),
            (.custom, "제외 모드를 켜면 번호를 뺄 수 있다"),
            (.custom, "제외한 당첨번호 개수가 따로 표시된다"),
            (.custom, "초기화를 누르면 선택이 모두 풀린다"),
            (.custom, "회차를 직접 입력해 조회할 수 있다"),
            (.stability, "없는 회차를 넣으면 잘못됐다고 알린다"),
            (.custom, "확률 정보 화면에서 등수별 확률이 보인다"),

            // 통계 분석 탭
            (.custom, "분석 범위를 바꾸면 결과가 다시 계산된다"),
            (.custom, "핫 번호와 콜드 번호가 나뉘어 보인다"),
            (.custom, "번호별 출현 빈도가 막대로 보인다"),
            (.custom, "홀짝 비율이 퍼센트로 나온다"),
            (.custom, "구간별 분포가 개수로 보인다"),
            (.custom, "자주 나온 번호 페어가 목록으로 보인다"),
            (.stability, "분석 중에는 분석 중이라고 표시된다"),
            (.custom, "급상승·급하락 번호가 표시된다"),

            // 추천 조합과 저장
            (.custom, "추천 조합 탭에서 조합이 제시된다"),
            (.custom, "조합에 이름을 붙여 저장할 수 있다"),
            (.custom, "저장한 조합의 회차 일치 개수가 나온다"),
            (.custom, "저장 조합의 평균 점수가 보인다"),
            (.stability, "조합 삭제 전에 확인을 묻는다"),
            (.stability, "저장한 조합이 없으면 빈 상태가 보인다"),

            // 당첨번호 탭
            (.custom, "최신 회차 당첨번호와 보너스가 보인다"),
            (.custom, "이전/다음으로 회차를 넘길 수 있다"),
            (.appearance, "번호 구간마다 공 색이 다르게 보인다"),
            (.custom, "다음 추첨까지 남은 시간이 초 단위로 줄어든다"),
            (.custom, "당첨금과 당첨자 수가 보인다"),
            (.custom, "확인하러가기를 누르면 당첨확인으로 넘어간다"),
            (.stability, "추첨 전 회차를 조회하면 안내가 뜬다"),

            // 당첨 확인
            (.custom, "회차를 고르고 내 번호를 넣으면 등수가 나온다"),
            (.custom, "번호를 띄어쓰기나 쉼표로 한 번에 넣을 수 있다"),
            (.stability, "1~45 밖 숫자를 넣으면 안내가 뜬다"),
            (.custom, "일치·불일치 번호가 나뉘어 보인다"),
            (.custom, "보너스 번호 일치가 따로 표시된다"),
            (.custom, "저장한 번호를 일괄 확인할 수 있다"),
            (.custom, "과거 회차 조회 시트로 이전 회차를 볼 수 있다"),

            // 구매 히스토리
            (.stability, "구매 내역이 재실행 후에도 남는다"),
            (.custom, "수동 입력으로 구매 번호를 추가한다"),
            (.custom, "자동 생성으로 번호 여섯 개가 채워진다"),
            (.custom, "회차별로 묶여서 목록이 나온다"),
            (.custom, "전체 확인으로 미확인 내역이 채점된다"),
            (.custom, "총 당첨금과 수익률이 보인다"),

            // 데이터와 예외
            (.stability, "오프라인에서도 본 회차가 캐시로 뜬다"),
            (.stability, "네트워크 오류 시 다시 시도 버튼이 나온다"),
            (.stability, "구매 내역이 없으면 빈 상태 안내가 보인다"),
            (.stability, "백그라운드에서 돌아와도 카운트다운이 맞다"),
            (.custom, "이메일과 인스타그램으로 문의할 수 있다"),
            (.stability, "회전해도 번호 격자가 깨지지 않는다"),
        ],
        // LeeoCon
        "com.leeo.LeeoCon": [
            (.platform, "메시지 앱 스티커 서랍에 LeeoCon이 보인다"),
            (.platform, "스티커를 눌러 대화에 보낼 수 있다"),
            (.platform, "스티커를 끌어 말풍선 위에 붙일 수 있다"),
            (.appearance, "스티커 가장자리가 깨지거나 잘리지 않는다"),
            (.appearance, "다크 모드 대화창에서도 스티커가 잘 보인다"),
        ],
        // TetraTint
        "com.leeo.TetraTint": [
            (.custom, "고른 두 색의 대비 비율이 보인다"),
            (.custom, "대비가 기준에 못 미치면 바로 알 수 있다"),
            (.custom, "색 값을 눌러 복사할 수 있다"),
            (.stability, "고른 색이 재실행 후에도 남는다"),
            (.platform, "창 크기를 줄여도 색 견본이 잘리지 않는다"),
        ],
        // 여울
        "com.leeo.yeoul": [
            (.stability, "타임라인에 사건을 추가하면 날짜 순으로 놓인다"),
            (.stability, "추가한 사건을 고치고 지울 수 있다"),
            (.stability, "타임라인이 재실행 후에도 남는다"),
            (.custom, "타임라인을 출력·내보내기 할 수 있다"),
            (.appearance, "사건이 많아도 타임라인 글자가 겹치지 않는다"),
        ],
        // 질문노트
        "com.lectureq.LectureQ": [
            (.stability, "질문을 적고 그 아래에 메모를 달 수 있다"),
            (.stability, "적은 질문과 메모가 재실행 후에도 남는다"),
            (.custom, "질문 목록에서 원하는 질문을 찾을 수 있다"),
            (.custom, "질문을 고치고 지울 수 있다"),
            (.platform, "단축키로 새 질문을 바로 만들 수 있다"),
        ],
    ]
}
